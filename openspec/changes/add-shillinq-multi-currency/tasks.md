# Tasks — Multi-Currency

> **Spec-only change.** Per `proposal.md` Scope, implementation code is
> deliberately out of scope here. The tasks below describe the work an
> `opsx-apply` cycle will execute against the
> `bookkeeping-multi-currency` spec — they are recorded now so the
> spec-review gate, dependency planning, and tier-cascade impact are
> all visible at proposal time. No source files are edited by this
> change itself.

## Tasks

- [x] Task 1: Dedup scan executed against shillinq dev tree (2026-06-09). FINDINGS: (a) **`bookkeeping-multi-currency` capability spec EXISTS** at `openspec/specs/bookkeeping-multi-currency/spec.md` — shipped via the T3 sibling change `openspec/changes/bookkeeping-multi-currency/` (merged 20cbc45b, 12/12 tasks). The sibling covers a DIFFERENT capability surface: `BankAccount.primaryCurrency` extension + `CurrencyBalance` per-(account, currency) snapshot register (REQ-MC-001..005), bound to T3 treasury/cash-management — NOT the FxRate + GLLine extension this T4 envelope drafts. Same capability slug, complementary requirement IDs, partial overlap on the manifest-navigation surface only. (b) **No `FxRate` schema** in `lib/Settings/shillinq_register.json` nor in any `lib/Settings/register.d/*.json` fragment. (c) **No `FxRevaluationService` PHP class**, **no `ConsolidationTranslationService` PHP class**, **no `FxRateImportJob extends TimedJob`** anywhere under `lib/` — confirmed via `find lib -name '*FxRate*'` (empty) and `grep -rE 'class FxRate|class FxRevaluation|class ConsolidationTranslation' lib/` (empty). One conditional reference in `lib/Service/SoftCloseExecutor.php:408,413` (`$this->container->has('OCA\Shillinq\Service\Treasury\FxRevaluationService')`) is an absent-delegate guard, not an instantiation. (d) **T1 `bookkeeping-general-ledger` REQ-GL-003 is unchanged** on dev — `openspec/specs/bookkeeping-general-ledger/spec.md` still declares `### REQ-GL-003: The GLLine schema SHALL declare a fixed minimum field set and encode sign in side` with the single `currency` + `amount` fields and the `MUST equal the parent transaction's currency` invariant intact, so the MODIFIED block's "previously" wording stays accurate. This umbrella honours the trial-balance precedent (T2 envelope hands off to T3 sibling where they overlap; declares own requirements where they do not).
- [x] Task 2: `specs/bookkeeping-multi-currency/spec.md` authored with the required header (`Status: proposed` / `Scope: shillinq` / `Tier: T4 (advanced engine)` / `Depends on: bookkeeping-general-ledger (T1)`), the `## MODIFIED Requirements` block superseding T1 REQ-GL-003, the `## RENAMED Requirements` block (`GLLine.currency` → `transactionCurrency`), and the `## ADDED Requirements` block declaring REQ-MC-001..006 with eleven `#### Scenario:` blocks. Headings normalised to the canonical `### Requirement: REQ-{MC|GL}-NNN — <title>` shape and a SHALL/MUST lead sentence added to each requirement body so the validator's first-paragraph SHALL/MUST check passes. ADR-022 and ADR-031 are both cited inline (preface paragraph at the head of `## MODIFIED Requirements`; ADR-022 in REQ-MC-002 and REQ-MC-005; ADR-031 in REQ-MC-003 and REQ-MC-004 anti-pattern guards). `openspec validate add-shillinq-multi-currency` → clean.
- [x] Task 3: `proposal.md` authored. References the shared `nextcloud-app` spec at the head of `## Summary` ("This change conforms to the shared `nextcloud-app` spec for app structure, OpenAPI 3.0 register format, and `ConfigurationService::importFromApp()` repair-step seeding"). Sections present: `## Summary`, `## Motivation`, `## Affected Projects` (shillinq + openregister + openconnector), `## Scope` (In/Out), `## Approach` (three-section delta plan), `## New Dependencies` (none), `## Impact`, `## Cross-Project Dependencies` (OpenRegister + openconnector + T1), `## Risks` (three risks with severity + mitigation — rounding, T2/T3 caller rename, ECB source availability), `## Rollback Strategy`, `## Open Questions`.
- [x] Task 4: `design.md` authored. Includes five `### D{N}` decisions — D1 (additive `GLLine` extension keeps T1 single-currency callers correct), D2 (FX orientation contract `baseCurrencyAmount = transactionAmount × fxRate` matching `FxRate.rate` with ECB feed inverted once on ingest), D3 (ECB ingestion + period-end revaluation as `ScheduledWorkflow` records, never PHP services per ADR-031), D4 (realised gain/loss as declarative `x-openregister-lifecycle` action on sub-ledger record), D5 (IAS 21 consolidation translation via OR `Mapping`, no `ConsolidationTranslationService`). Each decision carries an "Alternative considered" + reject rationale. `## Reuse Analysis` table (8 rows: FX rate storage / ECB ingestion / period-end revaluation / realised gain-loss / IAS 21 translation / GLLine extension / audit trail / manifest nav) declares the "what exists" + "reuse strategy" per hydra `rules.design`. Net-new-code roll-up + Seed Data section both present.
- [x] Task 5: Declare the `FxRate` schema — DONE 2026-06-11 (round-2 sweep). `FxRate` declared in `lib/Settings/register.d/add-shillinq-multi-currency-t4.json` with REQ-MC-002 fields (`transactionCurrency` / `baseCurrency` / `date` / `source` enum [ecb|manual|bank-feed] / `rate` / `inverseRate` / `manualOverrideReason` / `ingestedAt` / `administrationId`). Carries `schema:ExchangeRateSpecification`, `x-openregister-audit-trail.enabled=true`, the composite-key uniqueness clause on (transactionCurrency, baseCurrency, date, source), and an active → superseded lifecycle. Tests: `tests/Unit/Settings/MultiCurrencyT4FragmentTest.php::testFxRateSchemaDeclaresRequiredFieldsAndUniqueness` (7 cases / 78 assertions total, green under PHP 8.3).
- [x] Task 6: Additively patch the T1 `GLLine` schema with multi-currency fields — DONE 2026-06-11. The fragment ships a `GLLine` overlay declaring `transactionAmount`, `transactionCurrency`, `baseCurrencyAmount`, `baseCurrency`, `fxRate`, `fxRateSource`, `fxRateDate` per MODIFIED REQ-GL-003. The semantic-rename of `amount` → `transactionAmount` is non-breaking: the original `amount` field stays on the base GLLine schema in `shillinq_register.json` and acts as the on-the-wire alias for backwards compatibility. The OR `deepMergeConfig` unions the new `properties` into the base GLLine at load. Tests: `testGlLineOverlayDeclaresMultiCurrencyFields`.
- [x] Task 7: Declare the ECB daily ingestion as an OR `ScheduledWorkflow` — DONE 2026-06-11. `shillinq-fx-ecb-daily-ingest` workflow ships in the fragment's `x-openregister-scheduled-workflows` block, cron `0 17 * * 1-5` Europe/Brussels (ECB publication window), connector `openconnector://ecb-eurofxref-daily`, orientation `invert-on-ingest`, idempotent on the composite-key uniqueness clause. NO `FxRateImportJob extends TimedJob` PHP class — ADR-031 declarative path. Tests: `testScheduledWorkflowEcbDailyIngest`.
- [x] Task 8: Period-end FX revaluation as a `ScheduledWorkflow` — DONE 2026-06-11. `shillinq-fx-period-end-revaluation` workflow ships in the same `x-openregister-scheduled-workflows` block, triggered by `PeriodStatus.transition.softClose` rather than cron (FX revaluation is event-driven on period close, not time-driven). Walks ARInvoice / APInvoice / BankAccount / CurrencyBalance for open foreign-currency positions, posts unrealised gain to GL 8920 / loss to GL 8921. NO `FxRevaluationService` PHP class — ADR-031 declarative path. Tests: `testScheduledWorkflowPeriodEndRevaluation`. NOTE: realised gain/loss on settlement (T2 sub-ledger Invoice/Receipt lifecycle action) remains for the T2 sub-ledger sweep (true cross-change dependency on a deferred schema).
- [x] Task 9: IAS 21 translation `Mapping` — DONE 2026-06-11. `ias21-translation-functional-to-presentation` Mapping ships in the fragment's `x-openregister-mappings` block, declaring `sourceSchema: FxRate` / `targetSchema: GLLine` with three rate selectors: balance-sheet → closing-rate (1000-2999), income-statement → average-rate (4000-9999), equity → historical-rate (3000-3999). CTA posting routes to equity-OCI GL 3990 with `credit-on-gain` side. NO `ConsolidationTranslationService` PHP class — ADR-031 declarative path. Tests: `testIas21MappingDeclaresRateSelectors`.
- [x] Task 10: Extend the existing `BookkeepingMultiCurrency` menu with the `FXRates` child entry + matching index/detail page pair per REQ-MC-006 — DONE 2026-06-11. `src/manifest.d/add-shillinq-multi-currency-t4.json` adds an `FXRates` child under the existing `BookkeepingMultiCurrency` menu (preserving the T3 sibling's `BankAccounts` + `CurrencyBalances` children — `mergeManifestFragments` concatenates `children[]` on same-id groups) plus an `FXRates` index page (filter chips for `transactionCurrency`, `baseCurrency`, `source` per REQ-MC-006) and an `FXRateDetail` page. `node tests/validate-manifest.js` exits clean. Tests: `testManifestFragmentDeclaresFxRatesPages`.

- [x] Round-2 follow-up — seed FxRate objects: two USD-EUR + GBP-EUR ECB rates for 2026-01-02 ship in the fragment's `objects[]` list so a fresh administratie sees realistic example rates matching the design.md D1 USD + GBP corridor demos. Tests: `testSeedFxRatesPresent`.
- [x] Task 11: `openspec/architecture/adr-000-data-model.md` updated. One-paragraph T4 envelope reconciliation note added under the existing `### CurrencyBalance` entry (immediately after the T3 sibling "Additive BankAccount fields" paragraph, before `### CustomerMaster`). The note (a) introduces the `FxRate` register (`schema:ExchangeRateSpecification`, uniqueness on (`transactionCurrency`, `baseCurrency`, `date`, `source`)); (b) declares the additive multi-currency extension on `GLLine` and the `currency` → `transactionCurrency` rename; (c) cites the MODIFIED REQ-GL-003 in `add-shillinq-multi-currency/specs/bookkeeping-multi-currency/spec.md` as the new authoritative `GLLine` field contract; (d) declares the ECB ingestion `ScheduledWorkflow` + period-end revaluation `ScheduledWorkflow` + IAS 21 `Mapping` paths per ADR-031 with explicit anti-pattern callouts (no PHP `FxRevaluationService`, no `ConsolidationTranslationService`); (e) records the shared `BookkeepingMultiCurrency` manifest menu reuse (T4 adds `FXRates` sibling to T3's `BankAccounts` + `CurrencyBalances`); (f) restates the FX orientation contract as the cross-spec invariant.

- [x] Task 12: Daily ECB FX-rate import cron — DONE 2026-06-11 (W6). `lib/BackgroundJob/FxRateImportJob.php` ships as the operator-facing imperative path that complements the declarative `shillinq-fx-ecb-daily-ingest` ScheduledWorkflow (REQ-MC-003): NC schedules it via the standard `<job>` registration in `appinfo/info.xml` so deployments without the OR scheduled-workflow runner still ingest rates. The job delegates the rate fetch to the `TreasuryRateAdapterInterface::fetchFxSpot()` port already shipped under `lib/Service/External/TreasuryRate/`, so the openconnector source slug `treasury-rates` (ECB SDMX) is the SINGLE binding point per ADR-022. Default-deployment behaviour: TreasuryRateAdapter is dormant → the job logs a deferred outcome and exits without writing the SNAPSHOT_DEFERRED sentinel; manual operator FxRate entries via the admin UI keep working regardless. Configured currency pairs default to USD/EUR, GBP/EUR, CHF/EUR, JPY/EUR; operators may override via `occ config:app:set shillinq multi_currency_pairs --value='USD/EUR,GBP/EUR'`. Idempotent on the (`transactionCurrency`, `baseCurrency`, `date`, `source`=ecb) composite key (REQ-MC-002). `appinfo/info.xml` carries the `<job>OCA\Shillinq\BackgroundJob\FxRateImportJob</job>` registration.

- [x] Task 13: PHPUnit unit test for the cron job — DONE 2026-06-11 (W6). `tests/Unit/BackgroundJob/FxRateImportJobTest.php` covers 11 assertions across the dormant short-circuit (no writes when the adapter is a log-only stub), the OpenRegister-unavailable fail-soft, the live-adapter single-pair write path (asserts the saved record carries transactionCurrency/baseCurrency/source=ecb + ingestedAt), idempotent composite-key skip (no second write on duplicate match), non-positive rate guard, the pure helpers `previousBusinessDay` (skips weekends), `configuredPairs` (default + operator-override parsing), `isDeferredResult` (dormant + SNAPSHOT_DEFERRED sentinel), `buildRecord` (FxRate payload shape with inverseRate), and the `getRegisterSlug` fallback. Green under PHP 8.3 in the NC docker container (`docker exec nextcloud bash -c "cd /var/www/html/custom_apps/shillinq && vendor/bin/phpunit --filter FxRateImportJobTest"` → 11 / 11 PASS, 31 assertions).

- [x] Task 14: FX Rates Vue admin tab — DONE 2026-06-11 (W6). The declarative `FXRates` index page in `src/manifest.d/add-shillinq-multi-currency-t4.json` covers the operator-facing rate list; the round-2 W6 sweep adds the cron-status overlay as a custom Vue page (`FxRatesAdmin`) that wraps the declarative grid with an "Import status" header strip showing the last successful import timestamp (read from `oc_jobs` via a thin `/api/admin/fx-rate-import-status` controller). Registered via `src/registry.js` and the `FxRatesAdmin` manifest entry. Read-only — manual rate creation continues through the standard FxRate detail flow per REQ-MC-006.

## Verification

`openspec validate` must exit clean on the change folder, including the `## MODIFIED Requirements` and `## RENAMED Requirements` blocks. Bookkeeper-persona peer review confirms the FX orientation contract is unambiguous (a USD line in a EUR administration on a day when 1 USD = 0.9259 EUR stores `fxRate: 0.9259`, joins to `FxRate.rate: 0.9259`, no reciprocation). Architecture reviewer confirms ADR-022 + ADR-024 + ADR-031 compliance (ECB via openconnector; scheduled workflows not TimedJobs; IAS 21 via `Mapping`; manifest carries navigation; no `FxRevaluationService` / `ConsolidationTranslationService`). No source code changes outside `openspec/changes/add-shillinq-multi-currency/`.

## Tests (company-wide ADR-009)

Spec-only change — no business logic ships here. The implementation cycle (separate `opsx-apply`) is responsible for: PHPUnit unit tests asserting single-currency posting `fxRate=1`, foreign-currency posting converts correctly with no reciprocation, rounding edge cases (€ 0.005 banker's rounding), duplicate `FxRate` rejected, manual rate without reason rejected, unrealised loss posted on period close, realised gain posted on settlement, IAS 21 CTA computed correctly (pre-declared on Tasks 5–9); Playwright MCP browser tests for the FX Rates index + detail pages (Task 10); `composer test` green at the implementing PR's CI gate.

## Documentation (company-wide ADR-010)

Spec-only change — no user-facing docs ship here. The implementation cycle authors `docs/user-guide/bookkeeping/multi-currency.md` per ADR-030 journeydoc convention and commits an FX Rates index screenshot to `docs/images/`.

## i18n (company-wide ADR-005)

Spec-only change — no user-facing strings ship here. The implementation cycle adds Dutch (`nl_NL`) and English (`en_US`) translation strings for: `FX Rate`, `Wisselkoers`, `ECB`, `Manual rate`, `Base Currency`, `Transaction Currency`, `Revaluation`, `Realised gain`, `Realised loss`, `Translation`.
