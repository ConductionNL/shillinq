# Tasks — Multi-Currency

> **Spec-only change.** Per `proposal.md` Scope, implementation code is
> deliberately out of scope here. The tasks below describe the work an
> `opsx-apply` cycle will execute against the
> `bookkeeping-multi-currency` spec — they are recorded now so the
> spec-review gate, dependency planning, and tier-cascade impact are
> all visible at proposal time. No source files are edited by this
> change itself.

## Tasks

- [x] Task 1: Dedup scan executed against shillinq dev tree (2026-06-09). FINDINGS: (a) **`bookkeeping-multi-currency` capability spec EXISTS** at `openspec/specs/bookkeeping-multi-currency/spec.md` — shipped via the T3 sibling change `openspec/changes/bookkeeping-multi-currency/` (merged 20cbc45b, 12/12 tasks). The sibling covers a DIFFERENT capability surface: `BankAccount.primaryCurrency` extension + `CurrencyBalance` per-(account, currency) snapshot register (REQ-MC-001..005), bound to T3 treasury/cash-management — NOT the FxRate + GLLine extension this T4 envelope drafts. Same capability slug, complementary requirement IDs, partial overlap on the manifest-navigation surface only. (b) **No `FxRate` schema** in `lib/Settings/shillinq_register.json` nor in any `lib/Settings/register.d/*.json` fragment. (c) **No `FxRevaluationService` PHP class**, **no `ConsolidationTranslationService` PHP class**, **no `FxRateImportJob extends TimedJob`** anywhere under `lib/` — confirmed via `find lib -name '*FxRate*'` (empty) and `grep -rE 'class FxRate|class FxRevaluation|class ConsolidationTranslation' lib/` (empty). One conditional reference in `lib/Service/SoftCloseExecutor.php:408,413` (`$this->container->has('OCA\Shillinq\Service\Treasury\FxRevaluationService')`) is an absent-delegate guard, not an instantiation. (d) **T1 `bookkeeping-general-ledger` REQ-GL-003 is unchanged** on dev — `openspec/specs/bookkeeping-general-ledger/spec.md` still declares `### REQ-GL-003: The GLLine schema SHALL declare a fixed minimum field set and encode sign in side` with the single `currency` + `amount` fields and the `MUST equal the parent transaction's currency` invariant intact, so the MODIFIED block's "previously" wording stays accurate. This umbrella honours the trial-balance precedent (T2 envelope hands off to T3 sibling where they overlap; declares own requirements where they do not).
- [x] Task 2: `specs/bookkeeping-multi-currency/spec.md` authored with the required header (`Status: proposed` / `Scope: shillinq` / `Tier: T4 (advanced engine)` / `Depends on: bookkeeping-general-ledger (T1)`), the `## MODIFIED Requirements` block superseding T1 REQ-GL-003, the `## RENAMED Requirements` block (`GLLine.currency` → `transactionCurrency`), and the `## ADDED Requirements` block declaring REQ-MC-001..006 with eleven `#### Scenario:` blocks. Headings normalised to the canonical `### Requirement: REQ-{MC|GL}-NNN — <title>` shape and a SHALL/MUST lead sentence added to each requirement body so the validator's first-paragraph SHALL/MUST check passes. ADR-022 and ADR-031 are both cited inline (preface paragraph at the head of `## MODIFIED Requirements`; ADR-022 in REQ-MC-002 and REQ-MC-005; ADR-031 in REQ-MC-003 and REQ-MC-004 anti-pattern guards). `openspec validate add-shillinq-multi-currency` → clean.
- [x] Task 3: `proposal.md` authored. References the shared `nextcloud-app` spec at the head of `## Summary` ("This change conforms to the shared `nextcloud-app` spec for app structure, OpenAPI 3.0 register format, and `ConfigurationService::importFromApp()` repair-step seeding"). Sections present: `## Summary`, `## Motivation`, `## Affected Projects` (shillinq + openregister + openconnector), `## Scope` (In/Out), `## Approach` (three-section delta plan), `## New Dependencies` (none), `## Impact`, `## Cross-Project Dependencies` (OpenRegister + openconnector + T1), `## Risks` (three risks with severity + mitigation — rounding, T2/T3 caller rename, ECB source availability), `## Rollback Strategy`, `## Open Questions`.
- [x] Task 4: `design.md` authored. Includes five `### D{N}` decisions — D1 (additive `GLLine` extension keeps T1 single-currency callers correct), D2 (FX orientation contract `baseCurrencyAmount = transactionAmount × fxRate` matching `FxRate.rate` with ECB feed inverted once on ingest), D3 (ECB ingestion + period-end revaluation as `ScheduledWorkflow` records, never PHP services per ADR-031), D4 (realised gain/loss as declarative `x-openregister-lifecycle` action on sub-ledger record), D5 (IAS 21 consolidation translation via OR `Mapping`, no `ConsolidationTranslationService`). Each decision carries an "Alternative considered" + reject rationale. `## Reuse Analysis` table (8 rows: FX rate storage / ECB ingestion / period-end revaluation / realised gain-loss / IAS 21 translation / GLLine extension / audit trail / manifest nav) declares the "what exists" + "reuse strategy" per hydra `rules.design`. Net-new-code roll-up + Seed Data section both present.
- [~] Task 5: GAP recorded against sibling change `bookkeeping-multi-currency` (already on `development`, merged 20cbc45b). The sibling shipped a DIFFERENT multi-currency surface — `BankAccount.primaryCurrency` extension + `CurrencyBalance` per-(accountId, currency) snapshot register — and did NOT declare the `FxRate` schema this T4 envelope REQ-MC-002 specifies (no register holding the (`transactionCurrency`, `baseCurrency`, `date`, `source`) tuple with `schema:ExchangeRateSpecification` annotation, no uniqueness constraint, no `manualOverrideReason` precondition). `grep '"FxRate"|fxRate|"ecb"' lib/Settings/register.d/*.json lib/Settings/shillinq_register.json` finds only an unrelated `fxRate` local field in `bookkeeping-ifrs-16-lease.json` for lease modification accounting. Tracked as a follow-up implementation cycle against REQ-MC-002 directly; out of scope for this T2 spec-only envelope per `proposal.md ## Scope > Out of Scope` "Implementation code — this is a spec-only change".
- [~] Task 6: GAP recorded against the T1 `GLLine` schema in `lib/Settings/shillinq_register.json`. The sibling change `bookkeeping-multi-currency` (already on `development`) did NOT additively patch `GLLine` with the multi-currency field set declared by MODIFIED REQ-GL-003 — `grep -E '"baseCurrencyAmount"|"transactionCurrency"|"fxRate"|"baseCurrency"' lib/Settings/shillinq_register.json` returns empty. The T1 `GLLine` schema still carries the single-currency `amount` + `currency` field pair (T1 REQ-GL-003 verbatim). The semantic shift of `amount` → `transactionAmount` (preserving the on-the-wire property name) and the addition of `baseCurrencyAmount` / `baseCurrency` / `fxRate` / `fxRateSource` / `fxRateDate` is therefore a follow-up implementation cycle against MODIFIED REQ-GL-003 directly; out of scope for this T2 spec-only envelope. NOTE: this GAP is the prerequisite for REQ-MC-001 (which anchors against the MODIFIED REQ-GL-003 content) — a follow-up apply cycle MUST land Task 5 + Task 6 in the same commit batch (the `FxRate` register + the `GLLine` field set share the orientation contract).
- [~] Task 7: GAP recorded — no OR `ScheduledWorkflow` for daily ECB FX rate ingestion exists on dev (sibling shipped no FX ingestion record; no Workflow records of any kind reference `ecb` / `ExchangeRate` / `FxRate` in `lib/Settings/register.d/*.json` or `lib/Settings/shillinq_register.json`). Anti-pattern guard PASSED: no `FxRateImportJob extends TimedJob` class is present (`find lib/BackgroundJob -name '*Fx*' -o -name '*ExchangeRate*'` returns empty), so the implementing cycle starts from the declarative-only baseline REQ-MC-003 requires. Follow-up apply cycle MUST author the workflow + reference the openconnector ECB source by slug + invert the published rate on ingest per REQ-MC-002 orientation contract; out of scope for this T2 spec-only envelope.
- [~] Task 8: GAP recorded — no period-end FX revaluation `ScheduledWorkflow` exists on dev. Anti-pattern guard PASSED: no `FxRevaluationService` class is present anywhere under `lib/` (the only reference is a conditional `$container->has(...)` absent-delegate guard at `lib/Service/SoftCloseExecutor.php:408,413` — it does not instantiate the class, only delegates if registered, and the class itself is not defined). The realised gain/loss `x-openregister-lifecycle` action on T2 sub-ledger records is also absent. Follow-up apply cycle MUST author the period-close-triggered workflow that emits one balanced `GLTransaction` per open foreign-currency position + add the lifecycle action on the appropriate sub-ledger schema (likely the T2 `Invoice` / `Receipt` lifecycle, matched to settlement event); out of scope for this T2 spec-only envelope.
- [~] Task 9: GAP recorded — no IAS 21 translation `Mapping` record exists on dev. Anti-pattern guard PASSED: no `ConsolidationTranslationService` class is present anywhere under `lib/` (`grep -rE 'class ConsolidationTranslation' lib/` empty). The OR `Mapping` abstraction is available (consumed elsewhere — `bookkeeping-waterschappen-bbv-variant-07-mapping-detail.json` shipped a Mappings UI). Follow-up apply cycle MUST author the IAS 21 `Mapping` referencing the `FxRate` register (prerequisite: Task 5) with the closing-rate / historical-rate / average-rate rules per REQ-MC-005; out of scope for this T2 spec-only envelope.
- [~] Task 10: HANDOFF + GAP recorded against sibling change `bookkeeping-multi-currency` (already on `development`). PARTIAL: the sibling shipped a `BookkeepingMultiCurrency` parent menu entry in `src/manifest.d/bookkeeping-multi-currency.json` with two children — `BankAccounts` (`type: index` + detail) and `CurrencyBalances` (`type: index` + detail) — that satisfy the sibling REQ-MC-005 (different surface). The T4 envelope REQ-MC-006 `FX Rates` (`type: index` binding to `FxRate` with filter chips for `transactionCurrency`, `baseCurrency`, `source` + `type: detail`) page pair is NOT yet declared. The follow-up apply cycle MUST extend the existing `BookkeepingMultiCurrency` menu with a third `FXRates` child entry (rather than declare a parallel menu) and add the matching index + detail page entries; out of scope for this T2 spec-only envelope. `node tests/validate-manifest.js` exits 0 on the current state (195 pages, structural lint + consistency check both PASS).
- [x] Task 11: `openspec/architecture/adr-000-data-model.md` updated. One-paragraph T4 envelope reconciliation note added under the existing `### CurrencyBalance` entry (immediately after the T3 sibling "Additive BankAccount fields" paragraph, before `### CustomerMaster`). The note (a) introduces the `FxRate` register (`schema:ExchangeRateSpecification`, uniqueness on (`transactionCurrency`, `baseCurrency`, `date`, `source`)); (b) declares the additive multi-currency extension on `GLLine` and the `currency` → `transactionCurrency` rename; (c) cites the MODIFIED REQ-GL-003 in `add-shillinq-multi-currency/specs/bookkeeping-multi-currency/spec.md` as the new authoritative `GLLine` field contract; (d) declares the ECB ingestion `ScheduledWorkflow` + period-end revaluation `ScheduledWorkflow` + IAS 21 `Mapping` paths per ADR-031 with explicit anti-pattern callouts (no `FxRateImportJob`, no `FxRevaluationService`, no `ConsolidationTranslationService`); (e) records the shared `BookkeepingMultiCurrency` manifest menu reuse (T4 adds `FXRates` sibling to T3's `BankAccounts` + `CurrencyBalances`); (f) restates the FX orientation contract as the cross-spec invariant.

## Verification

`openspec validate` must exit clean on the change folder, including the `## MODIFIED Requirements` and `## RENAMED Requirements` blocks. Bookkeeper-persona peer review confirms the FX orientation contract is unambiguous (a USD line in a EUR administration on a day when 1 USD = 0.9259 EUR stores `fxRate: 0.9259`, joins to `FxRate.rate: 0.9259`, no reciprocation). Architecture reviewer confirms ADR-022 + ADR-024 + ADR-031 compliance (ECB via openconnector; scheduled workflows not TimedJobs; IAS 21 via `Mapping`; manifest carries navigation; no `FxRevaluationService` / `ConsolidationTranslationService`). No source code changes outside `openspec/changes/add-shillinq-multi-currency/`.

## Tests (company-wide ADR-009)

Spec-only change — no business logic ships here. The implementation cycle (separate `opsx-apply`) is responsible for: PHPUnit unit tests asserting single-currency posting `fxRate=1`, foreign-currency posting converts correctly with no reciprocation, rounding edge cases (€ 0.005 banker's rounding), duplicate `FxRate` rejected, manual rate without reason rejected, unrealised loss posted on period close, realised gain posted on settlement, IAS 21 CTA computed correctly (pre-declared on Tasks 5–9); Playwright MCP browser tests for the FX Rates index + detail pages (Task 10); `composer test` green at the implementing PR's CI gate.

## Documentation (company-wide ADR-010)

Spec-only change — no user-facing docs ship here. The implementation cycle authors `docs/user-guide/bookkeeping/multi-currency.md` per ADR-030 journeydoc convention and commits an FX Rates index screenshot to `docs/images/`.

## i18n (company-wide ADR-005)

Spec-only change — no user-facing strings ship here. The implementation cycle adds Dutch (`nl_NL`) and English (`en_US`) translation strings for: `FX Rate`, `Wisselkoers`, `ECB`, `Manual rate`, `Base Currency`, `Transaction Currency`, `Revaluation`, `Realised gain`, `Realised loss`, `Translation`.
