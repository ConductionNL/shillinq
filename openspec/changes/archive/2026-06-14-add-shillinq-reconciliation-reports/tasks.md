# Tasks — Reconciliation Reports

> **Spec-only change.** Per `proposal.md` Scope, implementation code is
> deliberately out of scope here. The tasks below describe the work an
> `opsx-apply` cycle will execute against the
> `bookkeeping-reconciliation-reports` spec — they are recorded now so
> the spec-review gate, dependency planning, and tier-cascade impact
> are all visible at proposal time. No source files are edited by
> this change itself.

## Tasks

- [x] Task 1: Dedup scan executed against shillinq dev tree (2026-06-09). FINDINGS: (a) Sibling capability spec `openspec/specs/bookkeeping-reconciliation-reports/spec.md` IS already on `development` — shipped via the sibling change `openspec/changes/bookkeeping-reconciliation-reports/` whose 10 requirements (`REQ-REC-001..010`) cover the **bank-reconciliation-centric** surface (`BankReconciliation` + `ReconciliationMatch` + `ReconciliationReport` schemas, statement-balance verification, unmatched-item resolution, T2 event consumption). The T2 umbrella's `REQ-RR-001..007` surface is a DIFFERENT slice (sub-ledger ↔ GL control-account match, intercompany match across administrations, variance vs `Budget`, consolidating controller exception report, `Budget` register) — the two requirement sets do not overlap by name and target a different operational surface (controller exception reports vs bank-reconciliation workflow). (b) `Budget` schema IS already declared in TWO register fragment files: `lib/Settings/register.d/bookkeeping-verplichtingenadministratie.json` (slug `Budget`, purpose `dimension`, vrije_ruimte formula for verplichtingenadministratie) and `lib/Settings/register.d/bookkeeping-provincies-bbv-variant.json` (slug `Budget`, BBV programme budget for provincie administrations). NEITHER carries the REQ-RR-004 shape (`accountNumber`, `periodId`, `budgetAmount`, `currency`, `administrationId`, `lifecycleState`) — those are programme/commitment-scoped Budget records, not the account-period budget shape this T2 envelope needs. (c) `lib/Service/` carries `DepositReconciliationService.php` (booking deposits, NOT bookkeeping reconciliation), `TaxReportCalculator.php` + `TaxReportService.php` (tax filing generation), `TaxPaymentReconciliationService.php` (BTW payment matching), `EmuReportingService.php` (EMU statistics filing), `OssPaymentReconciliation.php` (OSS VAT), `SisaReportingService.php` (SiSa subsidy reporting) — every one targets a different capability domain than the generic ledger reconciliation/variance reporting engine REQ-RR-001 forbids. No bookkeeping-ledger `*Report*.php` / `*Reconciliation*.php` / `*Variance*.php` reporting service exists. (d) `openspec/architecture/adr-000-data-model.md` carries a generic `### Budget` entry (L1026, "Primary spec: budget-planning-control") with a programme-level shape (`budgetName`, `totalAmount`, `fiscalYear`, `costCenter`) — the T2 envelope's account-period shape (`accountNumber + periodId + budgetAmount`) is NOT yet noted; Task 12 captures the reconciliation.
- [x] Task 2: `specs/bookkeeping-reconciliation-reports/spec.md` authored with the required header (`Status: proposed` / `Scope: shillinq` / `Tier: T4 (advanced engine)` / `Depends on: bookkeeping-general-ledger (T1), bookkeeping-accounts-payable-core (T2), bookkeeping-accounts-receivable-core (T2)`), seven `### Requirement: REQ-RR-NNN — <title>` blocks (REQ-RR-001 saved-query-not-engine, REQ-RR-002 sub-ledger↔GL match, REQ-RR-003 intercompany match, REQ-RR-004 variance vs Budget, REQ-RR-005 controller exception report, REQ-RR-006 manifest navigation, REQ-RR-007 launchpad runtime-GraphQL no-dep) using RFC 2119 keywords (SHALL/MUST in every body), and `#### Scenario:` blocks with GIVEN/WHEN/THEN for each requirement (no-engine reviewer-grep, launchpad-by-slug, balanced AP-vs-GL zero variance, mismatched AR-vs-GL exception, matched IC pair zero variance, unmatched IC leg exception, within-threshold variance not flagged, above-threshold variance flagged, exception report all-three-classes-one-query, controller-exceptions detail page rendering, launchpad no-shillinq-dep). Heading shape normalised to the canonical `### Requirement: REQ-RR-NNN — <title>` form and first-prose-line collapsed onto a single physical line so the `containsShallOrMust` extractor sees the in-paragraph `MUST` token — `openspec validate add-shillinq-reconciliation-reports --strict` exits clean. `feedback_launchpad-no-or-dependency.md` cited inline in REQ-RR-007.
- [x] Task 3: `proposal.md` authored. References the shared `nextcloud-app` spec at the head of `## Summary` ("This change conforms to the shared `nextcloud-app` spec for app structure, OpenAPI 3.0 register format, and `ConfigurationService::importFromApp()` repair-step seeding"). Sections present: `## Summary`, `## Motivation`, `## Affected Projects` (shillinq adds `Budget` + saved-query records + manifest entries; openregister and launchpad are no-source-change consumers), `## Scope` (In: capability spec, `Budget` register, four saved-query records, severity calculated field, launchpad runtime-GraphQL, manifest nav; Out: impl code, PHP report engine, bespoke Vue, launchpad widget authoring), `## Approach`, `## New Dependencies` (none), `## Impact`, `## Cross-Project Dependencies` (OR aggregation engine + RBAC; T1 GL; T2 AP/AR; launchpad GraphQL discovery), `## Risks` (three risks — Risk 1: OR aggregation engine cannot express cross-administration intercompany match / cross-schema variance join declaratively, mitigated via ADR-031 single-method guard exception; Risk 2: reports become a parallel reporting engine via creep, mitigated by REQ-RR-001 hard prohibition + reviewer gate; Risk 3: launchpad inadvertently grows a `shillinq` install-time dep, mitigated by `feedback_launchpad-no-or-dependency.md` + e2e test gate), `## Rollback Strategy`, `## Open Questions` (cross-admin intercompany engine support resolved in opsx-ff; default budget-variance threshold deferred to implementing-cycle UX review).
- [x] Task 4: `design.md` authored. `## Decisions` block carries four named decisions: `### D1 — Reports are aggregation queries, not a report engine` (ADR-031 binding; no `ReportingService.generateReconciliation()`; both consumers — launchpad widgets + manifest detail pages — read the same `x-openregister-aggregations`), `### D2 — Severity classification is a calculated field` (pure function of row data; sort-by-severity is a query-level op, no service-side sorting), `### D3 — launchpad consumes via runtime GraphQL, no install-time dep` (per `feedback_launchpad-no-or-dependency.md`; alternative-considered + rejected), `### D4 — Cross-administration intercompany match resolution` (engine-vs-guard decision deferred to `opsx-ff` discovery; if guard needed → `lib/Aggregation/IntercompanyMatchGuard.php` single-method ~20 LOC ADR-031-exception-annotated, same shape for `BudgetVarianceJoinGuard.php`). `## Reuse Analysis` table covers ten capability rows (sub-ledger ↔ GL recon, intercompany match, variance vs budget, controller exception report, Budget storage, severity classification, launchpad consumption, audit trail, RBAC, manifest navigation) — each with strategy + reuse path. `## Seed Data` documents the deliberate absence (budgets are administration-specific, saved-query records ship as part of the register declaration). Net-new code in the implementing cycle scoped to 1 schema + 4 saved-query records + 2 manifest entry pairs + up to 2 ~20 LOC guards if engine confirms it cannot express the cross-admin/cross-schema joins.
- [x] Task 5: HANDOFF to implementing cycle (separate `opsx-apply` against `bookkeeping-reconciliation-reports`). GAP confirmed on `development`: two `Budget` schemas exist in fragment files but neither carries the REQ-RR-004 account-period shape — `lib/Settings/register.d/bookkeeping-verplichtingenadministratie.json` declares `Budget` with `programmaCode + kostenplaats + boekjaar + geautoriseerd_bedrag + gerealiseerd_bedrag + openstaande_verplichtingen + vrije_ruimte` (commitment-administration shape; required `[administrationId, boekjaar, geautoriseerd_bedrag]`), and `lib/Settings/register.d/bookkeeping-provincies-bbv-variant.json` declares `Budget` with `budgetName + totalAmount + programmaStructure + fiscalYear + status` (BBV-programme shape; required `[budgetName, totalAmount, programmaStructure, status, fiscalYear, administrationId]`). REQ-RR-004 needs `(accountNumber, periodId, budgetAmount, currency, administrationId, lifecycleState)` — account-per-period granularity to join against `GLLine` aggregations. Since OR allows multiple registers to carry the same schema slug under different fragments, the implementing cycle's options are: (a) declare a third `Budget` in a new fragment file owned by this capability (preferred — keeps account-period budgets cleanly separated from programme/commitment budgets), or (b) extend the verplichtingenadministratie shape with optional `accountNumber + periodId + budgetAmount` fields and live with the union schema. Decision deferred to the implementing cycle's design-discovery; the spec's REQ-RR-004 stays shape-neutral on the storage layout. FK target `Account.accountNumber` exists (sibling `add-shillinq-chart-of-accounts` shipped, `Account` schema carries `accountNumber` as the key). Out of scope for this T2 spec-only envelope. (HANDOFF verified — sibling on dev)
- [x] Task 6: HANDOFF to implementing cycle (separate `opsx-apply` against `bookkeeping-reconciliation-reports`). GAP confirmed on `development`: no `SavedQuery` record carrying the REQ-RR-002 sub-ledger ↔ GL match aggregation exists in any `lib/Settings/register.d/*.json` fragment. OR's `SavedQuery` / `x-openregister-aggregations` primitive is the canonical home (per ADR-031); the implementing cycle declares one saved-query record per control account (AP control + AR control), joining T2 AP/AR sub-ledger objects (sibling specs `bookkeeping-accounts-payable-core` / `bookkeeping-accounts-receivable-core` shipped on `development`) to T1 `GLLine` aggregations (sibling `bookkeeping-general-ledger` shipped) grouped by `(controlAccountNumber, period)` and emitting `(subLedgerOpenBalance, glControlAccountBalance, difference, isReconciled)` rows with the `|difference| > administrationTolerance` exception flag as a calculated field. Out of scope for this T2 spec-only envelope. (HANDOFF verified — sibling on dev)
- [x] Task 7: HANDOFF to implementing cycle (separate `opsx-apply` against `bookkeeping-reconciliation-reports`). GAP confirmed on `development`: no `SavedQuery` record carrying the REQ-RR-003 intercompany match aggregation exists. The aggregation crosses two administrations (group-member legs) joined via a `Group` register declared in T5 intercompany (not yet shipped — `groupId` FK is pre-positioned in REQ-RR-003 prose without enforcing the FK target's existence). Per design.md D4, the implementing cycle's `opsx-ff` discovery phase resolves whether OR's aggregation engine can express the cross-administration join declaratively; if not, the fallback is a single-method PHP guard (`lib/Aggregation/IntercompanyMatchGuard.php::matchPostings(string $groupId, string $periodId): array`, ~20 LOC, ADR-031 exception annotated) called by the aggregation engine. The spec stays shape-neutral. Out of scope for this T2 spec-only envelope. (HANDOFF verified — sibling on dev)
- [x] Task 8: HANDOFF to implementing cycle (separate `opsx-apply` against `bookkeeping-reconciliation-reports`). GAP confirmed on `development`: no `SavedQuery` record carrying the REQ-RR-004 variance-vs-Budget aggregation exists. The aggregation joins T1 `GLLine` aggregations (grouped by `(accountNumber, periodId)`) to the account-period `Budget` shape (gap-locked behind Task 5; either a new fragment or extended verplichtingenadministratie shape). Cross-schema join classified as a candidate for the ADR-031 guard fallback per design.md D4 (`lib/Aggregation/BudgetVarianceJoinGuard.php::computeVariance(string $accountNumber, string $periodId, string $administrationId): array`, ~20 LOC) if OR's aggregation engine cannot express it declaratively — decision deferred to the implementing cycle's discovery phase. Threshold check (`|variancePct| > administrationVarianceThreshold`, default 10%) ships as a calculated field on the saved-query row, not a service-side comparison. Out of scope for this T2 spec-only envelope. (HANDOFF verified — sibling on dev)
- [x] Task 9: HANDOFF to implementing cycle (separate `opsx-apply` against `bookkeeping-reconciliation-reports`). GAP confirmed on `development`: no consolidating `SavedQuery` record per REQ-RR-005 exists. The consolidating query unions exception rows from the three upstream saved queries (REQ-RR-002 sub-ledger mismatches, REQ-RR-003 unmatched intercompany legs, REQ-RR-004 above-threshold variances) into a single result set; severity classification (`critical` / `warning` / `info`) is a calculated field per row driven by row data (variance magnitude × budget size, days-open × amount, etc. per design.md D2); the result set sorts by severity descending then administration. launchpad consumes this query via runtime GraphQL to render the controller's home dashboard widget. Out of scope for this T2 spec-only envelope. (HANDOFF verified — sibling on dev)
- [x] Task 10: HANDOFF to implementing cycle (separate `opsx-apply` against `bookkeeping-reconciliation-reports`). GAP confirmed on `development`: `src/manifest.json` carries no `Bookkeeping > Reconciliation Reports` menu/page entries — grep for `reconciliation report` / `exception report` / `controller-except` / `sub-ledger reconcil` / `variance report` / `budgets` finds zero matches. Existing `BankReconciliation` entries (5 manifest hits) belong to the T3 sibling and target the statement-matching workflow, not the REQ-RR-006 reports catalogue. The implementing cycle adds: (a) a `Bookkeeping > Reconciliation Reports` menu entry with `type: index` page listing the four saved-query records as a catalogue, (b) one `type: detail` page per saved-query slug (`subledger-ap-vs-gl-1700`, `subledger-ar-vs-gl-1300`, `intercompany-match`, `budget-vs-actual`, `controller-exceptions`) rendered by the generic `CnIndexPage` / `CnDetailPage` driven by the saved-query metadata, plus (c) a `Bookkeeping > Budgets` index/detail pair bound to the new account-period `Budget` schema. `node tests/validate-manifest.js` must exit 0 against the implementing-cycle's manifest. Out of scope for this T2 spec-only envelope. (HANDOFF verified — sibling on dev)
- [x] Task 11: HANDOFF to implementing cycle (separate `opsx-apply` against `bookkeeping-reconciliation-reports`) — CONDITIONAL fallback only. Trigger condition: `opsx-ff` discovery confirms OR's aggregation engine cannot express the cross-administration intercompany join (REQ-RR-003) or the cross-schema budget-variance join (REQ-RR-004) declaratively. If so, the implementing cycle authors `lib/Aggregation/IntercompanyMatchGuard.php` (single method `matchPostings(string $groupId, string $periodId): array`, ~20 LOC) and/or `lib/Aggregation/BudgetVarianceJoinGuard.php` (single method `computeVariance(string $accountNumber, string $periodId, string $administrationId): array`); each PHP guard carries an inline ADR-031-exception annotation linking back to `design.md` D4 and identifying the OR engine capability gap. If discovery shows the aggregation engine CAN express both joins declaratively, NO guards are authored and the saved-query records (Tasks 7 + 8) carry the full implementation. The fallback path is bounded — single method, ~20 LOC, no orchestration — to prevent the ADR-031 anti-pattern (PHP service mirroring an Exact / AFAS-style report engine) from creeping back in. Out of scope for this T2 spec-only envelope. (HANDOFF verified — sibling on dev)
- [x] Task 12: `openspec/architecture/adr-000-data-model.md` updated. One-paragraph reconciliation note appended under the existing `### Budget` entry (L1046+) declaring the T4 capability binding — introduces the **account-period budget shape** (`accountNumber + periodId + budgetAmount + currency + administrationId + lifecycleState`, FK `accountNumber → Account.accountNumber`) alongside the existing programme-level `Budget` documented above; cross-references the two prior fragment-file Budget declarations (`bookkeeping-verplichtingenadministratie.json` commitment shape and `bookkeeping-provincies-bbv-variant.json` BBV-programme shape) and notes the implementing-cycle decision to either add a third Budget fragment or extend one of the existing shapes (Task 5 carries the deferred decision); records the four reconciliation saved queries (sub-ledger ↔ GL control match, intercompany match, variance vs Budget, controller exception report per REQ-RR-002 / -003 / -004 / -005) and that launchpad consumes them via runtime GraphQL with no install-time dependency in either direction per ADR-022 + `feedback_launchpad-no-or-dependency.md`; cites ADR-031 binding (`x-openregister-aggregations` not a PHP `ReportingService`; severity calculated field; conditional single-method guard fallback for cross-administration intercompany / cross-schema budget-variance joins if engine cannot express declaratively).

## Verification

`openspec validate` must exit clean on the change folder. Architecture reviewer confirms ADR-022 + ADR-024 + ADR-031 compliance — specifically: no `lib/Service/*Report*.php` / `*Reconciliation*.php` / `*Variance*.php` classes; reports declared as `SavedQuery` / `x-openregister-aggregations`; severity as calculated field; launchpad carries no install-time dep on shillinq (grep `launchpad/appinfo/info.xml` for `<dependencies>`); if conditional guards are authored, each is single-method with ADR-031 exception annotation. No source code changes outside `openspec/changes/add-shillinq-reconciliation-reports/`.

## Tests (company-wide ADR-009)

Spec-only change — no business logic ships here. The implementation cycle (separate `opsx-apply`) is responsible for: PHPUnit unit tests asserting matched reconciliation reports zero variance; mismatched surfaces as exception; intercompany match for grouped administrations; within-threshold variance does not flag; exception report sorted by severity; launchpad GraphQL discovery (pre-declared on Tasks 5–9); if conditional guards land, PHPUnit covers matched-pair zero variance, unmatched leg returns open amount, mixed-currency match uses base-currency amount, within/above-threshold variance cases (Task 11); Playwright MCP browser tests for the Reports + Budgets pages and launchpad widget end-to-end (Task 10); `composer test` green at the implementing PR's CI gate.

## Documentation (company-wide ADR-010)

Spec-only change — no user-facing docs ship here. The implementation cycle authors `docs/user-guide/bookkeeping/reconciliation-reports.md` per ADR-030 journeydoc convention and commits a controller-exception-report screenshot to `docs/images/`.

## i18n (company-wide ADR-005)

Spec-only change — no user-facing strings ship here. The implementation cycle adds Dutch (`nl_NL`) and English (`en_US`) translation strings for: `Reconciliation Report`, `Aansluitingsrapport`, `Variance`, `Verschil`, `Exception`, `Uitzondering`, `Controller`, `Controlerend Boekhouder`, `Budget`, `Budget vs Actual`, `Critical`, `Warning`, `Info`, `Severity`.
