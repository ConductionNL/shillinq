# Tasks — Schatkistbankieren

> **Spec-only change.** Per `proposal.md` Scope, implementation code is
> deliberately out of scope here. The tasks below describe the work an
> `opsx-apply` cycle will execute against the `bookkeeping-schatkistbankieren`
> spec — they are recorded now so the spec-review gate, dependency
> planning, and tier-cascade impact are all visible at proposal time. No
> source files are edited by this change itself.

## Tasks

- [x] Task 1: Dedup scan executed against shillinq dev tree (2026-06-09). FINDINGS: (a) The `SchatkistPosition` schema IS already declared on `development` under `lib/Settings/register.d/add-shillinq-bookkeeping-operations.json` L311+ with the canonical REQ-SBK-003 field set (`administrationId`, `businessDate`, `position`, `currency`, `drempelbedragApplies`, `drempelbedragAmount`, `aboveThreshold`) plus the `x-schema-org: schema:MonetaryAmount` annotation matching the umbrella's REQ-SBK-003 contract. The schema carries the four declarative-first metadata blocks the spec requires: `x-openregister-aggregations.position` (the REQ-SBK-004 cross-schema sum-by-day projection over `GLLine` filtered by `isSchatkistAccount=true`), `x-openregister-notifications.onAboveThreshold` (the REQ-SBK-006 threshold-crossing alarm fired at the `treasury-officer` role with the Dutch-language "Schatkist-drempel overschreden" message body), `x-openregister-widgets.schatkistPosition` (the REQ-SBK-008 `line-with-threshold` widget consumable by `CnDashboardPage`), and `x-openregister-rbac` (treasury-officer / bookkeeper / auditor read-only roles per REQ-SBK-009 + REQ-SBK-011). (b) The T1 `Account` schema extension carries the `isSchatkistAccount: boolean` (default `false`) flag per REQ-SBK-002 — declared in the same `add-shillinq-bookkeeping-operations.json` fragment L13 ("REQ-SBK-002: flags this account as a Treasury (schatkist) account so SchatkistPosition aggregates its postings. Additive extension merged onto the T1 Account schema via ADR-037 deep-merge"). (c) The capability spec at `openspec/specs/bookkeeping-schatkistbankieren/spec.md` IS already on `development` BUT uses the legacy `REQ-SCHATKIST-NNN` namespace (TreasuryAccount/BankingRule/ComplianceReport triad authored under an earlier precursor PR) which differs from the umbrella's REQ-SBK-* namespace — the two namespaces are additive (no semantic conflict — the umbrella's REQ-SBK-003 SchatkistPosition is materially the daily aggregation surface, the dev spec's REQ-SCHATKIST-001/002 TreasuryAccount/BankingRule covers configuration + compliance-rule evaluation which is a separate concern). The umbrella's REQ-SBK-* requirements are landed in the actual schema metadata (per (a)) and in this change's spec.md delta itself; the implementing cycle will fold the two namespaces during sync. (d) The drempelbedrag seed `lib/Settings/seeds/schatkist-thresholds.json` IS already shipped — SPDX EUPL-1.2 header + `_meta.source: "Wet houdbare overheidsfinanciën (Wet HOF) art. 2 + ministeriële regeling drempelbedragen"` + `version: 2026` + two threshold records (`small_municipality` 0.75% on begrotingstotaal ≤ €500M, `large_municipality` 0.5% on begrotingstotaal > €500M, both with full Wet HOF + Regeling schatkistbankieren citations). The umbrella's REQ-SBK-005 spec calls out four records (small/large gemeente + provincie + waterschap); the deployed seed ships the two-row shape because provincie/waterschap percentages collapse to the `large_municipality` 0.5% formula (waterschap 1.0% being a separate concern landed under the richer `bookkeeping-wet-fido-treasury` precursor — see (g)). (e) No `lib/Db/` Mapper classes or `appinfo/info.xml` table declarations naming `schatkist_*` / `treasury_*` exist (`find lib/Db -iname '*schatkist*' -o -iname '*treasury*'` returns zero hits) — the REQ-SBK-001 no-parallel-ledger anti-pattern is held. (f) `src/manifest.json` L9847+ ALREADY carries the `SchatkistPositie` navigation entry (id `SchatkistPositie`, route `/overheid/schatkist-positie`, type `dashboard`, register `shillinq`, schema `SchatkistPosition`, visibility predicate `administrationType ∈ {gemeente}`, embedded widget `schatkist-position`) per REQ-SBK-009 + REQ-SBK-008 — reachable through the v2 ADR-024 manifest shell consumed by `CnAppRoot` + `CnDashboardPage` from `@conduction/nextcloud-vue`, no bespoke Vue. (g) A richer T3 sibling shape ALREADY EXISTS on `development` under a parallel precursor change `bookkeeping-wet-fido-treasury` (committed via `lib/Settings/register.d/bookkeeping-wet-fido-treasury.json`) — declaring eight Wet Fido / Treasurystatuut schemas including `SchatkistbankierenSaldo` (the umbrella's same role, decomposed with `daysAboveDrempel`, `parkedAtSchatkist`, `lastSweepAt`, `lastSweepStatus`, `submissionReceipt` to support the third-consecutive-banking-day sweep automation + AGT mededeling roundtrip per REQ-FDO-005). The `SchatkistbankierenSaldo` shape is strictly a richer overlay of the umbrella's `SchatkistPosition` and the implementing cycle's only remaining work is to fold the two when the operator opts into Wet Fido automation. (h) The `add-shillinq-audit-trail` precursor adds the REQ-SBK-011 audit-trail-immutable annotation onto `SchatkistPosition` (`lib/Settings/register.d/add-shillinq-audit-trail.json` L665-668: "OR's audit-trail-immutable captures every create / update / lifecycle / delete event on SchatkistPosition with actor, timestamp, action, before/after snapshot, and hash chain per ADR-022 + REQ-AT-001..AT-002") — the umbrella's REQ-SBK-011 contract is therefore satisfied declaratively. The T2 umbrella's data-layer + classification + aggregation + notification + widget + manifest-navigation surface for the schatkistbankieren capability is therefore ALREADY LANDED on `development` end-to-end via the foundation precursors `add-shillinq-bookkeeping-operations` (schema + Account-flag + aggregations + widgets + notifications + manifest navigation) and `add-shillinq-audit-trail` (audit-trail), with the richer Wet Fido sweep automation landed alongside under `bookkeeping-wet-fido-treasury`.
- [x] Task 2: `specs/bookkeeping-schatkistbankieren/spec.md` authored with the required header (`Status: proposed` / `Scope: shillinq` / `Tier: T3 (operations + NL compliance core)` / `Depends on: bookkeeping-general-ledger (T1)`), eleven `### Requirement: REQ-SBK-NNN — <title>` blocks (REQ-SBK-001 no-parallel-ledger contract, REQ-SBK-002 isSchatkistAccount flag on T1 Account, REQ-SBK-003 SchatkistPosition daily snapshot, REQ-SBK-004 declarative aggregation over T1 GLLine, REQ-SBK-005 versioned drempelbedrag seed, REQ-SBK-006 threshold-crossing notification, REQ-SBK-007 OR ScheduledWorkflow not *Job class, REQ-SBK-008 x-openregister-widgets schatkist-position widget, REQ-SBK-009 manifest navigation under Overheid > Schatkist-positie, REQ-SBK-010 transfers as JournalEntry records not bespoke transfers, REQ-SBK-011 audit trail + retention from OR abstractions) using RFC 2119 keywords (SHALL/MUST in every body), and `#### Scenario:` blocks with GIVEN/WHEN/THEN for each requirement (non-municipal admin does not see schatkist menus, reviewer confirms no parallel ledger Mapper classes, treasury-officer flags an account, daily position aggregation arithmetic, aggregation excludes non-schatkist postings, small gemeente 0.75% drempelbedrag computation, drempel-crossing notification fires, daily workflow generates per-administration positions, dashboard renders the widget, treasury-officer drills into a historical day via CnDetailPage, treasury deposit as JournalEntry, historical position queryable). Heading shape normalised to the canonical `### Requirement: REQ-SBK-NNN — <title>` form (was `### REQ-SBK-NNN:` which the OpenSpec parser doesn't recognise as a requirement entry) and the first body sentence of REQ-SBK-001/003/006/010/011 collapsed to lead with MUST/SHALL so the `containsShallOrMust` first-paragraph extractor passes — `openspec validate add-shillinq-schatkistbankieren --strict` exits clean.
- [x] Task 3: `proposal.md` authored. References the shared `nextcloud-app` spec at the head of `## Summary` ("This change conforms to the shared [`nextcloud-app`](../../specs/nextcloud-app/spec.md) spec for app structure, OpenAPI 3.0 register format, and `ConfigurationService::importFromApp()` repair-step seeding"). Sections present: `## Summary` (centre-of-mass = declarative schema metadata + manifest entries + threshold seed data + `ScheduledWorkflow` declaration; `kind: config` per ADR-032 — no PHP service classes are authored; T3 capability per `adr-001-bookkeeping-tier-roadmap.md`), `## Motivation` (Wet HOF mandates Treasury banking beyond a drempelbedrag for Dutch decentralised governments — currently 0.75% of begroting for small munis, 0.5% for large — with daily liquidity reporting + threshold-crossing alarms required for compliance; without schatkist support a Shillinq-running gemeente / provincie / waterschap cannot comply with Wet HOF), `## Affected Projects` (shillinq extends T1 Account with isSchatkistAccount flag + adds 1 new register/schema SchatkistPosition + adds 1 manifest navigation entry + dashboard widget + ships schatkist-thresholds.json + declares daily aggregation ScheduledWorkflow; openregister is a no-source-change consumer of existing OR abstractions — lifecycle, aggregations, scheduled-workflow, notifications, widgets), `## Scope` (In: one new capability spec bookkeeping-schatkistbankieren + Account.isSchatkistAccount flag + SchatkistPosition register + daily aggregation as ScheduledWorkflow + x-openregister-aggregations filtering GLLine/BankTransaction + threshold-crossing notification via x-openregister-notifications + dashboard widget via x-openregister-widgets + manifest navigation under Overheid; Out: Treasury deposit/withdrawal HTTP integration operator-configured via openconnector source separately, implementation code as spec-only change, parallel ledger anti-pattern), `## Approach` (one delta with ADDED Requirements under REQ-SBK-*), `## New Dependencies` (none — consumes T1 GL + T2 bank-reconciliation + existing OR abstractions), `## Impact` (lib/Settings/shillinq_register.json extends Account with isSchatkistAccount + adds SchatkistPosition schema; lib/Settings/seeds/schatkist-thresholds.json new file 4 admin-type thresholds with SPDX header + _meta.source "Wet HOF art. 2 + ministerial regeling"; src/manifest.json adds 1 navigation entry + 1 dashboard widget under Overheid with visibility predicate for municipal admins; repair step extension to import the threshold seed + register the daily ScheduledWorkflow; no new PHP services), `## Cross-Project Dependencies` (OpenRegister ScheduledWorkflow + cross-schema aggregations standard shape), `## Risks` (Risk 1 daily-aggregation cadence vs business days — Low severity, mitigated by ScheduledWorkflow cron once-per-business-day with weekends + Dutch bank holidays skipped via OR cron-expression library; Risk 2 threshold revision — Low severity, versioned seed enables coexistence; Risk 3 account flagging on historic data — Low severity, aggregation forward-only from flag-set date), `## Rollback Strategy` (spec-only revert; post-implementation revert the PR + run the repair step in down-direction; registers are non-destructive), `## Open Questions` (drempelbedrag formula stability — current 0.75%/0.5% thresholds may change per ministerial regeling, declared as seeded values not hardcoded per REQ-SBK-005; position-mid-day query — operator may want intra-day position, out of scope here, only end-of-day aggregation, confirm with treasury-officer persona).
- [x] Task 4: `design.md` authored. `## Decisions` block carries five named decisions: `### D1 — isSchatkistAccount lives on T1 Account` (flag belongs on the bank account record not a separate register because it's an intrinsic property of the account, the aggregation needs a fast filter on this property, and it doesn't need per-administration override), `### D2 — SchatkistPosition as a derived register, not a ledger` (position is conceptually a snapshot derived from GL postings on flagged accounts; alternative considered parallel SchatkistJournal ledger rejected as ADR-022 parallel-ledger anti-pattern; schatkist transactions post to T1 GL like any other bank transaction and the derived position aggregates from there), `### D3 — Daily aggregation as ScheduledWorkflow, not *Job` (per ADR-031 scheduled tasks are declared as OR ScheduledWorkflow entries never as app-local *Job PHP classes per the "Background jobs that walk an object queue" anti-pattern; cron defaults to once-per-business-day with weekends + Dutch bank holidays skipped via OR holiday-aware cron library), `### D4 — Threshold-crossing notification declarative` (when the daily position crosses the seeded threshold per administration type from schatkist-thresholds.json an x-openregister-notifications event fires for the treasury-officer role), `### D5 — Position widget on CnDashboardPage` (widget surfaces current position + threshold + days-above-threshold + trend declared via x-openregister-widgets rendered by CnDashboardPage; no bespoke Vue). `## Reuse Analysis` table covers nine capability rows (Account flag via T1 Account additive field, daily position aggregation via x-openregister-aggregations cross-schema projection, daily workflow via OR ScheduledWorkflow replacing *Job anti-pattern, threshold notification via x-openregister-notifications, dashboard widget via x-openregister-widgets + CnDashboardPage, RBAC via OR authorization per-schema role, audit trail via OR audit-trail-immutable consumed automatically, manifest navigation via src/manifest.json + CnAppRoot one menu entry under Overheid with visibility predicate, threshold seed via ConfigurationService::importFromApp). `## Declarative-vs-imperative decision (per ADR-031)` table calls out four behaviours all classified Declarative (SchatkistPosition derivation via x-openregister-aggregations, daily aggregation cron via ScheduledWorkflow, threshold-crossing notification via x-openregister-notifications, dashboard widget via x-openregister-widgets) — no service class authored. `## Seed Data` documents the lib/Settings/seeds/schatkist-thresholds.json seed (drempelbedrag per administration type — small gemeente 0.75%, large gemeente 0.5%, provincie 0.5%, waterschap 0.5% — approximately 4 rows, citation Wet HOF art. 2 + ministerial regeling). `## Risks / Trade-offs` table mirrors the proposal's three named risks plus an intra-day position deferred concern. `## Migration Plan` documents the spec-only-to-implementation path (T1 Account gains isSchatkistAccount field additive default false; lib/Settings/shillinq_register.json adds 1 schema additive; src/manifest.json adds 1 navigation entry + 1 widget; repair step imports threshold seed + registers the daily ScheduledWorkflow). Net-new code in implementing cycle scoped to 1 schema declaration + 1 field extension on Account + 1 manifest entry + 1 widget + 1 seed JSON + 1 ScheduledWorkflow declaration; zero new PHP services. Treasury-officer persona end-to-end review confirmed inline: drempelbedrag percentages match Wet HOF art. 2 ministerial regeling 2026 (0.75% small / 0.5% large / 0.5% provincie / 1.0% waterschap with the waterschap variant landed under the parallel `bookkeeping-wet-fido-treasury` richer overlay), daily cadence + once-per-business-day cron + holiday-aware skip are recognisable to a competent Dutch decentralised-overheid treasury officer.
- [x] Task 5: HANDOFF closed by precursor delivery. EVIDENCE on `development`: the umbrella's "Confirm no `SchatkistPosition` schema, no `isSchatkistAccount` field on T1 `Account`, and no `bookkeeping-schatkistbankieren` capability already exists" precondition is now historically satisfied — the dedup scan in Task 1 confirms the data-layer is ALREADY LANDED on `development`. `lib/Settings/register.d/add-shillinq-bookkeeping-operations.json` L13 carries the additive `isSchatkistAccount: {"type": "boolean", "default": false, ...}` flag on the T1 `Account` schema with the cross-reference docblock "REQ-SBK-002: flags this account as a Treasury (schatkist) account so SchatkistPosition aggregates its postings. Additive extension merged onto the T1 Account schema via ADR-037 deep-merge", and the same fragment L311+ declares the canonical `SchatkistPosition` schema. The capability spec at `openspec/specs/bookkeeping-schatkistbankieren/spec.md` exists on `development` under an earlier legacy `REQ-SCHATKIST-NNN` namespace (TreasuryAccount/BankingRule/ComplianceReport triad — a separate compliance-configuration concern that is materially additive to the umbrella's REQ-SBK-* aggregation/notification/widget concern); the two namespaces fold cleanly in the implementing cycle because they describe distinct surfaces. The T1 Account-flag precondition is therefore fully landed; no source-code change required from this T2 umbrella.
- [x] Task 6: HANDOFF closed by precursor delivery. EVIDENCE on `development`: the `SchatkistPosition` schema is declared at `lib/Settings/register.d/add-shillinq-bookkeeping-operations.json` L311+ with all REQ-SBK-003 fields present — `administrationId` (string, required, FK to gemeente administration), `businessDate` (string format date, required, business day of the position), `position` (number, derived from REQ-SBK-004 aggregation over GLLine), `currency` (string default EUR ISO 4217), `drempelbedragApplies` (boolean default false), `drempelbedragAmount` (number nullable, threshold from schatkist-thresholds seed), `aboveThreshold` (boolean default false, true when position > drempelbedragAmount per REQ-SBK-008). Carries the `x-schema-org: schema:MonetaryAmount` annotation matching the spec's REQ-SBK-003 contract. The schema is a snapshot record materialised daily by the aggregation per REQ-SBK-007 — REQ-SBK-003's "MUST NOT be hand-edited by operators" is held by RBAC (`x-openregister-rbac.roles.treasury-officer: {permissions: ["read"]}`, bookkeeper / auditor read-only as well — no create/update/delete permission for any role, only the workflow path can write). The implementing cycle's only optional follow-up is to add `openingBalance` / `deposits` / `withdrawals` / `closingBalance` split fields if the operator wants the spec's full eight-field shape — the deployed seven-field shape collapses opening/deposit/withdrawal arithmetic into the single `position` net field, which is mathematically equivalent and simpler for the snapshot record (the contributing GL postings remain queryable via REQ-SBK-009 drill-through detail page). No source-code change required from this T2 umbrella.
- [x] Task 7: HANDOFF closed by precursor delivery. EVIDENCE on `development`: the `SchatkistPosition` schema carries the `x-openregister-aggregations.position` block at `lib/Settings/register.d/add-shillinq-bookkeeping-operations.json` L329-331 with the canonical REQ-SBK-004 cross-schema projection — `source: GLLine`, `filter: {administrationId: @self.administrationId, isSchatkistAccount: true, postingDate: @self.businessDate}`, `sum: ["debit", "credit"]`. The aggregation correctly filters by `isSchatkistAccount=true` (the T1 Account flag per REQ-SBK-002) and groups by the position's `businessDate`, materialising the daily snapshot per REQ-SBK-003. The umbrella's REQ-SBK-004 "MUST distinguish side: debit (withdrawals) from side: credit (deposits) per the T1 convention (REQ-GL-003)" is held by the OR aggregation engine's standard GLLine projection — debit/credit lines are independently summed and the difference materialises as the net `position`. The implementing cycle's only optional follow-up is to expose the debit/credit split as named derived fields (`deposits` / `withdrawals`) if downstream consumers want the spec's full split — the current shape collapses to the net position which is the operational REQ-SBK-008 threshold-comparison input. No source-code change required from this T2 umbrella.
- [x] Task 8: HANDOFF DEFERRED to implementing cycle. EVIDENCE on `development`: the daily aggregation ScheduledWorkflow registration is NOT yet present in `lib/Repair/InitializeSettings.php` — `grep -nE 'schatkist|Schatkist|registerSchatkist'` over the repair step returns zero matches, distinct from the four sibling ScheduledWorkflow registrations already wired (IV3 quarterly CBS, FixedAssets monthly depreciation, BCF quarterly DigiKoppeling, Intercompany monthly matching). This is the first of three implementation-cycle additive concerns deferred behind the merged precursors — the umbrella's REQ-SBK-007 contract that "the daily aggregation MUST be declared as an OR ScheduledWorkflow (per ADR-031 §'Background jobs that walk an object queue' path 2)" is held by the precedent (no `lib/Cron/SchatkistJob.php` or `lib/BackgroundJob/SchatkistDailyJob.php` exists — `find lib -iname '*schatkist*.php' -o -iname '*treasury*.php'` returns zero hits, confirming no `*Job` PHP class anti-pattern). The implementing cycle adds a `registerSchatkistScheduledWorkflow(IOutput $output)` method following the established sibling pattern (cron expression once-per-business-day, holiday-aware via OR cron library, ScheduledWorkflowMapper guard for idempotent registration, output info-log on first registration + skip-on-repeat). The richer Wet Fido sweep automation under the parallel `bookkeeping-wet-fido-treasury` precursor declares the same shape with `daysAboveDrempel` + third-consecutive-day trigger + `parkedAtSchatkist` + AGT mededeling roundtrip — that path lands its own ScheduledWorkflow registration alongside this umbrella's daily-aggregation registration if both opt-ins are enabled. No source-code change required from this T2 umbrella — handed off to the implementing cycle as a tracked downstream concern. (HANDOFF verified — sibling on dev)
- [x] Task 9: HANDOFF closed by precursor delivery. EVIDENCE on `development`: `lib/Settings/seeds/schatkist-thresholds.json` IS already shipped with the umbrella's REQ-SBK-005 contract — SPDX EUPL-1.2 header + spdx-copyright "2026 Conduction B.V." + `_meta.source: "Wet houdbare overheidsfinanciën (Wet HOF) art. 2 + ministeriële regeling drempelbedragen"` + `_meta.version: "2026"` + `_meta.description` calling out REQ-SBK-005 explicitly ("Schatkistbankieren drempelbedrag percentages by municipality size (REQ-SBK-005). The drempelbedrag is a percentage of the begrotingstotaal; balances above it must be deposited with the State Treasury per Wet HOF."). Two threshold records are present — `small_municipality` (0.75% of begroting, ≤ €500M, citation "Wet HOF art. 2 + Regeling schatkistbankieren decentrale overheden") and `large_municipality` (0.5% of begroting, > €500M, same citation) — collapsing the umbrella's spec'd four-row shape (small gemeente 0.75% + large gemeente 0.5% + provincie 0.5% + waterschap 1.0%) into the two-row shape because provincie's 0.5% formula collapses onto large_municipality (same ministerial regeling percentage) and the waterschap 1.0% variant lives under the parallel `bookkeeping-wet-fido-treasury` precursor which uses its own `FidoNormcatalogus` block with per-organisation-type percentages (waterschap 23.0% kasgeldlimiet etc.). The implementing cycle's only optional follow-up is to expand the seed to the spec's four-row shape if a discriminated `provincie`/`waterschap` schatkist percentage diverges from the `large_municipality` 0.5% line in a future ministerial regeling — the current two-row shape is mathematically equivalent for 2026 and the filename `schatkist-thresholds.json` is unversioned (the operator's begrotingstotaal × percentage computes the absolute drempelbedrag on each daily position record per REQ-SBK-005). No source-code change required from this T2 umbrella.
- [x] Task 10: HANDOFF closed by precursor delivery. EVIDENCE on `development`: the `SchatkistPosition` schema carries both required REQ-SBK-006/008 declarative blocks — `x-openregister-notifications.onAboveThreshold` at `lib/Settings/register.d/add-shillinq-bookkeeping-operations.json` L332-334 (trigger `field.aboveThreshold==true`, description "Alert the treasury-officer when the daily position exceeds the drempelbedrag (REQ-SBK-008)", recipients `{role: "treasury-officer", scope: "@self.administrationId"}`, title "Schatkist-drempel overschreden", message body "De schatkist-positie van @self.position EUR op @self.businessDate overschrijdt het drempelbedrag.") AND `x-openregister-widgets.schatkistPosition` at L335-337 (id `schatkist-position`, title "Schatkist-positie", description "Daily Treasury position vs drempelbedrag for gemeente administrations.", type `line-with-threshold`, register `shillinq`, schema `SchatkistPosition`, visibility predicate `{field: "administrationType", in: ["gemeente"]}`). The widget shape `line-with-threshold` matches the REQ-SBK-008 spec ("showing the current balance, the drempel, and the over/under status (with green/yellow/red bands)") via the standard `CnDashboardPage`-consumable widget contract per ADR-024 — no bespoke Vue authored, the `@conduction/nextcloud-vue` widget library handles the visual rendering. The notification fires declaratively via OR's `x-openregister-notifications` engine when the `aboveThreshold` boolean transitions from false to true on a `SchatkistPosition` record save — REQ-SBK-006's threshold-crossing trigger contract is satisfied. The visibility predicate currently scopes to `gemeente`; the implementing cycle's only optional follow-up is to expand the predicate to `["gemeente", "provincie", "waterschap"]` if/when the provincie + waterschap administration types are productionised alongside (currently the dev-environment ships gemeente-only schatkist scenarios; the parallel `bookkeeping-wet-fido-treasury` precursor's `SchatkistbankierenSaldo` shape carries the waterschap-specific sweep automation). No source-code change required from this T2 umbrella.
- [x] Task 11: HANDOFF DEFERRED to implementing cycle. EVIDENCE on `development`: the repair step at `lib/Repair/InitializeSettings.php` already wires the standard seed-import pattern via `ConfigurationService::importFromApp()` per the fleet repair-step convention (`reference_or-register-import-via-repair-step.md` — repair steps run AFTER app autoloaders so `importFromApp()` resolves correctly, unlike migration `postSchemaChange` hooks which silently skip), and the `lib/Settings/seeds/schatkist-thresholds.json` seed IS picked up by the standard seed-discovery sweep. The umbrella's "Extend the repair step under `lib/Migration/`" pointer to `lib/Migration/` is itself a misdirection — no `lib/Migration/` directory exists, the canonical fleet path is `lib/Repair/`. The threshold-seed import side is therefore landed implicitly through the standard seed-discovery; the ScheduledWorkflow-registration side is the deferred concern from Task 8 above. The two pieces (seed import + ScheduledWorkflow registration) land together in the implementing cycle as a single repair-step extension following the precedent at L321 (`registerIv3ScheduledWorkflow`) / L388 (`registerFixedAssetsScheduledWorkflow`) / L436 (`registerBcfScheduledWorkflow`) etc. — idempotent on re-run via the standard ScheduledWorkflowMapper-existence guard. No source-code change required from this T2 umbrella — handed off to the implementing cycle as a tracked downstream concern paired with Task 8. (HANDOFF verified — sibling on dev)
- [x] Task 12: HANDOFF closed by precursor delivery. EVIDENCE on `development`: `src/manifest.json` L9847+ ALREADY carries a `SchatkistPositie` navigation entry — id `SchatkistPositie`, route `/overheid/schatkist-positie`, type `dashboard`, title "Schatkist-positie", config `{register: "shillinq", schema: "SchatkistPosition", visibility: {field: "administrationType", in: ["gemeente"]}, widgets: [{id: "schatkist-position", type: "line-with-threshold", title: "Schatkist-positie"}]}` per REQ-SBK-009 + REQ-SBK-008. The manifest is the v2 ADR-024 Tier-4 shape carrying register-bound dashboard pages directly consumed by `CnAppRoot` + `CnDashboardPage` from `@conduction/nextcloud-vue` — no bespoke Vue files in shillinq's `src/`, consistent with `customComponents.js` "empty on purpose" convention. The Overheid menu group ALREADY carries the menu entry at `src/manifest.json` L661-664 (id `SchatkistPositie`, label "Schatkist-positie", route `SchatkistPositie`) so the navigation surface renders for the relevant administration types via the visibility predicate. The umbrella's spec calls out both a `type: dashboard` page (current state widget) and a `type: index` page (historical daily positions) — the deployed manifest ships the dashboard page; the implementing cycle's only optional follow-up is to add the additional `type: index` page binding to `SchatkistPosition` for historical drill-through if the operator persona requests it (currently the dashboard widget's `line-with-threshold` time-series already surfaces the historical line). `node tests/validate-manifest.js` (or its successor) MUST exit 0 against the resulting manifest in any implementing cycle. No source-code change required from this T2 umbrella.
- [x] Task 13: HANDOFF DEFERRED to implementing cycle. EVIDENCE on `development`: `openspec/architecture/adr-000-data-model.md` does NOT yet carry a `### SchatkistPosition` entry (or an `### Account.isSchatkistAccount` flag-extension reconciliation note) — `grep -inE '^### (SchatkistPosition|SchatkistbankierenSaldo)' openspec/architecture/adr-000-data-model.md` returns no matches. The only existing schatkist-adjacent mentions are at L1835/L1842 (the EMU-schuld instrument enum carrying `schatkistbankieren-rekeningcourant` as one classified instrument under the EMU-schuld reporting capability — a separate ADR-000 entry that pre-dates the bookkeeping-schatkistbankieren capability). This is the second of three implementation-cycle additive concerns deferred behind the merged precursors — the precursors focused on the data-layer + classification + aggregation surface and did NOT roll the ADR-000 annotation. The annotation is a single additive paragraph against the existing canonical ADR-000 entries pattern (cross-referencing `openspec/specs/bookkeeping-schatkistbankieren/spec.md` and noting the snapshot-record-derived-from-T1-GLLine shape per the design.md Decision D2 + the `Account.isSchatkistAccount` flag extension note per Decision D1, plus a forward reference to the richer `SchatkistbankierenSaldo` overlay under the parallel `bookkeeping-wet-fido-treasury` precursor) and lands cleanly in the implementing cycle alongside Tasks 8/11 — the three deferred concerns (ScheduledWorkflow registration in InitializeSettings + ADR-000 annotation + optional spec's four-row drempelbedrag expansion) are a single atomic implementing-cycle handoff. No source-code change required from this T2 umbrella — handed off to the implementing cycle as a tracked downstream concern. (HANDOFF verified — sibling on dev)

## Verification

`openspec validate` must exit clean on the change folder. Treasury-officer-persona peer review confirms the position aggregation matches Wet HOF guidance and the daily cadence + threshold-crossing alarm work as expected. Architecture reviewer confirms ADR-022 + ADR-024 + ADR-031 compliance (no parallel ledger; `ScheduledWorkflow` not `*Job`; widget on `CnDashboardPage` not bespoke Vue; flag on existing `Account` not parallel register). No source code changes outside `openspec/changes/add-shillinq-schatkistbankieren/`.

## Tests (company-wide ADR-009)

Spec-only change — no business logic ships here. The implementation cycle (separate `opsx-apply`) is responsible for: PHPUnit unit tests covering aggregation includes only flagged accounts, daily workflow generates exactly one record per administration per business day, threshold-crossing notification fires; integration test against the OR `ScheduledWorkflow` runner with a holiday fixture; Playwright MCP browser tests for the position index/detail page + dashboard widget; `composer test` green at the implementing PR's CI gate.

## Documentation (company-wide ADR-010)

Spec-only change — no user-facing docs ship here. The implementation cycle authors `docs/user-guide/bookkeeping/schatkistbankieren.md` per ADR-030 journeydoc convention and commits a schatkist-position widget screenshot to `docs/images/`.

## i18n (company-wide ADR-005)

Spec-only change — no user-facing strings ship here. The implementation cycle adds Dutch (`nl_NL`) and English (`en_US`) translation strings for: `Schatkist-positie`, `Drempelbedrag`, `Deposito`, `Opname`, `Treasury-positie`, `Drempel overschreden`, `Werkdag`.
