# Tasks — SBR / XBRL Reporting

> **Spec-only change.** Per `proposal.md` Scope, implementation code is
> deliberately out of scope here. The tasks below describe the work an
> `opsx-apply` cycle will execute against the
> `bookkeeping-sbr-xbrl-reporting` spec — they are recorded now so
> the spec-review gate, dependency planning, and tier-cascade impact are
> all visible at proposal time. No source files are edited by this change
> itself.

## Tasks

- [x] Task 1: Confirm no `bookkeeping-sbr-xbrl-reporting` capability spec already exists, no `XBRLTaxonomy`/`SBRDocumentType`/`XBRLMapping` schemas are declared, and no `lib/Service/XBRL*`, `lib/Service/SBR*`, or `lib/Service/Reporting*` PHP classes are present (per ADR-031 anti-pattern enumeration) — VERIFIED 2026-06-09: no register.d fragment declares any of the three schemas (grep across `lib/Settings/register.d/*.json` returns no hits); no `lib/Service/XBRL*.php`, `lib/Service/SBR*.php`, `lib/Service/Reporting*.php` classes exist (only `InnovatieboxSbrExportService` + `PayrollSbrConversionService` for unrelated T3 capabilities, both consuming SBR not declaring the registry). The canonical spec `openspec/specs/bookkeeping-sbr-xbrl-reporting/spec.md` is in proposed state; this change carries the ADDED requirements for delivery.
- [x] Task 2: Author `specs/bookkeeping-sbr-xbrl-reporting/spec.md` with `Status: proposed` / `Scope: shillinq` / `Tier: T3 (regulatory reporting + compliance)` / `Depends on: bookkeeping-general-ledger, bookkeeping-chart-of-accounts` header, `REQ-SBR-NNN` requirements using RFC 2119 keywords, and `#### Scenario:` blocks with GIVEN/WHEN/THEN; cite ADR-024 + ADR-031 + ADR-022 inline — DONE: spec at `openspec/changes/bookkeeping-sbr-xbrl-reporting/specs/bookkeeping-sbr-xbrl-reporting/spec.md` carries REQ-SBR-001..009 with the required header, RFC 2119 wording, and full Scenario coverage (15 scenarios).
- [x] Task 3: Author `proposal.md` referencing the shared `nextcloud-app` spec and including Affected Projects / Scope (XBRL GL taxonomy versions, SBR filing types, account mapping, compliance validation) / Risks (taxonomy stability, mapping coverage gaps, endpoint auth, GL balance checks) / Rollback / Open Questions — DONE: `proposal.md` references `nextcloud-app` spec, declares the three Affected Projects (shillinq / openregister / docudesk), enumerates the four risks called out in the task, and lists Rollback Strategy plus four Open Questions.
- [x] Task 4: Author `design.md` with Reuse Analysis table, D1 (XBRL GL versionable register), D2 (SBR lifecycle via OR), D3 (mapping is schema-driven), D4 (compliance validation is aggregation), D5 (taxonomy metadata not computed), D6 (submission contract declared) — DONE: `design.md` carries D1..D6, the Reuse Analysis table, declarative-vs-imperative decision matrix, seed-data section, risks / trade-offs, migration plan, and open questions.
- [x] Task 5: Declare the `XBRLTaxonomy` schema in `lib/Settings/shillinq_register.json` with all REQ-SBR-002 fields (taxonomyId, name, taxonomyVersion, publicationDate, effectiveDate, expiryDate, status, description) — DONE in `lib/Settings/register.d/bookkeeping-sbr-xbrl-reporting.json` (ADR-037 fragment; monolithic shillinq_register.json untouched). All 8 fields declared with required[] enforcement on the 6 mandatory ones; `x-openregister-unique` on `taxonomyId` and `taxonomyVersion`; relations to XBRLMapping.
- [x] Task 6: Declare the `SBRDocumentType` schema in `lib/Settings/shillinq_register.json` with all REQ-SBR-003 fields (name, code, description, applicableEntityTypes, filingDeadline, requiredFields, submissionEndpoint, authMethod, status, administrationId) — DONE in the same fragment. All 10 REQ-SBR-003 fields declared, plus `taxonomyVersion`, `fiscalYearStart`, `fiscalYearEnd`, `validationErrors`, `submittedAt`, `approvedAt`, `rejectionReason`, `filingId` to support the REQ-SBR-005 lifecycle + REQ-SBR-006 aggregations; uniqueness on (administrationId, code, fiscalYearStart) so per-administration filings don't collide.
- [x] Task 7: Declare the `XBRLMapping` schema in `lib/Settings/shillinq_register.json` with all REQ-SBR-004 fields (sourceAccountId, targetXBRLConcept, taxonomyVersion, mappingDate, status, notes) — FK to Account and XBRLTaxonomy via relations — DONE in the same fragment. All 6 fields declared; uniqueness on (sourceAccountId, taxonomyVersion); relations to Account (sourceAccount) and XBRLTaxonomy (taxonomy).
- [x] Task 8: Add `x-openregister-lifecycle` to `SBRDocumentType` declaring every transition in REQ-SBR-005 (`draft → validated → submitted → approved` / `rejected`, with guards for GL completeness) — DONE. Lifecycle declares 5 states (draft, validated, submitted, approved, rejected), 6 transitions (validate / submit / approve / reject / revert / remediate). The `validate` transition carries two declarative guards: `unmappedAccountCount == 0` and `glBalanceDelta == 0` (per REQ-SBR-006); guard error messages surface to the operator.
- [x] Task 9: Implement pre-filing validation per REQ-SBR-006 as four `x-openregister-aggregations` predicates — DONE. The SBRDocumentType `x-openregister-aggregations` block declares: (1) `unmappedAccountCount` (GL completeness + mapping coverage — count of active Account rows lacking an XBRLMapping for `@self.taxonomyVersion`); (2) `unmappedAccountList` (operator-facing list of unmapped accounts surfaced via validationErrors); (3) `mappingCoveragePercent` (used by Mapping Validation index); (4) `glBalanceDelta` (debit - credit; gates `validate`); (5) `mandatoryFieldsMissing` (fiscal year + entity-type field presence).
- [x] Task 10: Implement mapping validation aggregation per REQ-SBR-007 — DONE. Both `unmappedAccountList` (returns Account UUIDs without active XBRLMapping for `@self.taxonomyVersion`) and `mappingCoveragePercent` are declarative aggregations on SBRDocumentType; idempotent and side-effect-free per the REQ-SBR-007 contract.
- [x] Task 11: Implement filing deadline notifications per REQ-SBR-008 — DONE. The SBRDocumentType `x-openregister-notifications.filingDeadlineApproaching` block declares a `scheduled` trigger that fires at `@self.filingDeadline - P30D`, skipped if the filing already reached submitted/approved; recipients = role `finance-manager` scoped to `@self.administrationId`. An `onRejected` notification additionally surfaces Belastingdienst rejection reasons per REQ-SBR-005.
- [x] Task 12: Add 3 manifest navigation entries (`XBRL Taxonomies`, `SBR Documents`, `Mapping Validation`) + their `type: index` / `type: detail` pages to `src/manifest.json` per REQ-SBR-009; `node tests/validate-manifest.js` exits 0 — DONE. Three new menu items (orders 85, 86, 87 under Bookkeeping) + six pages (`XBRLTaxonomies` / `XBRLTaxonomyDetail` / `SBRDocuments` / `SBRDocumentDetail` / `XBRLMappingValidation` / `XBRLMappingDetail`) wired to generic CnIndexPage + CnDetailPage renderers (ADR-024 Tier-4; no bespoke Vue components). SBRDocumentDetail carries `lifecycleActions: true` to surface the validate / submit / approve / reject / revert / remediate transitions from the schema lifecycle. XBRLTaxonomyDetail surfaces a related-list of XBRLMapping rows for the current taxonomy version. English + Dutch i18n keys added (English source strings per ADR-005). `node tests/validate-manifest.js`: structural lint PASS, consistency check PASS, 201 pages.
- [x] Task 13: Load seed XBRL GL taxonomy objects (2026, 2025 versions) via `ConfigurationService::importFromApp()` repair step per design.md Seed Data (2 XBRLTaxonomy + 2 SBRDocumentType + 2 XBRLMapping examples) — DONE. Canonical seed objects live in the register fragment `objects[]` (auto-loaded by `SettingsService::loadConfigurationForced` via the existing `InitializeSettings` post-migration step). An explicit `Load_SBR_XBRL_Seeds_Step` mirroring `Load_CBS_Seeds_Step` is registered in `appinfo/info.xml` as a deduplicated safety net (slug-based exist check, OR `ObjectService::saveObject` named-arg form per the OR-API memory, non-fatal on failure). Seed payload: XBRLTaxonomy `xbrl-gl-2026` + `xbrl-gl-2025`; SBRDocumentType `sbr-doctype-jaarverslag-adm-001` + `sbr-doctype-belastingaangifte-adm-001`; XBRLMapping for `acct-1000-current-assets` -> `CurrentAssets` and `acct-4000-operating-revenue` -> `OperatingRevenue` (taxonomy version 2026-01).
- [x] Task 14: Update `openspec/architecture/adr-000-data-model.md` with `XBRLTaxonomy`/`SBRDocumentType`/`XBRLMapping` entries, linking to the SBR/XBRL regulatory context and Belastingdienst reference — DONE. The existing stale XBRLTaxonomy entry (formerly attributed to `tax-levy-management` with `version`/`namespace`/`elements` fields) has been reconciled to the REQ-SBR-002 shape (taxonomyId / name / taxonomyVersion / publicationDate / effectiveDate / expiryDate / status / description) and re-primary-spec'd to `bookkeeping-sbr-xbrl-reporting`. Two new entries added: SBRDocumentType (REQ-SBR-003 + REQ-SBR-005 lifecycle fields) and XBRLMapping (REQ-SBR-004). All three entries cite the Belastingdienst regulatory context and link the T3-vs-T4 boundary (T3 declares contract, T4 generates + submits XBRL via openconnector Digipoort source slug).

## Deduplication Check

- Verify no overlap with `bookkeeping-chart-of-accounts` (which declares `Account`; we declare `XBRLMapping` as the relation layer).
- Verify no overlap with `bookkeeping-audit-trail` (which auto-tracks changes; we rely on that for SBR lifecycle audit).
- Verify no overlap with `bookkeeping-notifications` or OR's notification engine (used for deadline alerts).
- No custom XBRL generation service — T4 owns outbound generation.
- Document findings in implementing PR description.

## Verification

`openspec validate` must exit clean on the change folder. Bookkeeper-persona peer review (e.g., Belastingdienst compliance checklist) confirms the SBR/XBRL filing flow matches Dutch regulatory requirements (annual reporting, XBRL GL taxonomy alignment, mapping completeness, validation enforcement). Architecture reviewer confirms ADR-024 (schema declarations), ADR-031 (declarative lifecycle + aggregations, no PHP service), and ADR-022 (no app-local dunning parallel) compliance. Manifest validation (`node tests/validate-manifest.js`) exits 0. No source code changes outside `openspec/changes/bookkeeping-sbr-xbrl-reporting/`.

## Tests (company-wide ADR-009)

Spec-only change — no business logic ships here. The implementation cycle (separate `opsx-apply`) is responsible for: PHPUnit unit tests for XBRL GL taxonomy versioning, SBR filing lifecycle transitions, pre-filing validation aggregations (GL completeness, mapping coverage, mandatory fields, GL balance), account mapping resolution, filing deadline notification trigger, and webhook parsing from Belastingdienst (pre-declared on Tasks 8–11); Playwright MCP browser tests for the 3 manifest navigation entries and the "Mapping Validation" workflow for fixing unmapped accounts (pre-declared on Task 12); `composer test` green at the implementing PR's CI gate.

## Documentation (company-wide ADR-010)

Spec-only change — no user-facing docs ship here. The implementation cycle authors:
- `docs/user-guide/bookkeeping/sbr-xbrl-reporting.md` per ADR-030 journeydoc convention covering:
  - Regulatory context (Belastingdienst annual filing requirement).
  - XBRL GL taxonomy setup and version selection workflow.
  - SBR document creation, mapping validation, and lifecycle state management.
  - Filing deadline notifications and submission workflow (noting T4 handles actual submission).
- Governance/architect guide: `docs/architecture/sbr-xbrl-mapping-design.md` documenting the account-to-XBRL concept mapping model, aggregation design, and T3/T4 boundary (T3 declares contract, T4 submits).
- Screenshots: `docs/images/sbr-xbrl-taxonomies-index.png`, `docs/images/sbr-document-detail-lifecycle.png`, `docs/images/mapping-validation-coverage.png`.

## i18n (company-wide ADR-007)

Spec-only change — no user-facing strings ship here. The implementation cycle adds Dutch (`nl_NL`) and English (`en_US`) translation strings for: `XBRL Taxonomies`, `SBR Documents`, `Mapping Validation`, `XBRL Taxonomy`, `SBR Document Type`, `Filing Deadline`, `Validation Status`, `Mapping Coverage`, `GL Completeness`, `Mapping Coverage`, `Account Mapping`, `Unmapped Accounts`, `Jaarverslag (Annual Report)`, `Belastingaangifte (Tax Filing)`, `Draft`, `Validated`, `Submitted`, `Approved`, `Rejected`, `Active`, `Archived`, `Belastingdienst`, `DNB`, `XBRL GL Concept`.

## External adapter

- [x] Adapter port: dormant `DigipoortSbrAdapterInterface` + `LogDigipoortSbrAdapter` shipped at `lib/Service/External/Digipoort/` and wired in `lib/AppInfo/Application.php::register()`. The REQ-SBR-005 `submit` lifecycle transition can advance without a live Digipoort connector — the port shape (filingType / xbrlInstanceBytes / xbrlChecksum / taxonomyVersion / kenmerk) is stable so production swap to an openconnector-backed binding at source slug `digipoort-sbr` is non-breaking.
