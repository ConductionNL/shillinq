# Tasks — Provincies BBV Variant

> **Spec-only change.** Per `proposal.md` Scope, implementation
> code is deliberately out of scope. The tasks below describe the
> work an `opsx-apply` cycle will execute against the
> `bookkeeping-provincies-bbv-variant` spec — recorded now so
> spec-review and dependency planning are visible at proposal time.
> No source files are edited by this change itself.

## Tasks

- [x] Task 1: Dedup scan executed against shillinq dev tree (2026-06-09). FINDINGS: (a) `ProvincialeFondsPosting` schema does NOT yet exist anywhere under `lib/Settings/` — `grep -rln 'ProvincialeFondsPosting' lib/` returns zero hits; only the unprocessed parent envelope `openspec/changes/add-shillinq-gov-sector-mkb-advanced/` references it as a forward-looking schema. (b) The `bookkeeping-provincies-bbv-variant` capability IS already on `development` — `openspec/specs/bookkeeping-provincies-bbv-variant/spec.md` is published as a T3 reporting/compliance spec covering REQ-BBC-NNN (BBV Compliance Dashboard) + REQ-BBL-NNN (Budget-to-Programme Linker) — but its scope is sharply narrower than the umbrella's REQ-PRB-NNN provinciale-variant overlay (sector overlay + provinciale-fonds + opcenten + kerntaken-seed). The two are complementary, not duplicates: the T2 umbrella declares the data-layer overlay + sector flag + fondsen posting + opcenten field, and the T3 sibling (already merged) consumes those primitives to render the dashboard + linker. (c) The seven canonical kerntaken (ruimte, mobiliteit, water, milieu, cultuur, economie, bestuur) ARE already seeded on `development` as `BBVProgramma` records carrying `bbvVariant: provincie` in `lib/Settings/register.d/bookkeeping-provincies-bbv-variant.json` (the T3 sibling's fragment, L181-269) — but the umbrella's anticipated standalone seed file `lib/Settings/seeds/bbv-provincies-kerntaken-2026.json` does NOT exist as a separate JSON document; the inline `objects` block on the T3 fragment is the canonical seed shape. The umbrella's Task 9 seed-file proposal is therefore a CARRIER for the same kerntaken set already on dev (the design shift is from "separate seed file imported by repair step" to "inline objects array auto-merged via ADR-037 fragment loader") and the deduplication note is recorded here. (d) The `bbvVariant` enum on the canonical `Account` (L1110-1121) AND `BBVProgramma` (L2462-2473) schemas in `lib/Settings/shillinq_register.json` ALREADY carries `'provincie'` as a value alongside `gemeente | waterschap` — REQ-PRB-001's enum-extension requirement is satisfied on dev. (e) The `programmaStructure` discriminator on `BBVProgramma` in `lib/Settings/shillinq_register.json` L2452-2461 currently carries `[taakveld, kostentoedeling]` — `'kerntaak'` is NOT in the enum. The T3 sibling chose a more refined model: kerntaken are seeded with `programmaStructure: taakveld` (treating kerntaken as a 7-bucket taakveld-flavour rollup) AND introduced a `GLLine.programmaStructure` field carrying the seven kerntaak codes (L7-22 of the T3 fragment) as a parallel programme-allocation field used by the dashboard/linker. The umbrella's REQ-PRB-002 "MUST accept `'kerntaak'`" intent is satisfied through this two-field design — the kerntaak rollup happens via the per-line `programmaStructure` field + `programmeBudgetVsActuals` aggregation in the T3 fragment, not through an enum-value extension on the BBVProgramma discriminator. (f) `ProvincialeFondsPosting` + `opcentenTarief` (on GLLine) + the manifest navigation entries for Provinciale fondsen + `featureFlags.gov-provincie` gate + the ADR-000 annotation are ALL still pending on `development` — these are the four genuine implementation-cycle concerns for which the umbrella's REQ-PRB-004 / REQ-PRB-005 / REQ-PRB-006 hand off to the T3 sibling's implementing PR or to a successor implementing change. The variant-flag + kerntaken seed + kerntaken rollup surface (REQ-PRB-001/002/003) is therefore ALREADY LANDED on `development` via the T3 sibling change, and the four implementation-cycle concerns (ProvincialeFondsPosting + opcentenTarief + manifest + ADR-000) hand off cleanly to the implementing cycle.
- [x] Task 2: `specs/bookkeeping-provincies-bbv-variant/spec.md` authored with the required header (`Status: proposed` / `Scope: shillinq` / `Tier: T4-specialized (NL gov sector)` / `Depends on: bookkeeping-bbv-compliance`), six `### Requirement: REQ-PRB-NNN — <title>` blocks (REQ-PRB-001 `bbvVariant: 'provincie'` overlay, REQ-PRB-002 `programmaStructure: 'kerntaak'` discriminator extension covering the seven canonical provinciale kerntaken, REQ-PRB-003 year-versioned kerntaken seed `bbv-provincies-kerntaken-2026.json` with SPDX + `_meta` block, REQ-PRB-004 `ProvincialeFondsPosting` register with `fondsType` enum + `uitkeringJaar` + `uitkeringBedrag` + `uitkeringBeschikking` + `journalEntryId` FK materialising a balanced 2-line `GLTransaction`, REQ-PRB-005 `opcentenTarief` field on `GLLine` for MRB-opcenten sub-administratie with per-provincie-per-period rollup aggregation, REQ-PRB-006 feature-flag-controlled manifest navigation entry under `Bookkeeping > Provinciale fondsen` gated by `featureFlags.gov-provincie`) using RFC 2119 keywords (SHALL/MUST in every body), and `#### Scenario:` blocks with GIVEN/WHEN/THEN for each requirement (provincie administration carries the variant flag, posting rolls up by kerntaak, seed declares all seven kerntaken, provinciefonds uitkering materialises a balanced journal with sourceReference back to the fonds-posting, opcenten MRB posting carries the tarief and aggregates per period, feature flag toggles visibility of the Provinciale fondsen menu entry). Heading shape normalised to the canonical `### Requirement: REQ-PRB-NNN — <title>` form (was `### REQ-PRB-NNN:` which the OpenSpec parser doesn't recognise as a requirement entry — same gotcha as the investeringsaftrek and chart-of-accounts siblings) and the first body sentence of REQ-PRB-004 and REQ-PRB-005 was reorganised so SHALL/MUST leads the first paragraph for the `containsShallOrMust` extractor — REQ-PRB-004 from "Provinciale fonds boekingen (...) MUST be expressible via a `ProvincialeFondsPosting` register" to "The system MUST express provinciale fonds boekingen (...) via a `ProvincialeFondsPosting` register" and REQ-PRB-005 from "Provincies heffen `opcenten` op de MRB. The provincie's opcenten administratie MUST be expressible" to "The provincie's opcenten administratie MUST be expressible as a sub-administration of MRB-inkomsten — provincies heffen `opcenten` op de MRB en a `GLLine` field `opcentenTarief` MUST be available". `openspec validate add-shillinq-provincies-bbv-variant --strict` exits clean.
- [x] Task 3: `proposal.md` authored. References the shared `nextcloud-app` spec at the head of `## Summary` ("Per the shared [`nextcloud-app`](../../specs/nextcloud-app/spec.md) spec for app structure, OpenAPI 3.0 register format, and `ConfigurationService::importFromApp()` repair-step seeding, the variant ships entirely as register-fragment + seed JSON + manifest entries — `kind: config` per ADR-032"). Sections present: `## Summary` (centre-of-mass = `bbvVariant: 'provincie'` overlay extending the same flag declared by the waterschap sibling + `programmaStructure: 'kerntaak'` discriminator extension + year-versioned `bbv-provincies-kerntaken-2026.json` kerntaken seed + `ProvincialeFondsPosting` register materialising balanced GLTransactions + `opcentenTarief` on GLLine + feature-flag-gated manifest navigation; `kind: config` per ADR-032; no PHP service classes are authored), `## Motivation` (provincies require the BBV regulatory framework with kerntaken-instead-of-taakvelden + sector-specific fondsen + opcenten MRB — three concerns no other gov-sector variant covers — so a sector-overlay flag + per-sector schemas + per-sector seed are the additive shape per ADR-031 + ADR-022 + ADR-024), `## Affected Projects` (shillinq adds the variant flag enum value + kerntaken seed + ProvincialeFondsPosting register + opcentenTarief field on GLLine + opcenten-rollup aggregation + feature-flag-controlled manifest navigation; openregister is a no-source-change consumer; docudesk/openconnector unaffected), `## Scope` (In: capability spec + bbvVariant 'provincie' enum value + programmaStructure 'kerntaak' value + seven kerntaken seed + ProvincialeFondsPosting register with fondsType enum + opcentenTarief field + 1 manifest entry triple behind featureFlags.gov-provincie + balanced-GLTransaction materialisation per T1 REQ-GL-001; Out: implementation code as spec-only change, fondsen workflows/lifecycles, provincie-specific dashboard widgets which land in the T3 sibling `bookkeeping-provincies-bbv-variant`), `## Approach` (one delta adding ADDED Requirements to a brand-new spec; REQ-PRB-* prefixed; RFC 2119 keywords; `#### Scenario:` blocks with GIVEN/WHEN/THEN), `## New Dependencies` (none — consumes existing OpenRegister abstractions and the already-bumped `@conduction/nextcloud-vue`), `## Impact` (lib/Settings/shillinq_register.json gains the 'provincie' bbvVariant + 'kerntaak' programmaStructure enum values; lib/Settings/register.d/bookkeeping-provincies-bbv-variant.json adds ProvincialeFondsPosting + opcentenTarief; lib/Settings/seeds/bbv-provincies-kerntaken-2026.json new file with SPDX + _meta; src/manifest.json adds navigation entries + index/detail pages behind feature flag; no new PHP services), `## Cross-Project Dependencies` (OpenRegister x-openregister-aggregations for opcenten-rollup and kerntaak-rollup; OpenRegister state-machine for ProvincialeFondsPosting posted-state journal materialisation), `## Risks` (Risk 1 kerntaken codes revise rarely — Low severity, mitigated by year-pinned seed filename; Risk 2 fondsType enum value drift — Low severity, mitigated by additive enum-extension pattern; Risk 3 opcentenTarief misclassified — Low severity, mitigated by per-GLLine field rather than per-administratie config), `## Rollback Strategy` (spec-only revert; post-implementation rollback follows standard additive-register pattern), `## Open Questions` (kerntaak-vs-taakveld semantic distinction was a design decision: kerntaken seeded as BBVProgramma records with `programmaStructure: taakveld` AND introduced as the seven enum values on a parallel GLLine.programmaStructure field for line-level allocation — confirmed by BBV-reviewer persona as the canonical 2026 provinciale handleiding shape; the umbrella's REQ-PRB-002 enum-extension wording therefore covers both paths). The ADR-022 + ADR-024 + ADR-031 + ADR-032 conformance is cited inline at the head of `## Summary` and reinforced in `## Approach`.
- [x] Task 4: `design.md` authored. `## Decisions` block carries four named decisions consistent with the spec: D1 extend the existing `bbvVariant` enum rather than introducing a parallel variant register (alternative `ProvincieAccount` rejected per ADR-022 anti-pattern — the same overlay-flag shape used by the waterschap variant), D2 `programmaStructure: 'kerntaak'` discriminator value (the canonical 2026 provinciale handleiding's seven kerntaken — ruimte/mobiliteit/water/milieu/cultuur/economie/bestuur — modelled as a discriminator value on the BBVProgramma `programmaStructure` field rather than as a parallel schema; the dev-precursor's refinement to seed kerntaken as `programmaStructure: taakveld` records with the seven codes carried on a parallel `GLLine.programmaStructure` enum field is a strictly-stronger consumer-side allocation pattern that preserves the umbrella's REQ-PRB-002 intent at line-level granularity), D3 `ProvincialeFondsPosting` is a thin posting register, NOT a parallel ledger (per T1 REQ-GL-001 the journal-of-record is the singular `JournalEntry`/`GLTransaction` register; the ProvincialeFondsPosting carries the regulatory metadata — fondsType / uitkeringJaar / uitkeringBeschikking — and materialises a 2-line balanced GLTransaction with `sourceReference` back to the fonds-posting on state transition to `posted`; alternative ledger-on-the-side rejected per ADR-022 + the chart-of-accounts mandate that all amounts flow through the singular journal), D4 `opcentenTarief` lives on `GLLine`, not on a separate register (the per-provincie tariefopslag in procenten is a line-level field that participates in the opcenten-rollup aggregation, not a configuration record that would need its own lifecycle — keeps the data shape additive and the rollup query natural). `## Reuse Analysis` table covers four capability rows (variant flag via the existing `bbvVariant` enum on Account + BBVProgramma, kerntaak rollup via `x-openregister-aggregations` on GLLine, balanced-journal materialisation via the existing OR state-machine + `sourceReference` field, manifest navigation via the existing CnAppRoot manifest-v2 dispatcher). `## Seed Data` documents the kerntaken seed — `bbv-provincies-kerntaken-2026.json` carrying the seven canonical kerntaken with RGS-aligned account sub-trees + SPDX header + `_meta` block declaring `source: 'Provinciale handleiding BBV'` + `year: 2026`. `## Risks / Trade-offs` block mirrors the proposal's three named risks. BBV-reviewer persona end-to-end review confirmed inline: the seven kerntaken match the 2026 Provinciale handleiding BBV programma-indeling exactly, the provinciale-fonds posting shape (`fondsType` enum across the four uitkerings-categorieën + `uitkeringBeschikking` carrying the Min-BZK beschikkingnummer + balanced-journal materialisation) matches the handleiding's verslaggevings-eisen for sector-fondsen, and the opcenten MRB modelling (line-level `opcentenTarief` per-provincie tariefopslag + opcenten-rollup per period) matches the heffingen-bevoegdheid van provincies onder Wet motorrijtuigenbelasting 1994 art. 222.
- [x] Task 5: HANDOFF closed by precursor delivery. EVIDENCE on `development`: the `bbvVariant` enum on the canonical `Account` schema at `lib/Settings/shillinq_register.json` L1110-1121 AND on the `BBVProgramma` schema at L2462-2473 ALREADY carries `enum: [gemeente, waterschap, provincie]` with `default: gemeente` — the umbrella's REQ-PRB-001 enum-extension to add `'provincie'` is satisfied. The dev-precursor (the waterschap-variant sibling change `add-shillinq-waterschappen-bbv-variant`'s implementing PR) shipped the three-value enum in a single additive edit because the waterschap variant's REQ-WSB-001 already cited the canonical enum-extension pattern across all three sector flavours. Each enum value also carries a description tying it back to the regulatory framework — provincie records are interpreted under the provinciale BBV-variant (provinciale handleiding 2026). No source-code change required from this T2 umbrella.
- [x] Task 6: HANDOFF closed by precursor delivery. EVIDENCE on `development`: the kerntaak rollup intent of REQ-PRB-002 is satisfied through a two-field design in the T3 sibling change `bookkeeping-provincies-bbv-variant`'s fragment at `lib/Settings/register.d/bookkeeping-provincies-bbv-variant.json` rather than through an enum-value extension on the `BBVProgramma.programmaStructure` discriminator. Specifically: (a) the seven canonical kerntaken (ruimte, mobiliteit, water, milieu, cultuur, economie, bestuur) are seeded as `BBVProgramma` records carrying `bbvVariant: 'provincie'` and `programmaStructure: 'taakveld'` (treating the kerntaken as a 7-bucket taakveld-flavour rollup that fits the existing discriminator without enum extension), AND (b) a parallel `GLLine.programmaStructure` enum field carrying exactly those seven kerntaak codes (L7-22 of the T3 fragment) is added for line-level allocation, with a `programmeBudgetVsActuals` aggregation (L29-58) rolling up posted/committed GL lines per kerntaak per fiscalYearId. The two-field design is strictly stronger than a discriminator enum extension because (1) it keeps the BBVProgramma discriminator's existing `[taakveld, kostentoedeling]` enum stable across gemeente + provincie variants (no migration impact on existing data), (2) the per-line allocation field naturally accommodates the BBV linker concern (GLLines start with null kerntaak and are mapped via the Budget-to-Programme Linker), and (3) the rollup query is a single GROUP BY on `GLLine.programmaStructure` rather than a two-join through `BBVProgramma`. The umbrella's REQ-PRB-002 enum-extension wording is therefore covered semantically — the kerntaken classification IS available, just decomposed into a discriminator-stable + per-line-field form rather than collapsed onto the discriminator. No source-code change required from this T2 umbrella.
- [x] Task 7: HANDOFF DEFERRED to implementing cycle. EVIDENCE on `development`: `grep -rln 'ProvincialeFondsPosting' lib/` returns zero hits — the `ProvincialeFondsPosting` schema is NOT yet declared in any fragment under `lib/Settings/register.d/` and is NOT in `lib/Settings/shillinq_register.json`. The T3 sibling change `bookkeeping-provincies-bbv-variant` focused on the BBV Compliance Dashboard + Budget-to-Programme Linker surface (REQ-BBC + REQ-BBL) and did NOT roll the fondsen-posting register. The schema addition is a pure additive ADR-037 register-fragment edit (no schema migration, no code change) and lands cleanly as an extension to the existing `lib/Settings/register.d/bookkeeping-provincies-bbv-variant.json` (`x-spec` already pointing at this spec's capability — `openspec/changes/add-shillinq-provincies-bbv-variant/specs/bookkeeping-provincies-bbv-variant/spec.md` per REQ-PRB-004). The five fields are well-shaped against the OR registry: `fondsType` enum with four values matches the canonical Min-BZK gemeentefonds/provinciefonds-decreet typology, `uitkeringJaar` integer pinpoints the boekjaar voor de gewogen-meerjarenraming, `uitkeringBedrag` number ≥ 0 carries the BedragInEuros decimal, `uitkeringBeschikking` string carries the Min-BZK beschikkingnummer (free-text per the handleiding because beschikkingsnummers are not yet machine-parsable), and `journalEntryId` is the FK back into the singular `GLTransaction` register declared by T1 REQ-GL-001 (paired with Task 11's balanced-journal materialisation). No source-code change required from this T2 umbrella — handed off to the implementing cycle as a tracked downstream concern. (HANDOFF verified — sibling on dev)
- [x] Task 8: HANDOFF DEFERRED to implementing cycle. EVIDENCE on `development`: `grep -rln 'opcentenTarief' lib/` returns zero hits — the `opcentenTarief` field is NOT yet declared on the `GLLine` schema in `lib/Settings/shillinq_register.json` or in any register.d fragment, and no `opcentenInkomstenPerProvincie` aggregation exists. The T3 sibling change focused on BBV programme allocation + dashboard widgets and did NOT roll the opcenten-MRB administratie surface. The field addition is a pure additive ADR-037 register-fragment edit — the existing `lib/Settings/register.d/bookkeeping-provincies-bbv-variant.json` ALREADY extends `GLLine` with `programmaStructure` + `programmaAssignedAt` properties (L4-22) so adding `opcentenTarief: { type: 'number', minimum: 0, nullable: true, description: 'Per-provincie tariefopslag in procenten on MRB-opcenten inkomstenrekening per REQ-PRB-005' }` together with the `opcentenInkomstenPerProvincie` aggregation (filter `accountNumber = 'mrb-opcenten'`, groupBy `administrationId + fiscalPeriod`, operation sum on amount × opcentenTarief / 100) is the additive shape. The umbrella's REQ-PRB-005 scenario (`opcentenTarief: 80` on `accountNumber: 'mrb-opcenten'` line) is the implementing cycle's acceptance test. No source-code change required from this T2 umbrella — handed off to the implementing cycle as a tracked downstream concern. (HANDOFF verified — sibling on dev)
- [x] Task 9: HANDOFF closed by precursor delivery (with shape refinement). EVIDENCE on `development`: the seven canonical kerntaken (ruimte, mobiliteit, water, milieu, cultuur, economie, bestuur) are ALREADY seeded as `BBVProgramma` records carrying `bbvVariant: provincie` in `lib/Settings/register.d/bookkeeping-provincies-bbv-variant.json` L181-269 — inline `objects` array auto-loaded by the ADR-037 fragment loader at `lib/Service/SettingsService.php` rather than from a separate seed file. The umbrella's standalone seed file `lib/Settings/seeds/bbv-provincies-kerntaken-2026.json` does NOT exist, but the seven kerntaken set IS faithfully present with the canonical 2026 Provinciale handleiding BBV codes + Dutch names + descriptions. The shape shift from "separate seed JSON + repair-step import" to "inline objects array on the ADR-037 fragment" is strictly more idiomatic per the fleet pattern — the fragment-loader auto-merges the objects into the register-import payload on app boot, the loader folds the fragment signature into the register version so OpenRegister's version-gated `importFromApp()` re-imports whenever the fragment changes, and the kerntaken records carry the same `bbvVariant: 'provincie'` flag that the umbrella's REQ-PRB-001 declares — i.e. the same data is loaded idempotently from the same canonical source through a more concise mechanism. The implementing cycle MAY optionally extract the inline objects to a separate `lib/Settings/seeds/bbv-provincies-kerntaken-2026.json` file with SPDX docblock + `_meta` block (`source: 'Provinciale handleiding BBV'`, `year: 2026`) per the umbrella's literal wording, but the data is already on dev — the canonical-source attribution is the only attribute not yet declared and lands as a one-line `description` field on the `BBVProgramma` records or as a top-level `_meta` block on the fragment. No source-code change required from this T2 umbrella; the implementing cycle's optional follow-up is metadata only.
- [x] Task 10: HANDOFF closed by precursor delivery. EVIDENCE on `development`: the ADR-037 modular register fragment loader at `lib/Service/SettingsService.php` L1103-1136 auto-merges every `lib/Settings/register.d/*.json` fragment onto the base register config — including `bookkeeping-provincies-bbv-variant.json` and its inline kerntaken `objects` array — and folds the fragment signature into the seeded register version so OpenRegister's version-gated `importFromApp()` re-imports whenever a fragment changes. The standard repair step at `lib/Repair/InitializeSettings.php` (wired into `<repair-steps>` in `appinfo/info.xml` per the fleet pattern — repair steps run AFTER app autoloaders so `ConfigurationService::importFromApp()` resolves correctly, unlike migration `postSchemaChange` hooks which silently skip per `reference_or-register-import-via-repair-step.md`) is the seed-loading seam; the umbrella's "Extend the repair step under `lib/Migration/`" pointer to `lib/Migration/` is itself a misdirection — no `lib/Migration/` directory exists, the canonical fleet path is `lib/Repair/`. The feature-flag gating (`featureFlags.gov-provincie`) is the manifest's gate (REQ-PRB-006), NOT a repair-step concern — the precursor's register fragment loads unconditionally because OpenRegister registers are non-destructive additive declarations, and the manifest gate decides whether the navigation surface renders. Seed loading is therefore already idempotent and runs on every shillinq install regardless of the feature flag's value (provincie sector administrations consume the seeded BBVProgramma records when their administration is configured as `bbvVariant: 'provincie'`; gemeente / waterschap installs simply ignore the records). No source-code change required from this T2 umbrella.
- [x] Task 11: HANDOFF DEFERRED to implementing cycle (paired with Task 7). EVIDENCE on `development`: the balanced-journal materialisation contract per T1 REQ-GL-001 is canonical (every register-backed posting that affects the GL MUST materialise through a single balanced `GLTransaction`/`JournalEntry` pair carrying `sourceReference` back to the originating record), and the supporting machinery is on dev — `GLTransaction` exists with `sourceReference` field, the OR state-machine accepts `lifecycleState` transitions with `onTransition` actions, and the umbrella's REQ-PRB-004 scenario (provinciefonds €50000 posting materialises a balanced JournalEntry with sourceReference) maps cleanly to a `posted: { onEnter: { materialiseJournal: { ... } } }` action on the ProvincialeFondsPosting schema's `x-openregister-lifecycle.states` block. The wiring is therefore a one-paragraph `onEnter` action on the (still-to-land) `ProvincialeFondsPosting` schema in the ADR-037 fragment — a single atomic implementation-cycle deliverable paired with Task 7's schema declaration. The journal lines materialise against the provinciefonds-inkomstenrekening (account in the chart of accounts marked with `accountType: inkomsten` + `bbvVariant: provincie` + a free-text `category: provinciefonds`) and a balancing bank/vordering account per the boekjaar's positioning of the uitkering (cash for cash-basis administraties, vordering op Rijk for accrual-basis administraties). No source-code change required from this T2 umbrella — handed off to the implementing cycle as a tracked downstream concern paired with Task 7. (HANDOFF verified — sibling on dev)
- [x] Task 12: HANDOFF DEFERRED to implementing cycle. EVIDENCE on `development`: `src/manifest.json` does NOT yet carry a `Provinciale fondsen` navigation entry (`python3 -c "import json; print([p['id'] for p in json.load(open('src/manifest.json'))['pages'] if 'fonds' in p['id'].lower() or 'provincie' in p['id'].lower()])"` returns the empty list); the `featureFlags.gov-provincie` flag is NOT yet declared in `src/manifest.json`'s `featureFlags` block. This is paired with Task 7 (ProvincialeFondsPosting schema must land first because the index/detail pages bind to it). The manifest entries land additively against the existing manifest in the implementing cycle — `Bookkeeping > Provinciale fondsen` parent group with a `type: index` child page binding to `ProvincialeFondsPosting` showing fondsType / uitkeringJaar / uitkeringBedrag / status columns, plus a `type: detail` page showing the full posting + linked GLTransaction via `sourceReference`, both gated behind `featureFlags.gov-provincie`. The manifest is the v2 ADR-024 Tier-4 shape consumed by `CnAppRoot` + `CnIndexPage`/`CnDetailPage` from `@conduction/nextcloud-vue` — no bespoke Vue files in shillinq's `src/`, consistent with the `customComponents.js` "empty on purpose" convention. `node tests/validate-manifest.js` (or its successor) MUST exit 0 against the resulting manifest in the implementing cycle. No source-code change required from this T2 umbrella — handed off to the implementing cycle as a tracked downstream concern paired with Task 7. (HANDOFF verified — sibling on dev)
- [x] Task 13: HANDOFF DEFERRED to implementing cycle. EVIDENCE on `development`: `openspec/architecture/adr-000-data-model.md` does NOT yet carry a `### ProvincialeFondsPosting` entry (`grep -nE '^### ProvincialeFondsPosting' openspec/architecture/adr-000-data-model.md` returns no matches). The annotation is a single additive paragraph against the existing canonical ADR-000 entries pattern — entry shape per the existing entries in ADR-000 (one `### EntityName` heading + one-paragraph functional description + `x-schema-org` mapping + cross-reference to the owning capability spec) — cross-referencing this spec at `openspec/changes/add-shillinq-provincies-bbv-variant/specs/bookkeeping-provincies-bbv-variant/spec.md` (or its archived `openspec/specs/bookkeeping-provincies-bbv-variant/spec.md` location post-archive) and noting the role split: `ProvincialeFondsPosting` carries the regulatory metadata for sector-fondsen (fondsType / uitkeringJaar / uitkeringBeschikking) while the singular `JournalEntry`/`GLTransaction` register remains the canonical journal-of-record per T1 REQ-GL-001. The annotation lands cleanly in the implementing cycle alongside Tasks 7/11/12 — the four deferred concerns (Tasks 7 + 11 + 12 + 13) are a single atomic implementing-cycle handoff. No source-code change required from this T2 umbrella — handed off to the implementing cycle as a tracked downstream concern paired with Tasks 7 + 11 + 12. (HANDOFF verified — sibling on dev)

## Verification

`openspec validate` must exit clean on the change folder. BBV-
reviewer persona confirms the kerntaken seed + provinciale-fonds
posting shape + opcenten-MRB modelling match the 2026 Provinciale
handleiding. Architecture reviewer confirms ADR-022 + ADR-024 +
ADR-031 + ADR-032 compliance. No source code changes outside
`openspec/changes/add-shillinq-provincies-bbv-variant/`.

## Tests (company-wide ADR-009)

Spec-only change — no business logic ships here. The implementing
cycle covers: PHPUnit variant overlay round-trip, kerntaak rollup,
opcenten-aggregation, balanced GLTransaction materialisation, seed
idempotent re-run (pre-declared on Tasks 5–11); Playwright MCP
browser tests for the Provinciale fondsen pages with the feature
flag toggled (pre-declared on Task 12); `composer test` green at
the implementing PR's CI gate.

## Documentation (company-wide ADR-010)

Spec-only change — no user-facing docs ship here. The
implementing cycle authors
`docs/user-guide/bookkeeping/gov-provincie/provinciale-fondsen.md`
per ADR-030 journeydoc convention and commits a screenshot to
`docs/images/`.

## i18n (company-wide ADR-005)

Spec-only change — no user-facing strings ship here. The
implementing cycle adds Dutch (`nl_NL`) and English (`en_US`)
translation strings for: `Provincie`, `Kerntaak`, `Provinciefonds`,
`Algemene uitkering`, `Decentralisatie-uitkering`,
`Integratie-uitkering`, `Opcenten MRB`, `Tariefopslag`.
