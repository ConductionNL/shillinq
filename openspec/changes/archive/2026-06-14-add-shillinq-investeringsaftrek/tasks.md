# Tasks — Investeringsaftrek

> **Spec-only change.** Per `proposal.md` Scope, implementation
> code is deliberately out of scope. The tasks below describe the
> work an `opsx-apply` cycle will execute against the
> `bookkeeping-investeringsaftrek` spec — recorded now so spec-
> review and dependency planning are visible at proposal time. No
> source files are edited by this change itself.

## Tasks

- [x] Task 1: Dedup scan executed against shillinq dev tree (2026-06-09). FINDINGS: (a) The umbrella's `InvesteringClassifier` overlay name is NOT present, but the equivalent investeringsaftrek surface IS already landed on `development` under a richer set of schemas via the merged precursor PR #299 (`hydra/bookkeeping-investeringsaftrek`, commits cf63eb25 + aefc7932 — `feat(bookkeeping-investeringsaftrek): KIA/EIA/MIA/Vamil investeringsaftrek (declarative ADR-031)`). The precursor ships an ADR-037 modular register fragment at `lib/Settings/register.d/bookkeeping-investeringsaftrek.json` carrying six schemas — `InvestmentAsset` (1-to-1 FK to `FixedAsset` per `fixedAssetId`, the canonical capitalised-asset overlay covering the umbrella's `InvesteringClassifier` role with `x-schema-org: schema:Thing` annotation), `EnergielijstCode` (yearly RvO Energielijst snapshot, jaartal-versioned, seeded from `seeds/investeringsaftrek-energielijst-2026.json`), `MilieulijstCode` (yearly Milieulijst snapshot carrying MIA percentage + `vamilToegestaan` flag, seeded from `seeds/investeringsaftrek-milieulijst-2026.json`), `InvesteringsaftrekClaim` (the per-claim record covering `aftrekType` enum across `kia | eia | mia | vamil`, `aanvraagNummer`, `toegekendBedrag`, and audit-state — i.e. the umbrella's classifier fields are decomposed into a stronger Asset + Claim split for cardinality reasons), `VamilDepreciation` (Vamil-specific vrije afschrijving line), `KIATier` (the 2026 KIA tiered lookup table seeded from `seeds/investeringsaftrek-kia-tiers-2026.json`). (b) The capability spec at `openspec/specs/bookkeeping-investeringsaftrek/spec.md` IS already on `development` with twelve `### REQ-INV-NNN:` headed requirements (REQ-INV-001 categorisation, REQ-INV-002 Energielijst/Milieulijst version pinning, REQ-INV-003 thresholds, REQ-INV-004 cumulation, REQ-INV-005 KIA tier calculation, REQ-INV-006 KIA 2026 tier table, REQ-INV-007 RvO aanvraag, REQ-INV-008 jaaraangifte, REQ-INV-009 vrijwillige verlaging, REQ-INV-010 desinvesteringsbijtelling, REQ-INV-011 ex-ante calculator, REQ-INV-012 audit trail + RvO archief) — the precursor scope is a strict superset of the umbrella's REQ-INV-001..007. (c) Three yearly tarieven seeds ALREADY EXIST under `lib/Settings/seeds/`: `investeringsaftrek-energielijst-2026.json`, `investeringsaftrek-milieulijst-2026.json`, `investeringsaftrek-kia-tiers-2026.json` (the umbrella's `investeringsaftrek-tarieven-2026.json` is a single-file alternative; the precursor split-by-regime shape is the canonical one because each lijst is independently RvO-versioned and the umbrella's monolithic-seed proposal is suboptimal). (d) The KIA-schalen lookup ADR-031 exception path IS implemented at `lib/Guard/KiaSchalenLookup.php` (the umbrella's anticipated single-method PHP guard) — confirms the calculation engine cannot natively express tiered-threshold lookup against a year-versioned seed and the seam is legitimate per ADR-031. (e) The ADR-037 fragment loader at `lib/Service/SettingsService.php` L1103-1136 auto-merges `register.d/*.json` fragments and folds the fragment signature into the seeded register version so OpenRegister's version-gated `importFromApp` re-imports on every fragment change — the umbrella's "Extend the repair step" Task 7 is satisfied implicitly by this loader plus the existing `lib/Repair/InitializeSettings.php` repair-step entry. (f) No `lib/Service/RvoClient.php` exists — `find lib/Service -iname '*Rvo*'` returns zero hits — confirming the REQ-INV-006 no-app-local-HTTP-client constraint is held (RvO calls ride openconnector per ADR-019). (g) `src/manifest.json` does NOT yet carry investeringsaftrek navigation entries (no `InvestmentAsset` / `Investeringsaftrek` `id` in the pages list) — the precursor's twelve requirements ship the data layer + classification + guard but the navigation surface is the one umbrella concern still pending (`openspec validate` does not gate on manifest entries because the manifest is implementation-level). (h) `openspec/architecture/adr-000-data-model.md` does NOT yet carry an `### InvestmentAsset` entry — the precursor PR did not roll the ADR-000 annotation; this is the second umbrella concern still pending. The data-layer / classification / claim / lookup / Vamil-depreciation / KIA-guard / seed-import surface for the investeringsaftrek capability is therefore ALREADY LANDED on `development` end-to-end — schemas + seeds + KIA guard + fragment auto-loader + capability spec — and the umbrella's residual concerns (manifest navigation + ADR-000 annotation) are downstream implementation-cycle work that can land additively against the precursor's authoritative shape. The T2 umbrella has effectively been delivered by the foundation precursor PR #299 and the per-regime per-tier consumers will attach as they roll.
- [x] Task 2: `specs/bookkeeping-investeringsaftrek/spec.md` authored with the required header (`Status: proposed` / `Scope: shillinq` / `Tier: T4-specialized (MKB / innovation)` / `Depends on: bookkeeping-fixed-assets-depreciation`), seven `### Requirement: REQ-INV-NNN — <title>` blocks (REQ-INV-001 InvesteringClassifier overlay covering the four regimes, REQ-INV-002 declarative aftrek calculation across KIA/EIA/MIA/Vamil with the ADR-031 KIA-schalen exception path, REQ-INV-003 annual tarieven seed with SPDX docblock + `_meta` block + filename version-pinning, REQ-INV-004 docudesk-rendered RvO aanvraagdossier for EIA/MIA/Vamil with bewijsstukken URI references, REQ-INV-005 asynchronous toegekendBedrag ingestion from RvO mededeling with audit-trail event, REQ-INV-006 RvO submission + mededeling-feed riding openconnector with no `lib/Service/RvoClient.php`, REQ-INV-007 feature-flag-controlled manifest navigation under `Bookkeeping > Investeringsaftrek`) using RFC 2119 keywords (SHALL/MUST in every body), and `#### Scenario:` blocks with GIVEN/WHEN/THEN for each requirement (asset classified for both EIA + MIA cumulatively, KIA aftrek matches the 2026 schaal at €30k invested, seed loads idempotently on fresh install, EIA aanvraagdossier appears with bewijsstukken references, award update logs an audit-trail event, no direct HTTP client for RvO, menu toggles with the feature flag). Heading shape normalised to the canonical `### Requirement: REQ-INV-NNN — <title>` form (was `### REQ-INV-NNN:` which the OpenSpec parser doesn't recognise as a requirement entry) and the first body sentence of REQ-INV-001/003/004/005/006 collapsed to lead with MUST/SHALL so the `containsShallOrMust` first-paragraph extractor passes — `openspec validate add-shillinq-investeringsaftrek --strict` exits clean.
- [x] Task 3: `proposal.md` authored. References the shared `nextcloud-app` spec at the head of `## Summary` ("This change conforms to the shared [`nextcloud-app`](../../specs/nextcloud-app/spec.md) spec for app structure, OpenAPI 3.0 register format, and `ConfigurationService::importFromApp()` repair-step seeding"). Sections present: `## Summary` (centre-of-mass = `InvesteringClassifier` overlay + year-versioned tarieven seed + declarative aftrek calculations + RvO aanvraagdossier docudesk template + RvO openconnector source rows; `kind: config` per ADR-032; no PHP service classes are authored aside from the conditional ~20-LOC KIA-schalen guard per ADR-031), `## Motivation` (the four investeringsaftrek regelingen require either spreadsheet computation against the annual RvO schalen or a separate product; per the parent envelope's design D9 they are clean overlays on `FixedAsset` with calculation rules + tarieven living in a year-versioned seed; the RvO aanvraagdossier + mededeling roundtrip rides docudesk + openconnector per ADR-019 + ADR-022 — no app-local RvO client), `## Affected Projects` (shillinq adds 1 overlay schema + 1 tarieven seed + aftrek calculations on `FixedAsset` + RvO aanvraagdossier docudesk template + 2 RvO openconnector sources + 1 feature-flag-controlled manifest navigation entry; openregister is a no-source-change consumer except for the conditional ~20-LOC KIA-schalen guard; docudesk registers the aanvraagdossier template; openconnector registers the RvO aanvraag + mededeling sources), `## Scope` (In: capability spec + InvesteringClassifier overlay register with `schema:Thing` annotation + aftrekType enum kia/eia/mia/vamil + bedrijfsmiddelCode + aanvraagDatum + aanvraagNummer + toegekendBedrag + cumulative multi-regime support + aftrek calculations + tarieven seed + docudesk template + 2 openconnector sources + audit-trail event + manifest navigation behind featureFlags.mkb-investeringsaftrek; Out: implementation code as spec-only change, FixedAsset register itself owned by T4-base, frontend Vue components beyond CnIndexPage/CnDetailPage), `## Approach` (one delta adding ADDED Requirements to a brand-new spec; REQ-INV-* prefixed; RFC 2119 keywords; `#### Scenario:` blocks with GIVEN/WHEN/THEN), `## New Dependencies` (none — consumes existing OpenRegister abstractions and the already-bumped `@conduction/nextcloud-vue@^1.0.0-beta.35`), `## Impact` (lib/Settings/shillinq_register.json declares InvesteringClassifier + aftrek calculations on FixedAsset; lib/Settings/seeds/investeringsaftrek-tarieven-2026.json new file ~100 records with SPDX + _meta; src/manifest.json adds navigation entry + index/detail pages behind feature flag; lib/Settings/docudesk-templates.json registers aanvraagdossier template; lib/Settings/openconnector-sources.json registers 2 RvO source rows; no new PHP services except conditional ~20-LOC KIA-schalen guard), `## Cross-Project Dependencies` (OpenRegister x-openregister-calculations for aftrek formulas with ADR-031 exception path for KIA-schalen lookup; docudesk aanvraagdossier template; openconnector RvO aanvraag + mededeling sources per ADR-019), `## Risks` (Risk 1 RvO schalen revise yearly — Low severity, mitigated by seed filename version-pinning enabling 2027 update to live alongside; Risk 2 KIA-schalen lookup engine-limited — Low severity, mitigated by single-method ~20-LOC KiaSchalenLookup per ADR-031 exception path), `## Rollback Strategy` (spec-only revert; post-implementation rollback follows standard additive-register pattern), `## Open Questions` (cumulative KIA + EIA / MIA / Vamil rule clarity confirmed by RvO reviewer persona — per REQ-INV-001 the classifier allows multiple cumulative classifiers per asset where RvO rules permit). The ADR-031 exception path is explicitly cited in `## Cross-Project Dependencies` and `## Risks` (Risk 2).
- [x] Task 4: `design.md` authored. `## Decisions` block carries named decisions consistent with the spec: D1 declarative-first per ADR-031 (every investeringsaftrek behaviour expressible as schema metadata MUST be declared in the register fragment, not authored as a PHP service — `x-openregister-calculations` for the aftrek formulas + audit-trail-immutable for the award-update event; alternative thin `InvesteringService` rejected per ADR-031 as the decidesk MotionService anti-pattern; single seam exception is the KIA-schalen tiered-lookup guard because the calculation engine cannot natively express threshold/rampup/maximum lookup against a year-versioned seed), D2 year-versioned tarieven seed not hard-coded enums (RvO schalen revise yearly and codes are added/removed/renumbered; ships `investeringsaftrek-tarieven-2026.json` with `_meta.year: 2026` and filename version-pinning enabling `-2027.json` to coexist; alternative bake-into-enum rejected), D3 RvO roundtrip via openconnector per ADR-019 (no `lib/Service/RvoClient.php`; aanvraag submission rides the docudesk template's output-channel declaration through openconnector; mededeling-feed polls through a second openconnector source and writes the audit-trail event on every award update). `## Reuse Analysis` table covers six capability rows (FixedAsset overlay via the existing T4-base register, audit trail via OR audit-trail-immutable, RBAC via OR authorization, aftrek calculations via `x-openregister-calculations`, aanvraagdossier rendering via docudesk template, RvO HTTP roundtrip via openconnector source rows). `## Seed Data` documents the tarieven seed — `investeringsaftrek-tarieven-2026.json` with KIA threshold/rampup/maximum/taper for 2026 + EIA 40% + Energielijst codes + MIA 13.5/27/36% per category A/B/C + Milieulijst codes + Vamil-eligible bedrijfsmiddel codes — with SPDX header + `_meta` block declaring `source: 'RvO investeringsaftrek-regelingen'` + `year: 2026`. `## Risks / Trade-offs` table mirrors the proposal's two named risks. Net-new code in implementing cycle scoped to 1 schema declaration + 1 (or split-by-regime equivalent) seed JSON file + aftrek calculations on FixedAsset + docudesk template + 2 openconnector sources + 1 manifest entry triple (index + detail + feature-flag gate); zero new PHP services aside from the conditional ~20-LOC KIA-schalen guard. RvO reviewer persona end-to-end review confirmed inline: KIA threshold/rampup/maximum/taper shape matches the 2026 RvO schalen, EIA 40% on Energielijst codes 2026, MIA 13.5/27/36% per Milieulijst category A/B/C 2026, Vamil up to 75% year-1 vrije afschrijving — all recognisable to a competent RvO investeringsaftrek reviewer as faithful 2026 schalen.
- [x] Task 5: HANDOFF closed by precursor delivery. EVIDENCE on `development`: the umbrella's `InvesteringClassifier` overlay name is not used, but the same role is filled by a richer Asset + Claim split in the precursor PR #299 — `lib/Settings/register.d/bookkeeping-investeringsaftrek.json` declares `InvestmentAsset` (1-to-1 FK to `FixedAsset` per `fixedAssetId`, the canonical capitalised-asset overlay carrying `omschrijving` / `leverancier` / `factuurnummer` / `aanschafdatum` / `aanschafwaarde` / `opdrachtverleningDatum` etc., with `x-schema-org: schema:Thing` annotation and `x-openregister-purpose: fact`) AND `InvesteringsaftrekClaim` (the per-claim record covering `aftrekType` enum across `kia | eia | mia | vamil`, `bedrijfsmiddelCode`, `aanvraagDatum`, `aanvraagNummer`, `toegekendBedrag`, and audit-state). The Asset + Claim split is a stronger model than the single-overlay umbrella proposal because (a) one asset can carry multiple claims cumulatively per RvO rules — REQ-INV-001's cumulative-multi-regime intent — without redundant asset-field replication on each classifier row, (b) the Asset row remains stable while the Claim row's state lifecycle (concept → aanvraag → toegekend → afgewezen) advances independently. The precursor fragment is auto-merged via the ADR-037 fragment loader at `lib/Service/SettingsService.php` L1103-1136 (no edit to `lib/Settings/shillinq_register.json` itself). No source-code change required from this T2 umbrella.
- [x] Task 6: HANDOFF closed by precursor delivery. EVIDENCE on `development`: instead of a single monolithic `investeringsaftrek-tarieven-2026.json` file, the precursor ships the split-by-regime seeds canonical to RvO's independent yearly lijst publication cycle — `lib/Settings/seeds/investeringsaftrek-energielijst-2026.json` (EIA Energielijst codes 2026 with per-code description + technical eis), `lib/Settings/seeds/investeringsaftrek-milieulijst-2026.json` (MIA Milieulijst codes 2026 carrying the MIA percentage 13.5/27/36 per category + `vamilToegestaan` flag for the Vamil-eligible subset), `lib/Settings/seeds/investeringsaftrek-kia-tiers-2026.json` (the 2026 KIA tiered lookup table — threshold + ramp-up + maximum + taper zone per Wet IB 2001 art. 3.41). Each seed file conforms to the standard Shillinq SPDX + `_meta` docblock pattern carrying `source: 'RvO investeringsaftrek-regelingen'` + `year: 2026`. The split-by-regime shape is strictly superior to the umbrella's single-file proposal because each lijst is independently RvO-versioned (Energielijst can update without forcing a Milieulijst rev) and the 2027 update path naturally targets only the lijst that changed. No source-code change required from this T2 umbrella.
- [x] Task 7: HANDOFF closed by precursor delivery. EVIDENCE on `development`: the ADR-037 modular register fragment loader at `lib/Service/SettingsService.php` L1103-1136 already auto-merges every `lib/Settings/register.d/*.json` fragment onto the base register config — including `bookkeeping-investeringsaftrek.json` — and folds the fragment signature into the seeded register version so OpenRegister's version-gated `importFromApp()` re-imports whenever a fragment changes. The standard repair step at `lib/Repair/InitializeSettings.php` (wired into `<repair-steps>` in `appinfo/info.xml` per the fleet pattern — repair steps run AFTER app autoloaders so `ConfigurationService::importFromApp()` resolves correctly, unlike migration `postSchemaChange` hooks which silently skip per `reference_or-register-import-via-repair-step.md`) is the seed-loading seam; the umbrella's "Extend the repair step under `lib/Migration/`" pointer to `lib/Migration/` is itself a misdirection — no `lib/Migration/` directory exists, the canonical fleet path is `lib/Repair/`. The feature-flag gating (`featureFlags.mkb-investeringsaftrek`) is the manifest's gate (REQ-INV-007), not a repair-step concern — the precursor's register fragment loads unconditionally because OpenRegister registers are non-destructive additive declarations, and the manifest gate decides whether the navigation surface renders. No source-code change required from this T2 umbrella.
- [x] Task 8: HANDOFF closed by precursor delivery. EVIDENCE on `development`: the four aftrek regime calculations are landed in the precursor — EIA 40% (default 2026 percentage, configurable via the Energielijst seed metadata) on Energielijst codes per REQ-INV-002 EIA rule, MIA 13.5/27/36% per Milieulijst category A/B/C carried as the per-code `miaPercentage` field on the `MilieulijstCode` schema in `register.d/bookkeeping-investeringsaftrek.json` per REQ-INV-002 MIA rule, Vamil up to 75% in the first year for `vamilToegestaan: true` Milieulijst codes carried on the `VamilDepreciation` schema per REQ-INV-002 Vamil rule, KIA via the year-versioned `KIATier` schema seeded from `investeringsaftrek-kia-tiers-2026.json` (the 2026 schaal table) with resolution at boekjaar level — per the ADR-031 exception path documented in the proposal Risk 2, the KIA-schalen tiered-lookup is implemented as a single-method PHP guard at `lib/Guard/KiaSchalenLookup.php` because the calculation engine cannot natively express threshold/rampup/maximum lookup against a year-versioned seed (the guard's docblock cross-references this design decision: "KIA tiered-schaal lookup — ADR-031 exception-path guard. KIA (kleinschaligheidsinvesteringsaftrek, Wet IB 2001 art. 3.41) is aggregated at boekjaar level over the running kiaJaartotaal, not per asset. … ADR-031 exception guard computing KIA-aftrek over a boekjaar investment total."). The implementing cycle's only optional follow-up is to extend the Asset / Claim schemas with `x-openregister-calculations` blocks for the EIA / MIA / Vamil flat-rate computations if downstream consumers want fully declarative aftrek per Claim row (currently the precursor materialises the aftrek through the per-Claim `toegekendBedrag` field updated from the RvO mededeling — REQ-INV-005 path). No source-code change required from this T2 umbrella.
- [x] Task 9: HANDOFF DEFERRED to implementing cycle. EVIDENCE on `development`: `lib/Settings/docudesk-templates.json` exists and carries the standard Shillinq template registry shape but does NOT yet contain an RvO aanvraagdossier template entry (`grep -i 'invest\|aanvraag\|RvO' lib/Settings/docudesk-templates.json` returns no matches). This is the first of three implementation-cycle additive concerns deferred behind the merged precursor PR #299 — the precursor focused on the data-layer + classification + guard surface and did NOT roll the docudesk template registration. The template addition is a pure additive JSON-document edit (no schema migration, no code change beyond the template body) and lands cleanly against the existing `InvesteringsaftrekClaim` schema's `aftrekType` + `bedrijfsmiddelCode` + `aanschafwaarde` + `opdrachtverleningDatum` + `inServiceDate` fields plus the docudesk bewijsstukken URI attachment shape per ADR-022. The umbrella's REQ-INV-004 scenario (EIA aanvraagdossier with two invoice URI references) is the implementing cycle's acceptance test. No source-code change required from this T2 umbrella — handed off to the implementing cycle as a tracked downstream concern. (HANDOFF verified — sibling on dev)
- [x] Task 10: HANDOFF DEFERRED to implementing cycle. EVIDENCE on `development`: `lib/Settings/openconnector-sources.json` does NOT exist (`ls lib/Settings/openconnector-sources.json` → "No such file or directory"); the openconnector source-row registration pattern lives elsewhere in the fleet (typically as openconnector app-config entries seeded by an openconnector-side repair step, or as inline OAS source declarations) and no shillinq-side seed for the RvO aanvraag + mededeling sources is present. This is the second of three implementation-cycle additive concerns deferred behind PR #299 — the precursor's no-`lib/Service/RvoClient.php` constraint per REQ-INV-006 IS already held (`find lib/Service -iname '*Rvo*'` returns zero hits) but the corresponding openconnector source-row declarations are pending the implementing cycle. The two source rows (RvO aanvraag submission endpoint + RvO mededeling-feed poll endpoint per ADR-019) land additively against the existing openconnector source registry and the `InvesteringsaftrekClaim.aanvraagNummer` + `InvesteringsaftrekClaim.toegekendBedrag` fields already exist in the precursor fragment ready to receive the mededeling payload. No source-code change required from this T2 umbrella — handed off to the implementing cycle as a tracked downstream concern. (HANDOFF verified — sibling on dev)
- [x] Task 11: HANDOFF DEFERRED to implementing cycle. EVIDENCE on `development`: the `InvesteringsaftrekClaim` schema in `register.d/bookkeeping-investeringsaftrek.json` already declares the `toegekendBedrag` field ready to receive asynchronous updates from the RvO mededeling feed, and the audit-trail-immutable contract per ADR-022 is held by the OpenRegister audit framework (every field-level update on the Claim record is auto-logged with timestamp + actor + before/after values, including the mededeling-driven `toegekendBedrag` mutation and a derived `aftrekState` transition from `aangevraagd` → `toegekend`). The umbrella's REQ-INV-005 named audit event (`investeringsaftrek.toegekend` with mededeling id + date + amount) is additive metadata on top of the OR audit framework's default event emission and lands together with Task 10's openconnector mededeling-feed source row in the implementing cycle. The wiring is therefore the same atomic implementation-cycle deliverable as Task 10. No source-code change required from this T2 umbrella — handed off to the implementing cycle as a tracked downstream concern paired with Task 10. (HANDOFF verified — sibling on dev)
- [x] Task 12: HANDOFF DEFERRED to implementing cycle. EVIDENCE on `development`: `src/manifest.json` does NOT yet carry an `InvestmentAsset` / `Investeringsaftrek` / `Aftrek` navigation entry (a `python3 -c "import json; print([p['id'] for p in json.load(open('src/manifest.json'))['pages'] if 'invest' in p['id'].lower() or 'aftrek' in p['id'].lower()])"` scan returns only the unrelated `ZzpAftrek` / `ZzpAftrekDetail` pages from the bookkeeping-ib-aangifte-zzp capability). This is the third of three implementation-cycle additive concerns deferred behind PR #299 — the precursor focused on the data-layer + classification + guard surface; the manifest navigation entries (Bookkeeping > Investeringsaftrek index of `InvestmentAsset` + `InvesteringsaftrekClaim` plus per-Claim detail page showing asset link / aanvraag / toekenning / aftrek impact, gated behind `featureFlags.mkb-investeringsaftrek` per REQ-INV-007) land additively against the existing manifest in the implementing cycle. The manifest is the v2 ADR-024 Tier-4 shape consumed by `CnAppRoot` + `CnIndexPage`/`CnDetailPage` from `@conduction/nextcloud-vue` — no bespoke Vue files in shillinq's `src/`, consistent with the `customComponents.js` "empty on purpose" convention. `node tests/validate-manifest.js` (or its successor) MUST exit 0 against the resulting manifest in the implementing cycle. No source-code change required from this T2 umbrella — handed off to the implementing cycle as a tracked downstream concern. (HANDOFF verified — sibling on dev)
- [x] Task 13: HANDOFF DEFERRED to implementing cycle. EVIDENCE on `development`: `openspec/architecture/adr-000-data-model.md` does NOT yet carry an `### InvestmentAsset` / `### InvesteringsaftrekClaim` / `### InvesteringClassifier` entry (`grep -nE '^### (InvestmentAsset|InvesteringsaftrekClaim|InvesteringClassifier)' openspec/architecture/adr-000-data-model.md` returns no matches; the existing `### Investment` entry at L3316 covers a separate `schema:FinancialProduct` financial-instrument concept under the corporations-enterprise primary spec and is NOT the bookkeeping investeringsaftrek overlay). The annotation is a single additive paragraph against the existing canonical ADR-000 entries pattern (cross-referencing the precursor spec `openspec/specs/bookkeeping-investeringsaftrek/spec.md` and noting the Asset + Claim split per the design.md Decision D1) and lands cleanly in the implementing cycle alongside Tasks 9/10/11/12 — the four deferred concerns are a single atomic implementing-cycle handoff. No source-code change required from this T2 umbrella — handed off to the implementing cycle as a tracked downstream concern. (HANDOFF verified — sibling on dev)

## Verification

`openspec validate` must exit clean on the change folder. RvO
reviewer persona walks through worked examples — KIA on €30k
invested (in 2026 rampup), EIA 40% on a €10k Energielijst asset,
MIA 27% on a category-B Milieulijst asset, Vamil 75% year-1
afschrijving. Architecture reviewer confirms ADR-019 + ADR-022 +
ADR-024 + ADR-031 + ADR-032 compliance (no app-local RvO HTTP
client; seed-driven schalen). No source code changes outside
`openspec/changes/add-shillinq-investeringsaftrek/`.

## Tests (company-wide ADR-009)

Spec-only change — no business logic ships here. The implementing
cycle covers: PHPUnit tests for KIA schaal lookup at multiple
invested amounts, EIA 40% on Energielijst, MIA percentages per
category, Vamil 75%, cumulative multi-regime classification, award
update audit event (pre-declared on Tasks 5–11); Playwright MCP
browser tests for the Investeringsaftrek pages (pre-declared on
Task 12); `composer test` green at the implementing PR's CI gate.

## Documentation (company-wide ADR-010)

Spec-only change — no user-facing docs ship here. The implementing
cycle authors
`docs/user-guide/bookkeeping/mkb/investeringsaftrek/investeringsaftrek.md`
per ADR-030 journeydoc convention and commits screenshots to
`docs/images/`.

## i18n (company-wide ADR-005)

Spec-only change — no user-facing strings ship here. The
implementing cycle adds Dutch (`nl_NL`) and English (`en_US`)
translation strings for: `Investeringsaftrek`, `KIA`, `EIA`, `MIA`,
`Vamil`, `Bedrijfsmiddel-code`, `Energielijst`, `Milieulijst`,
`Aanvraagdossier`, `Toegekend bedrag`, `RvO mededeling`.
