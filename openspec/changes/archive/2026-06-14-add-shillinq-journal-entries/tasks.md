# Tasks — Journal Entries

> **Spec-only change.** Per `proposal.md` Scope, implementation code is
> deliberately out of scope here. The tasks below describe the work an
> `opsx-apply` cycle will execute against the `bookkeeping-journal-entries`
> spec — they are recorded now so the spec-review gate, dependency
> planning, and tier-cascade impact are all visible at proposal time. No
> source files are edited by this change itself.

## Tasks

- [x] Task 1: Dedup scan executed against shillinq dev tree (2026-06-09). FINDINGS: (a) Sibling capability spec `openspec/specs/bookkeeping-journal-entries/spec.md` is already on `development` (shipped via the T1 sibling change `openspec/changes/bookkeeping-journal-entries/`, REQ-JE-001..010 identical-shape to this T2 umbrella's draft). (b) The `JournalEntry` schema is NOT in `lib/Settings/shillinq_register.json`; it lives in the fragment file `lib/Settings/register.d/add-shillinq-bookkeeping-foundation.json` (slug `journalentry`, title `JournalEntry`, 13 properties matching REQ-JE-002 exactly: `journalNumber`, `entryDate`, `description`, `lines`, `sourceDocumentUri`, `sourceDocumentApp`, `journalType`, `cadence`, `reversesOn`, `glTransactionId`, `approvalState`, `administrationId`, `state`; `required: [journalNumber, entryDate, description, lines, journalType, approvalState, administrationId, state]`; `x-spec` traces to `openspec/changes/add-shillinq-bookkeeping-foundation/specs/bookkeeping-journal-entries/spec.md`). (c) `openspec/architecture/adr-000-data-model.md` carries the older `### JournalEntry` entry (L3592, "Primary spec: financial-reporting-accountability") with the legacy field shape (`entryNumber`, `debitAmount`/`creditAmount`, `isBalanced`, `accountCode`, `journalCode`, `vatAmount`). The T1 sibling supersets that shape with `journalNumber` + `lines[]` + `journalType` enum and defers `vatAmount` to Tier 5 — Task 12 captures that reconciliation.
- [x] Task 2: `specs/bookkeeping-journal-entries/spec.md` authored with the required header (`Status: proposed` / `Scope: shillinq` / `Tier: T1 (foundation)` / `Depends on: bookkeeping-general-ledger` (which depends on `bookkeeping-chart-of-accounts`)), ten `### Requirement: REQ-JE-001..010 — <title>` blocks using RFC 2119 keywords, and `#### Scenario:` blocks with GIVEN/WHEN/THEN per requirement (incl. memoriaal, recurring without cadence, unknown journalType rejection, reversing accrual auto-post next period, scheduled-workflow tick, docudesk FK linkage, post→GLTransaction materialisation, approval-workflow gate, manifest navigation, voided→excluded-from-balance). Heading format normalised to the canonical `### Requirement: REQ-JE-NNN — <title>` shape and SHALL/MUST prose added to REQ-JE-002 + REQ-JE-003 bodies so `openspec validate add-shillinq-journal-entries --strict` exits clean. ADR-022 + ADR-031 cited inline (ADR-022 in REQ-JE-001 audit-trail, REQ-JE-006 docudesk FK, REQ-JE-008 approval-workflow; ADR-031 in REQ-JE-004 / REQ-JE-005 scheduled-workflow non-anti-pattern enumeration).
- [x] Task 3: `proposal.md` authored. References the shared `nextcloud-app` spec at the head of `## Summary` ("This change conforms to the shared `nextcloud-app` spec for app structure, OpenAPI 3.0 register format, and `ConfigurationService::importFromApp()` repair-step seeding"). Sections present: `## Summary`, `## Motivation`, `## Affected Projects`, `## Scope` (In/Out), `## Approach`, `## New Dependencies`, `## Impact`, `## Cross-Project Dependencies`, `## Risks` (two risks with severity + mitigation — Risk 1: approval-policy amount-threshold binding; Risk 2: `ScheduledWorkflow` + n8n adapter availability), `## Rollback Strategy`, `## Open Questions`.
- [x] Task 4: `design.md` authored. Includes `## Reuse Analysis` (11-row capability/exists/strategy table), `### D1 — JournalEntry is the human surface; GL transactions are the materialised result`, `### D2 — Recurring + reversing schedules declarative via ScheduledWorkflow`, `### D3 — Source documents reference docudesk by FK, not by embedded blob`, `### D4 — Approval gate via OR's approval-workflow extension`, plus a `## Declarative-vs-imperative decision` table per ADR-031. Bookkeeper-persona memoriaal + recurring + reversing UX walked end-to-end against the schema field set; field/lifecycle/manifest shape recognises as a faithful memoriaalboeking + recurring-template + reversing-accrual flow with no surprises.
- [x] Task 5: HANDOFF to sibling change `bookkeeping-journal-entries` (already merged to `development` — 33/33 tasks closed via `bookkeeping-journal-entries-finish` merge bb7f3a68). The sibling shipped the `JournalEntry` schema in `lib/Settings/register.d/add-shillinq-bookkeeping-foundation.json` (fragment file, not the monolithic `shillinq_register.json`) with all 13 REQ-JE-002 fields present: `journalNumber`, `entryDate`, `description`, `lines` (`minItems: 2`, each line `{accountNumber, side, amount, description}`), `sourceDocumentUri`, `sourceDocumentApp`, `journalType`, `cadence`, `reversesOn`, `glTransactionId`, `approvalState`, `administrationId`, `state`. `required` array matches REQ-JE-002. Slug `journalentry`; `x-schema-org: schema:AccountingTransaction`; `x-spec` back-references the sibling's spec path. (HANDOFF verified — sibling on dev)
- [x] Task 6: HANDOFF to sibling change `bookkeeping-journal-entries` (already on `development`). The sibling declares `journalType` as a closed enum `["manual", "recurring", "reversing"]` on the schema. `cadence` is declared `nullable: true` with a description noting "Required when journalType is `recurring`, forbidden otherwise (REQ-JE-005)"; `reversesOn` likewise nullable with "Required when journalType is `reversing` (REQ-JE-004)". GAP: the conditional-required enforcement (`if journalType=recurring then cadence required; if=reversing then reversesOn required; otherwise both forbidden`) is documented in description prose but NOT yet declared as a JSON-Schema `allOf`/`oneOf` conditional or an `x-openregister-validation` block — at runtime the engine accepts a `manual` journal with a stray `cadence` and a `recurring` journal with no `cadence`. Tracked as a sibling-change follow-up; out of scope for this T2 spec-only envelope. (HANDOFF verified — sibling on dev)
- [x] Task 7: HANDOFF to sibling change `bookkeeping-journal-entries` (already on `development`). The sibling declares `x-openregister-lifecycle` on `JournalEntry` with `field: state`, `initialState: draft`, four states (`draft`, `pending`, `posted`, `voided`) matching REQ-JE-008, and five transitions (`submit: draft→pending`, `post: pending→posted`, `postDirect: draft→posted`, `reject: pending→draft`, `void: posted→voided`). DEVIATION from REQ-JE-008's strict "no app-local approval" posture: `post`/`postDirect`/`void` transitions declare `requires: "OCA\\Shillinq\\Lifecycle\\JournalEntryGuard::canPost"` (a PHP guard class) rather than the abstract `requires: { approval-workflow: { policy: ... } }` declarative form documented in `design.md` D4. The deviation is constrained — `JournalEntryGuard::canPost` delegates the policy decision rather than reimplementing approval state — but tightening to the declarative `{approval-workflow: {policy: "@self.amountPolicy"}}` shape is queued for the OR engine's approval-workflow extension landing (Risk 1 in `proposal.md`). Tracked as a sibling-change follow-up; out of scope for this T2 spec-only envelope. (HANDOFF verified — sibling on dev)
- [x] Task 8: HANDOFF to sibling change `bookkeeping-journal-entries` (already on `development`). The sibling declares `x-openregister-relations` on `JournalEntry` with ONE relation: `glTransaction → GLTransaction.id` (`one-to-one`, localField `glTransactionId`) per REQ-JE-007 back-reference. GAP: the per-line `lines[].accountNumber → Account.accountNumber` FK from REQ-JE-008 / Task 8's Account-FK requirement is NOT yet declared in `x-openregister-relations` — the constraint lives only as a description string on `lines.items.properties.accountNumber` ("FK to Account.accountNumber"). Per-row line-array FK declarations are an OR engine capability (`x-openregister-relations` does not currently address array-element FKs); tracked as an OR-side follow-up and sibling-change task. The sibling `add-shillinq-chart-of-accounts` (`Account.accountNumber`) and `add-shillinq-general-ledger` (`GLTransaction`) schemas are both shipped on `development` so the FK targets exist. Out of scope for this T2 spec-only envelope. (HANDOFF verified — sibling on dev)
- [x] Task 9: HANDOFF to sibling change `bookkeeping-journal-entries` (already on `development`). PARTIAL: the `cadence` object is declared on `JournalEntry` (interval enum {daily/weekly/monthly/yearly}, anchor, endsOn, count) per REQ-JE-005. The `reversesOn` field is declared on `JournalEntry` per REQ-JE-004. GAP: no top-level `x-openregister-scheduled-workflows` (or equivalent) block declares the cadence-to-`ScheduledWorkflow` binding — the sibling stores the cadence but does not yet wire the OR `ScheduledWorkflow` primitive to consume it on the cron tick. Per ADR-031, the `occ openregister:scheduled-workflow:run` fallback path is acceptable while the n8n adapter is unwired on the target environment; the sibling has not yet declared which path applies. Reversing-journal binding to the period-close trigger is owned by Tier 3 (`bookkeeping-period-close`) and out of scope for Tier 1. Tracked as a sibling-change follow-up; out of scope for this T2 spec-only envelope. (HANDOFF verified — sibling on dev)
- [x] Task 10: HANDOFF to sibling change `bookkeeping-journal-entries` (already on `development`). The sibling's `post`/`postDirect` lifecycle transitions both declare an `actions: [{trigger: "x-openregister-lifecycle-action", action: "materialise-gl-transaction", actionParameters: {register: "shillinq", schema: "GLTransaction", linesSchema: "GLLine", keepBalanced: true, backReferenceField: "journalEntryId", idempotencyKey: "journalNumber"}}]` block per REQ-JE-007 / REQ-JE-004 declarative materialisation. The OR engine consumes the lifecycle action and materialises one balanced `GLTransaction` + N `GLLine` rows from the `JournalEntry.lines[]` template — no PHP orchestration in shillinq. The transition references the `materialise-gl-transaction` action symbol rather than emitting a CloudEvent (the proposal Approach mentioned both shapes; the engine-side action route satisfies REQ-JE-004 equivalently). (HANDOFF verified — sibling on dev)
- [x] Task 11: HANDOFF to sibling change `bookkeeping-journal-entries` (already on `development`). Manifest entries shipped in `src/manifest.json`: menu entry under `Bookkeeping > Journals` (`menu[1].children[2]`, route `Journals`); `pages[45]` Journals index page (`/journals`, `config.schema: JournalEntry`, columns `journalNumber`/`entryDate`/`description`/`journalType`/`approvalState`/`state`, `detailRoute: JournalDetail`); `pages[46]` JournalDetail page (`/journals/:id`, `config.schema: JournalEntry`, surfaces `journalNumber`, `journalType`, `state`, `approvalState`, `sourceDocumentUri`, and the lines grid). REQ-JE-009 satisfied. `node tests/validate-manifest.js` exits 0 against the shipped manifest. (HANDOFF verified — sibling on dev)
- [x] Task 12: `openspec/architecture/adr-000-data-model.md` updated. One-paragraph annotation added under `### JournalEntry` (L3592) declaring the Tier-1 capability binding — confirms `bookkeeping-journal-entries` supersets the legacy ADR-000 entry (renames `entryNumber`→`journalNumber`, replaces flat `debitAmount`/`creditAmount`/`accountCode` with the `lines[]` array of `{accountNumber, side, amount, description}`, replaces stored `isBalanced` with the ADR-031 declarative balance derivation on `GLTransaction`, defers `vatAmount` to Tier 5 per `proposal.md` Out-of-Scope, adds the `journalType` closed enum + `cadence` + `reversesOn` fields). Cites ADR-022 (consume OR's approval-workflow + audit-trail-immutable + docudesk FK; no app-local approval table; no embedded blob) and ADR-031 (declarative lifecycle + declarative balance + scheduled-workflow primitive for recurring/reversing; no `RecurringJournalService` PHP class). Records the constrained ADR-031 deviation (the sibling's `JournalEntryGuard::canPost` PHP guard on `post`/`postDirect`/`void` transitions) with the explicit note that new lifecycle gates SHOULD wire `requires: {approval-workflow: {policy: ...}}` declaratively once the OR approval-workflow extension exposes amount-threshold policy binding (Risk 1).

## Verification

`openspec validate` must exit clean on the change folder. Bookkeeper-persona peer review (e.g. `/test-persona-janwillem` for SMB) confirms the schema shape matches a real memoriaal + recurring + reversing flow with the expected approval UX. Architecture reviewer confirms ADR-022 + ADR-024 + ADR-031 compliance (no app-local audit; no app-local approval table; no app-local cron; manifest carries the navigation; source documents are docudesk FK URIs not blobs). No source code changes outside `openspec/changes/add-shillinq-journal-entries/`.

## Tests (company-wide ADR-009)

Spec-only change — no business logic ships here. The implementation cycle (separate `opsx-apply`) is responsible for: PHPUnit unit tests covering lifecycle transitions (`pending → posted → voided`), recurring materialisation via the OR scheduled-workflow primitive, reversing journal posts inverse on period boundary, approval gate routes through OR's approval-workflow (pre-declared on Tasks 5–10); Playwright MCP browser tests for the Journals index + detail + create form (pre-declared on Task 11); `composer test` green at the implementing PR's CI gate. No new REST endpoints (OR exposes register CRUD generically).

## Documentation (company-wide ADR-010)

Spec-only change — no user-facing docs ship here. The implementation cycle authors `docs/user-guide/bookkeeping/journal-entries.md` per ADR-030 journeydoc convention and commits a Journal create-form screenshot to `docs/images/`.

## i18n (company-wide ADR-005)

Spec-only change — no user-facing strings ship here. The implementation cycle adds Dutch (`nl_NL`) and English (`en_US`) translation strings for: `Journal`, `Journal Entry`, `Memoriaal`, `Manual`, `Recurring`, `Reversing`, `Cadence`, `Reverses On`, `Source Document`, `Approval Pending`, `Approved`, `Posted`, `Voided`.
