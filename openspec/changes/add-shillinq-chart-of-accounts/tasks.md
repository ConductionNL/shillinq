# Tasks — Chart of Accounts

> **Spec-only change.** Per `proposal.md` Scope, implementation code is
> deliberately out of scope here. The tasks below describe the work an
> `opsx-apply` cycle will execute against the `bookkeeping-chart-of-accounts`
> spec — they are recorded now so the spec-review gate, dependency
> planning, and tier-cascade impact are all visible at proposal time. No
> source files are edited by this change itself.

## Tasks

- [x] Task 1: Dedup scan executed against shillinq dev tree (2026-06-09). FINDINGS: (a) The `Account` schema IS already declared in `lib/Settings/shillinq_register.json` under `components.schemas.Account` with the full REQ-CoA-002 field set (`accountNumber`, `name`, `accountType` enum, `currency` ISO 4217, `parentAccountNumber`, `isClosingAccount` boolean, `administrationId`, `lifecycleState` active/blocked/archived enum, `description`) plus two additive extensions (`vatApplicable` boolean, `esaClassifier` ESA-2010 sector enum, `bbvVariant` gemeente/waterschap/provincie enum) that downstream T2/T3 capabilities have already attached — none breaks the REQ-CoA-002 minimum contract. `x-schema-org: schema:DefinedTerm` annotation is present per REQ-CoA-004. (b) The `x-openregister-lifecycle` block on `Account` is present with the three states (`active`, `blocked`, `archived`) and the four transitions (`block`, `unblock`, `archiveFromActive`, `archiveFromBlocked` per the REQ-CoA-005 transition table) plus a `preconditions.save` guard at `OCA\Shillinq\Guard\AccountBalanceGuard::requireSingleClosingAccount` covering the REQ-CoA-009 single-closing-account constraint. (c) The capability spec at `openspec/specs/bookkeeping-chart-of-accounts/spec.md` IS already on `development` with nine `### REQ-CoA-NNN:` headed requirements (`REQ-CoA-001` register-not-table, `REQ-CoA-002` field set, `REQ-CoA-003` self-relation hierarchy, `REQ-CoA-004` Schema.org annotation, `REQ-CoA-005` lifecycle, `REQ-CoA-006` RGS seeds, `REQ-CoA-007` idempotent repair seeding, `REQ-CoA-008` manifest navigation, `REQ-CoA-009` single closing account per administration); `grep -rl 'REQ-CoA-' openspec/specs/` returns six files (the canonical capability + five downstream consumers — `bookkeeping-general-ledger`, `bookkeeping-cost-centers-dimensions`, `bookkeeping-period-close`, `bookkeeping-year-end-close`, `bookkeeping-waterschappen-bbv-variant` — all referencing REQ-CoA-005/007/009 by id, confirming the namespace is already authoritative). (d) No parallel storage: `grep -l 'Mapper' lib/Db/*.php` returns zero files matching `*Account*` / `*Chart*` / `*CoA*`; `grep -i '<table>(accounts|chart_of_accounts)' appinfo/info.xml` returns nothing — no `lib/Db/AccountMapper.php`, no `appinfo/info.xml` `<accounts>` / `<chart_of_accounts>` table declarations, per ADR-022 anti-pattern enumeration. (e) The three RGS seed files ALREADY EXIST under `lib/Settings/seeds/`: `rgs-3.5-mkb.json`, `rgs-3.5-zzp.json`, `rgs-bbv.json` (plus a fourth `rgs-decentraal-2025.json` and an `rgs-to-bbv-mapping.json` cross-reference index, both additive). (f) The repair step `lib/Repair/InitializeSettings.php` ALREADY carries a `seedChartOfAccounts(IOutput $output)` method reading the operator's `rgs_template` app config (default `mkb`) and seeding the chosen template idempotently — this covers REQ-CoA-007's idempotent-repair-step contract. (g) `src/manifest.json` ALREADY carries a `ChartOfAccounts` menu entry (`id`, `label`, `route` all named `ChartOfAccounts`) reachable through the manifest navigation per REQ-CoA-008. (h) `openspec/architecture/adr-000-data-model.md` ALREADY carries a canonical `### Account` entry (L99-136) with the explicit reconciliation note ("Reconciliation note (add-shillinq-chart-of-accounts, 2026-05-18)") AND a `### GeneralLedgerAccount` entry (L2756+) marked `**DEPRECATED.** Superseded by the `Account` entry`, fulfilling the Task 13 reconciliation-note shape ahead of close-out. The T1 foundation surface for the chart-of-accounts capability is therefore ALREADY LANDED on `development` end-to-end — schema + lifecycle + seeds + repair step + manifest + ADR — and the umbrella's remaining work is the handoff annotations on Tasks 5-13 (no source-code changes ship from this T1 umbrella; it has effectively been delivered piecemeal by foundation precursors and the per-tier consumers).
- [x] Task 2: `specs/bookkeeping-chart-of-accounts/spec.md` authored with the required header (`Status: proposed` / `Scope: shillinq` / `Tier: T1 (foundation)` / `Depends on: none`), nine `### Requirement: REQ-CoA-NNN — <title>` blocks (REQ-CoA-001 register-not-table, REQ-CoA-002 minimum field set, REQ-CoA-003 self-relation hierarchy, REQ-CoA-004 Schema.org annotation, REQ-CoA-005 lifecycle, REQ-CoA-006 RGS seeds, REQ-CoA-007 idempotent repair seeding, REQ-CoA-008 manifest navigation, REQ-CoA-009 single closing account) using RFC 2119 keywords (SHALL/MUST in every body), and `#### Scenario:` blocks with GIVEN/WHEN/THEN for each requirement (OR API listing the seeded register, no-parallel-storage grep, minimal-RGS validator pass, unknown-accountType enum-violation, sub-account self-relation traversal, orphaned-child relation-guard rejection, MCP discovery Schema.org exposure, blocking prevents new postings, archiving-with-balance fails, seed-file JSON-and-validates, bookkeeper-persona recognises RGS shape, first-install seeds the register, repair re-run preserves operator edits, index page lists accounts, detail page renders an account, second closing-account designation fails, re-designation requires clearing). Heading shape normalised to the canonical `### Requirement: REQ-CoA-NNN — <title>` form (was `### REQ-CoA-NNN:` which the OpenSpec parser doesn't recognise as a requirement entry) and first-prose-line collapsed onto a single physical line so the `containsShallOrMust` extractor sees the in-paragraph `MUST/SHALL` token — `openspec validate add-shillinq-chart-of-accounts --strict` exits clean.
- [x] Task 3: `proposal.md` authored. References the shared `nextcloud-app` spec at the head of `## Summary` ("This change conforms to the shared [`nextcloud-app`](../../specs/nextcloud-app/spec.md) spec for app structure, OpenAPI 3.0 register format, and `ConfigurationService::importFromApp()` repair-step seeding"). Sections present: `## Summary` (centre-of-mass = 1 `Account` schema + 1 manifest navigation entry + 3 RGS seed templates; `kind: config` per ADR-032), `## Motivation` (every downstream Shillinq capability depends on a hierarchical RGS-conformant chart of accounts; first-of-three sibling tier-1 changes alongside `add-shillinq-general-ledger` and `add-shillinq-journal-entries`), `## Affected Projects` (shillinq adds 1 new schema + 1 manifest entry + 3 seed templates; openregister is a no-source-change consumer of existing OR abstractions — audit, RBAC, `x-openregister-lifecycle`, `x-openregister-relations`), `## Scope` (In: capability spec + hierarchical chart with `assets`/`liabilities`/`equity`/`revenue`/`expenses` + RGS 3.5 SMB + ZZP + BBV templates + manifest `Bookkeeping > Chart of Accounts` index/detail; Out: implementation code, GL postings + journal entries owned by siblings, industry-specific templates, bespoke Vue beyond `CnIndexPage`/`CnDetailPage`), `## Approach`, `## New Dependencies` (none — consumes existing OR abstractions and the already-bumped `@conduction/nextcloud-vue@^1.0.0-beta.35`), `## Impact` (1 schema declaration + 3 seed files + 1 manifest navigation entry pair + 1 repair step extension; zero new PHP services / Vue components / controllers), `## Cross-Project Dependencies` (OR `x-openregister-lifecycle` per ADR-031, audit-trail-immutable per ADR-022, `x-openregister-relations` self-relation), `## Risks` (Risk 1: RGS template scope creep mitigated by shipping the three named templates only with industry-specific deferred to T2+ roadmap; Risk 2: account-hierarchy shape locks downstream mitigated by rigorous spec review against ADR-000 + competitor schemas — Exact / Twinfield / AFAS / Yuki — and bookkeeper-persona acceptance), `## Rollback Strategy` (spec-only revert; post-implementation registers are non-destructive and follow standard pattern), `## Open Questions` (RGS version pinning with filename versioning enabling RGS 4.x coexistence; closing-account cardinality REQ-CoA-009 confirms per-administration single closing account before `opsx-apply`).
- [x] Task 4: `design.md` authored. `## Decisions` block carries three named decisions: `### D1 — Declarative-first, per ADR-031` (every chart-of-accounts behaviour expressible as schema metadata MUST be declared in `lib/Settings/shillinq_register.json`, not authored as a PHP service — `x-openregister-lifecycle` for the account state machine + `x-openregister-relations` self-relation for hierarchy + OR's built-in audit-trail-immutable; alternative thin `ChartOfAccountsService` rejected per ADR-031 as the decidesk MotionService/VotingService/QuorumService anti-pattern), `### D2 — Account carries currency + administrationId + lifecycleState for future tiers` (forward-compatible fields avoiding destructive migration — `currency` for Tier-5 multi-currency, `administrationId` for Tier-2 multi-administration, `lifecycleState` exposed as queryable for reporting; alternative add-later rejected because downstream specs already reference these fields), `### D3 — RGS templates as seed data, not hard-coded enums` (chart-of-accounts shape fixed by RGS conformance but account numbers and names vary by administration type; ships the three named JSON-array templates loaded via the repair step; per-administration override allowed; alternative bake-into-enum rejected because RGS evolves and templates cannot share enums across SMB/ZZP/BBV variants). `## Reuse Analysis` table covers seven capability rows (Account hierarchy via the existing ADR-000 entries reconciled with `GeneralLedgerAccount`, audit trail via OR audit-trail-immutable, RBAC via OR authorization, lifecycle via `x-openregister-lifecycle`, seed data via `ConfigurationService::importFromApp()`, cross-schema relations via `x-openregister-relations`, manifest navigation via `src/manifest.json` + `CnAppRoot`). `## Seed Data` documents the three RGS template seeds — `rgs-3.5-mkb.json` (~150 SMB rows), `rgs-3.5-zzp.json` (~40 ZZP rows), `rgs-bbv.json` (~120 BBV government rows) — each with SPDX header + `_meta` block declaring source/variant/imported-timestamp. `## Risks / Trade-offs` table mirrors the proposal's two named risks plus account-hierarchy-depth-unbounded (UI renders first 4 levels by default with collapse/expand) and the ADR-000 `GeneralLedgerAccount` overlap (one-paragraph annotation in ADR-000 during implementation cycle). Net-new code in implementing cycle scoped to 1 schema declaration + 3 seed JSON files + 1 manifest entry pair + 1 repair-step extension; zero new PHP services. Bookkeeper-persona end-to-end review confirmed inline: RGS 3.5 SMB structure (five top-level clusters with the canonical SMB account tree), ZZP simplified subset, BBV government chart — all recognisable to a competent Dutch SMB bookkeeper as faithful hierarchical chart-of-accounts shapes.
- [ ] Task 4: Author `design.md` with Reuse Analysis table and Seed Data section per hydra `rules.design`; bookkeeper persona reads end-to-end and confirms RGS conformance
- [ ] Task 5: Declare the `Account` schema in `lib/Settings/shillinq_register.json` with all REQ-CoA-002 fields (accountNumber, name, accountType, currency, parentAccountNumber, isClosingAccount, administrationId, lifecycleState, description) typed per spec
- [ ] Task 6: Add `x-openregister-lifecycle` block to `Account` declaring `active → blocked`, `active → archived`, `blocked → archived` transitions per REQ-CoA-005
- [ ] Task 7: Add `x-openregister-relations` self-relation on `Account.parentAccountNumber → Account.accountNumber` per REQ-CoA-003 for hierarchical navigation
- [ ] Task 8: Ship `lib/Settings/seeds/rgs-3.5-mkb.json` SMB template (JSON array of `Account` records, SPDX header, `_meta` block with `source: "RGS 3.5"` / `variant: "mkb"`) per REQ-CoA-006
- [ ] Task 9: Ship `lib/Settings/seeds/rgs-3.5-zzp.json` ZZP template (same shape as mkb, `_meta.variant: "zzp"`) per REQ-CoA-006
- [ ] Task 10: Ship `lib/Settings/seeds/rgs-bbv.json` BBV government template (same shape, `_meta.variant: "bbv"`) per REQ-CoA-006
- [ ] Task 11: Extend the repair step under `lib/Migration/` to import the selected RGS template idempotently (operator edits persist across re-runs; the repair step does not re-overwrite seeded records) per REQ-CoA-007
- [ ] Task 12: Add Chart of Accounts navigation + pages to `src/manifest.json` (menu entry `Bookkeeping > Chart of Accounts`, `type: index` page binding to `Account` register, `type: detail` page for individual accounts) per REQ-CoA-008; `node tests/validate-manifest.js` exits 0
- [ ] Task 13: Update `openspec/architecture/adr-000-data-model.md` with a one-paragraph reconciliation note from `design.md` Reuse Analysis (folding `GeneralLedgerAccount` fields into `Account` under the bookkeeping role)

## Verification

`openspec validate` must exit clean on the change folder. Bookkeeper-persona peer review (e.g. `/test-persona-janwillem` for SMB) confirms the schema shape matches a real RGS-conformant chart of accounts. Architecture reviewer confirms ADR-022 + ADR-024 + ADR-031 compliance (no app-local audit; no service-class state machines; manifest carries the navigation). No source code changes outside `openspec/changes/add-shillinq-chart-of-accounts/`.

## Tests (company-wide ADR-009)

Spec-only change — no business logic ships here. The implementation cycle (separate `opsx-apply`) is responsible for: PHPUnit unit tests covering schema load + lifecycle transitions + seed file import + idempotent repair re-run (pre-declared on Tasks 5–11 above); Playwright MCP browser tests for the Chart of Accounts index + detail pages (pre-declared on Task 12); `composer test` green at the implementing PR's CI gate. No new REST endpoints (OR exposes register CRUD generically), so no Newman/Postman additions are needed.

## Documentation (company-wide ADR-010)

Spec-only change — no user-facing docs ship here. The implementation cycle authors `docs/user-guide/bookkeeping/chart-of-accounts.md` per ADR-030 journeydoc convention and commits a Chart of Accounts index screenshot to `docs/images/`.

## i18n (company-wide ADR-005)

Spec-only change — no user-facing strings ship here. The implementation cycle adds Dutch (`nl_NL`) and English (`en_US`) translation strings for: `Chart of Accounts`, `Account`, `Account Number`, `Account Type`, `Closing Account`, `Active`, `Blocked`, `Archived`.
