# Tasks — SEPA Direct Debit (Incasso)

> **Spec-only change.** Per `proposal.md` Scope, implementation code is
> deliberately out of scope here. The tasks below describe the work an
> `opsx-apply` cycle will execute against the
> `bookkeeping-sepa-direct-debit` spec — they are recorded now so the
> spec-review gate, dependency planning, and tier-cascade impact are
> all visible at proposal time. No source files are edited by this change
> itself.

## Tasks

- [ ] Task 1: Confirm no `bookkeeping-sepa-direct-debit` capability spec already exists, no `SepaMandate`/`DirectDebitCollection`/`DirectDebitBatch`/`RTransaction`/`PreNotification` schemas are declared, and no `lib/Service/Sepa*` / `lib/Service/Collection*` / `lib/Service/Incasso*` PHP classes are present; scan for any legacy incasso code from earlier app versions
- [ ] Task 2: Author `specs/bookkeeping-sepa-direct-debit/spec.md` with `Status: proposed` / `Scope: shillinq` / `Tier: T2 (compliance + operations)` / `Depends on: bookkeeping-accounts-receivable-core, bookkeeping-bank-connectors, bookkeeping-chart-of-accounts` header, `REQ-SDD-NNN` requirements using RFC 2119 keywords, and `#### Scenario:` blocks with GIVEN/WHEN/THEN; cite ADR-022 + ADR-031 + SDD Core/B2B rulebooks inline
- [ ] Task 3: Author `proposal.md` summarizing mandate registration, sequence-type derivation, pre-notification (14-day default), submission-window enforcement (D-5/D-2/D-1), pain.008 generation + validation, pain.002/camt.054 ingestion, R-transaction handling, reposting, dormancy expiry, and 7-year audit trail; include Affected Projects / Scope / Risks (OR ScheduledWorkflow stability, business-day calendar, pain XML marshalling complexity, reposting heuristic edge cases, mandate document storage) / Rollback / Open Questions
- [ ] Task 4: Author `design.md` documenting the eleven decisions (D1–D11): mandate as source of truth, sequence-type derived not input, pre-notification legally mandated before pain.008, submission windows enforced, pain.008 validates against EBA + Dutch overlay, pain.002/camt.054 event-driven, R-transaction captured separately, reposting gated by reason-code heuristic, mandate cancellation blocks future collections, dormancy expiry automatic, audit-trail + pain-file archival for 7 years
- [ ] Task 5: Declare the `SepaMandate` schema in `lib/Settings/shillinq_register.json` with all REQ-SDD-001 fields (mandateReference, creditorIdentifier, scheme, type, status, signedAt, signedBy, debtorIban, debtorBic, debtorName, debtorAddress, debtorAccountType, firstCollectionDate, lastCollectionDate, lastUsedAt, mandateDocument, cancellationReason, administrationId)
- [ ] Task 6: Declare the `DirectDebitCollection` schema in `lib/Settings/shillinq_register.json` with all REQ-SDD-002 fields (mandateId, invoiceId, amount, currency, sequenceType, requestedCollectionDate, endToEndId, status, submittedInBatchId, pain002ReasonCode, camt054ReferenceId, repostedAsCollectionId, administrationId) — sequenceType is auto-derived, not operator-input
- [ ] Task 7: Declare the `DirectDebitBatch` schema in `lib/Settings/shillinq_register.json` with all REQ-SDD-005 fields (messageId, creationDateTime, requestedCollectionDate, scheme, sequenceType, collectionCount, controlSum, status, pain008Xml, pain002Xml, submittedAt, administrationId)
- [ ] Task 8: Declare the `RTransaction` schema in `lib/Settings/shillinq_register.json` with all REQ-SDD-007 fields (collectionId, type, reasonCode, reasonText, originatorBic, transactionAmount, valueDate, notifiedAt, administrationId)
- [ ] Task 9: Declare the `PreNotification` schema in `lib/Settings/shillinq_register.json` with all REQ-SDD-003 fields (collectionId, sentAt, channel, noticeDays, recipientAddress, administrationId)
- [ ] Task 10: Extend the existing `Counterparty` schema with `defaultMandateId` (nullable FK to `SepaMandate`, optional, used as default for invoice creation)
- [ ] Task 11: Extend the existing `Invoice` schema with `paymentMethod` (enum: `bank_transfer`, `direct_debit`, `cash`, etc.) and `directDebitMandateId` (nullable, required when `paymentMethod = direct_debit`); optionally add `directDebitPreNotificationInvoiceId` (nullable FK to self, marking invoice as pre-notification carrier)
- [ ] Task 12: Add `x-openregister-lifecycle` to `SepaMandate` declaring transitions: `pending → active` (operator-triggered, with mandate-document validation), `active → cancelled` (operator-triggered, with `cancellationReason` recording), `active → expired` (automated at 36 months via ScheduledWorkflow, per REQ-SDD-008), `cancelled → active` (never; terminal), `expired → active` (never; requires fresh mandate)
- [ ] Task 13: Add `x-openregister-lifecycle` to `DirectDebitCollection` declaring transitions: `scheduled → submitted` (batch-generation trigger, with pre-notification + submission-window guards per REQ-SDD-003 + REQ-SDD-004), `submitted → accepted_by_bank` (pain.002 ingestion, per REQ-SDD-006), `submitted → rejected` (pain.002 ingestion with rejection code, per REQ-SDD-006), `accepted_by_bank → succeeded` (camt.054 ingestion, per REQ-SDD-007), `accepted_by_bank → refunded` (camt.054 R-transaction, per REQ-SDD-007), `rejected → scheduled` (reposting, one-click, per REQ-SDD-009, only for bank-problem reason codes)
- [ ] Task 14: Declare sequence-type derivation as `x-openregister-aggregations` query precondition on `DirectDebitCollection` creation: query mandate's collection history; evaluate FRST/RCUR/OOFF/FNAL rules per REQ-SDD-002; block if manual `sequenceType` input is provided
- [ ] Task 15: Declare pre-notification blocking as `x-openregister-lifecycle.requires` precondition on batch generation: query all collections in batch; refuse if any `PreNotification.sentAt IS NULL` AND invoice does not carry pre-notification line annotation per REQ-SDD-003
- [ ] Task 16: Declare submission-window validation as `x-openregister-lifecycle.requires` precondition on batch generation: compute business-day distance from today to collection date (using Dutch holiday calendar per OR or inline 2026 holidays); refuse if insufficient window per REQ-SDD-004 (D-5 FRST/OOFF CORE, D-2 RCUR/FNAL CORE, D-1 B2B)
- [ ] Task 17: Implement pain.008 generation + validation per REQ-SDD-005: consume from `bookkeeping-bank-connectors` stack (marshalling already exists); add validation gate checking: mandate signed-at <= collection date, pre-notification exists, IBAN checksum valid, correct `PmtInf` blocking per `(scheme, sequenceType, requestedCollectionDate)` tuple, creditor ID carried, control sum + count correct; store pain.008 XML in `DirectDebitBatch.pain008Xml` (non-deletable, bewaarplicht 7 years)
- [ ] Task 18: Implement pain.002 ingestion per REQ-SDD-006: consume from bank-connector (webhook or polling event); parse `GrpSts`, iterate `TxInfAndSts` entries; update `DirectDebitBatch.status` (ACCP → accepted_by_bank, PART → partially_rejected, RJCT → fully_rejected); update each `DirectDebitCollection.status` and `pain002ReasonCode`; store pain.002 XML in `DirectDebitBatch.pain002Xml` (non-deletable)
- [ ] Task 19: Implement camt.054 reconciliation per REQ-SDD-007: consume from bank-connector; parse debit entries (match `endToEndId`), returns, refunds, etc.; for each matched debit: transition collection to `succeeded`, mark invoice as paid, post journal entry (debit bank, credit receivable); for each return/refund: create `RTransaction` record, transition collection to `refunded`, re-open invoice, reverse journal entry; for refund with reason `MD06`: flag mandate for bookkeeper review
- [ ] Task 20: Implement one-click reposting per REQ-SDD-009: on rejected collection detail page, offer "Opnieuw incasseren" button; validate reason code is bank-problem (not debtor-refusal); if valid, create new `DirectDebitCollection` for same mandate/amount, set `requestedCollectionDate` to next valid date (respecting submission window), set `sequenceType: RCUR` (mandate is now active), set original collection's `repostedAsCollectionId` to new collection; if invalid, refuse with explanation
- [ ] Task 21: Implement mandate cancellation blocking per REQ-SDD-008: on `SepaMandate.cancel()`, set `status: cancelled`, record `cancellationReason`; add precondition on `DirectDebitCollection` creation: refuse if mandate status = `cancelled` or `expired` (error: `sdd.mandate.cancelled` / `sdd.mandate.expired`)
- [ ] Task 22: Implement 36-month dormancy expiry per REQ-SDD-008: implement ScheduledWorkflow or daily job checking all `active` mandates; if `lastUsedAt` is > 36 months in past, transition to `expired` and send bookkeeper notification (email + dashboard alert) to re-collect signature
- [ ] Task 23: Implement audit-trail export per REQ-SDD-010: add endpoint `/api/v1/sepa-mandate/{mandateId}/audit-export` and `/api/v1/invoice/{invoiceId}/sepa-audit-export` returning ZIP containing: mandate document PDF, CSV of collections + statuses + reason codes, pain.008/pain.002/camt.054 XML files, R-transaction records, journal entries; complete within 10 seconds for typical 24-month mandate history
- [ ] Task 24: Implement pain-file archival governance per REQ-SDD-010: mark all `DirectDebitBatch.pain008Xml`, `DirectDebitBatch.pain002Xml`, `RTransaction.*` as non-deletable via OR's retention policy (or application-level flag `isArchived: true`, block deletion unless explicit override); add deletion endpoint `/api/v1/sepa-batch/{batchId}/delete-with-retention-override` requiring operator confirmation + reason + timestamp
- [ ] Task 25: Add 4 manifest navigation entries (`SEPA Mandates`, `Direct Debit Collections`, `Batches`, `Rejections & Returns`) + their `type: index` / `type: detail` pages to `src/manifest.json` per REQ-SDD (navigation flow: Mandates list → detail (collections, pre-notifications, R-transactions, export audit); Collections list → detail (status, reason, repost action); Batches list → detail (pain.008 XML, pain.002 status, submission window); Rejections list → detail (reason code breakdown, R-transaction details, repost eligibility)); verify `node tests/validate-manifest.js` exits 0
- [ ] Task 26: Update `openspec/architecture/adr-000-data-model.md` with `SepaMandate`, `DirectDebitCollection`, `DirectDebitBatch`, `RTransaction`, `PreNotification` entries in the entity table, noting relationships (mandate ← collections, batch ← collections, mandate ← pre-notifications, collection ← R-transactions, mandate ← Counterparty defaultMandateId FK, Invoice ← directDebitMandateId FK), and reconciling against any existing Payment/DirectDebit entries

## Verification

`openspec validate` must exit clean on the change folder. Bookkeeper-persona peer review (e.g. `/test-persona-janwillem` for SMB bookkeeper, or `/test-persona-annemarie` for VNG accountant) confirms the incasso flow matches Dutch SMB practice (mandate intake → collection sequence-type derivation → pre-notification dispatch → pain.008 batch generation → pain.002 acceptance → camt.054 settlement or R-transaction handling → reposting for bank problems → mandate cancellation / dormancy expiry → audit-trail export). Architecture reviewer confirms ADR-022 + ADR-024 + ADR-031 compliance (mandate workflow declarative or ADR-031-exception-annotated guard; pain.008/pain.002/camt.054 handled per bank-connector contract; no app-local collection-orchestration service; manifest carries navigation; audit trail non-deletable for 7 years). Legal/compliance reviewer confirms SDD Core + B2B rulebook compliance (sequence types, submission windows, pre-notification timeline, 36-month dormancy, reason codes, R-transaction handling per PSD2 Art. 76 refund rights). No source code changes outside `openspec/changes/bookkeeping-sepa-direct-debit/`.

## Tests (company-wide ADR-009)

Spec-only change — no business logic ships here. The implementation cycle (separate `opsx-apply`) is responsible for:

- **Unit tests** (PHPUnit): sequence-type derivation (FRST/RCUR/OOFF/FNAL per mandate history), pre-notification creation + blocking, submission-window calculation (business days, Dutch holidays), mandate cancellation + dormancy expiry, reposting eligibility (reason-code heuristic), pain.002/camt.054 parsing + status update, R-transaction creation + journal reversal
- **Integration tests**: mandate → collection → pre-notification → batch generation → pain.008 validation; pain.002 ingestion updating batch/collection states; camt.054 debit confirmation closing collection + journal posting; camt.054 return reversing journal
- **Playwright MCP browser tests**: manifest navigation (Mandates, Collections, Batches, Rejections pages); mandate detail (lifecycle transitions, cancel action); collection detail (repost action, reason-code display); batch generation workflow (pre-notification check, submission-window check, pain.008 XML display); audit-bundle export
- **Performance tests**: batch generation with 1000 collections (10 s target); camt.054 ingestion with 500 R-transactions (5 s target); audit-export with 24-month history (10 s target)
- `composer test` green at implementing PR's CI gate

## Documentation (company-wide ADR-010)

Spec-only change — no user-facing docs ship here. The implementation cycle authors:

- `docs/user-guide/bookkeeping/sepa-direct-debit.md` per ADR-030 journeydoc convention: mandate intake workflow, pre-notification dispatch, batch generation + submission, pain.002/camt.054 reconciliation, R-transaction handling, reposting, dormancy renewal, audit-export for tax/compliance review
- Screenshot gallery in `docs/images/sepa-*`: mandate detail, collection list, batch generation (pain.008 summary), rejection handling, audit-export result
- Reference table: SDD CORE submission windows, B2B windows, sequence-type rules, reason-code taxonomy (bank problems vs debtor refusals), R-transaction types, pre-notification channels
- Troubleshooting: "why was my batch rejected?", "how do I repost a failed collection?", "what does this reason code mean?", "how do I export for my accountant?"

## i18n (company-wide ADR-007)

Spec-only change — no user-facing strings ship here. The implementation cycle adds Dutch (`nl_NL`) and English (`en_US`) translation strings for:

- Entity names: `SEPA Mandate`, `Direct Debit Collection`, `Batch`, `Rejection`, `Return`, `Refund`
- Status values: `pending`, `active`, `cancelled`, `expired`, `scheduled`, `submitted`, `accepted_by_bank`, `succeeded`, `rejected`, `refunded`
- Sequence types: `FRST` (first), `RCUR` (recurrent), `OOFF` (one-off), `FNAL` (final)
- Actions: `Cancel Mandate`, `Retry Collection`, `Generate Batch`, `Export Audit Trail`, `Delete with Retention Override`
- Notifications: `Pre-notification sent`, `Collection succeeded`, `Collection rejected (reason: AC04)`, `Mandate expired after 36 months inactivity`, `High refund rate — review mandate eligibility`
- Validation errors: `sdd.mandate.scheme.mismatch`, `sdd.mandate.reference.duplicate`, `sdd.mandate.oneoff.second_collection_refused`, `sdd.prenotification.too_short`, `sdd.submission.window.late`, `sdd.mandate.signed.after.collection`, `sdd.mandate.debtor_refusal`, `sdd.mandate.cancelled`, `sdd.mandate.expired`
- Reason codes (ISO 20022): `AC01` (invalid debit date), `AC04` (closed account), `AM04` (insufficient funds), `MS03` (no reason given), `MD01` (no mandate), `MD06` (consumer refund request), etc.
