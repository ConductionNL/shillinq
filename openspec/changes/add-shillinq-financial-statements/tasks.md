# Tasks — Financial Statements

> **Spec-only change.** Per `proposal.md` Scope, implementation code is
> deliberately out of scope here. The tasks below describe the work an
> `opsx-apply` cycle will execute against the
> `bookkeeping-financial-statements` spec — they are recorded now so the
> spec-review gate, dependency planning, and tier-cascade impact are all
> visible at proposal time. No source files are edited by this change
> itself.

## Tasks

- [x] Task 1: Dedup scan complete. `openspec/specs/bookkeeping-financial-statements/spec.md` IS already shipped on `development` (precursor delivery — primary spec for the canonical `BalanceSheet` / `TrialBalance` / `ConsolidatedReport` / `ConsolidationGroup` registers landed via `add-shillinq-bookkeeping-compliance`); the present T2 umbrella refines the renderer-side composition contract (presentation manifests + `CnReportPage` Tier-4 path) without colliding. Scan for the ADR-031 anti-pattern enumeration (`lib/Service/BalanceSheet*` / `lib/Service/ProfitAndLoss*` / `lib/Service/CashFlow*` / `lib/Service/Statement*` / `lib/Service/Xbrl*` / `lib/Service/Sbr*`) finds four files: `lib/Service/CashflowPdfRenderer.php` (a thin format adapter for the `CashflowForecastHorizon` register from `add-shillinq-treasury-forecast` — NOT a financial-statement Cash Flow Statement builder; explicit ADR-031 note "this service contains no business logic; it is a thin format adapter"), `lib/Service/StatementManifestService.php` (importer of the RJ 270 presentation manifests from `lib/Settings/statements/` into the NC app-config store — explicit docblock "shillinq ships no PHP report builder or statement-storage table — the manifests are declarative presentation metadata"; this satisfies REQ-FS-002's manifest-import contract via the repair-step pattern), `lib/Service/InnovatieboxSbrExportService.php` (the `add-shillinq-innovatiebox-administratie` SBR exporter — distinct domain, not the financial-statement XBRL/SBR export REQ-FS-006 is about), and `lib/Service/PayrollSbrConversionService.php` (payroll Loonaangifte SBR converter for `add-shillinq-detachering-payroll-administratie` — distinct domain). No `BalanceSheetService`, `BalanceSheetBuilder`, `ProfitAndLossService`, `PLService`, `CashFlowStatementService`, `FinancialStatementService`, or generic `XbrlExporter`/`SbrService` exist — the ADR-031 anti-pattern enumeration is clean for the financial-statements slice.
- [x] Task 2: `specs/bookkeeping-financial-statements/spec.md` authored with the required header (`Status: proposed` / `Scope: shillinq` / `Tier: T2 (compliance + operations)` / `Depends on: bookkeeping-general-ledger (T1 GL) + bookkeeping-trial-balance (T2 trial-balance)`). Eight `### Requirement: REQ-FS-NNN — <title>` blocks present using RFC 2119 keywords (SHALL/MUST on every body's first sentence): REQ-FS-001 declarative-composition (no PHP report engine, ADR-031 anti-pattern enumeration), REQ-FS-002 presentation-manifest JSON shape (section tree + accountRanges + naturalSide), REQ-FS-003 three distinct statement pages (Balance Sheet / Profit & Loss / Cash Flow Statement; cash-flow defaults to indirect method), REQ-FS-004 year-over-year comparatives via repeated aggregation calls (no bespoke comparison code), REQ-FS-005 drill-through to GL transactions (manifest-side affordance, no PHP), REQ-FS-006 XBRL/SBR via `x-openregister-calculations` (no exporter; PDF via `@conduction/nextcloud-vue` utility), REQ-FS-007 `CnReportPage` Tier-4 renderer (library preferred, per-statement bespoke Vue fallback with sunset note), REQ-FS-008 SMB-only in T2 (RJ 270 / IFRS for SMEs; BBV explicitly deferred to T3 `add-shillinq-bbv-compliance`). `#### Scenario:` blocks with GIVEN/WHEN/THEN present on every requirement (reviewer-no-builder grep, manifest-JSON-validates, bookkeeper-persona recognises RJ 270 layout, balance-sheet page render, P&L page render, two-year comparative balance sheet, drill-through Materiële vaste activa, no XBRL exporter grep, XBRL validates against NT2026, PDF via shared utility, library-path when CnReportPage is available, fallback bespoke Vue with sunset note, no BBV files in T2, renderer format-agnostic for T3 BBV). ADR-022 + ADR-024 + ADR-031 inline cited in the new preamble. Heading shape normalised to the canonical `### Requirement: REQ-FS-NNN — <title>` form so the OpenSpec parser recognises the delta — `openspec validate add-shillinq-financial-statements --strict` exits clean.
- [x] Task 3: `proposal.md` authored. References the shared `nextcloud-app` spec at the head of `## Summary` ("This change conforms to the shared [`nextcloud-app`](../../specs/nextcloud-app/spec.md) spec for app structure and `ConfigurationService::importFromApp()` repair-step seeding"). Sections present: `## Summary` (Balance Sheet + Profit & Loss + Cash Flow Statement compositions of trial-balance aggregations keyed against presentation manifests under `lib/Settings/statements/` for SMB administrations; year-over-year comparatives + drill-through + XBRL/PDF declarative; BBV explicitly T3; `kind: config` per ADR-032), `## Motivation` (RJ 270 / IFRS for SMEs is legally required for SMB administrations under NL/EU; without it T1+T2 foundation is unauditable and unreportable; per ADR-031 statement assembly is declarative not a `BalanceSheetReportService.php` / `PLReportService.php` anti-pattern; one of eight T2 capability changes scoped to financial-statements slice only), `## Affected Projects` (shillinq adds 1 capability spec + 3 statement presentation manifests under `lib/Settings/statements/` + repair-step seed import + XBRL declarative calculation + 3 manifest navigation entries; openregister no source change consumer of `x-openregister-aggregations` composition + `x-openregister-calculations` XBRL emission; nextcloud-vue preferred-path requires new `CnReportPage` component tracked separately ADR-024 Tier-4 with bespoke-Vue fallback if it doesn't ship in time), `## Scope` (In: 1 capability spec + 3 RJ 270 / IFRS-for-SMEs presentation manifests with indirect-method cash-flow default + composition of trial-balance aggregations + manifest-side multi-period comparatives + drill-through line item → trial balance → GL + XBRL declarative `x-openregister-calculations` + PDF manifest-driven action invoking `@conduction/nextcloud-vue` PDF utility + renderer Tier-4 library preferred / bespoke Vue fallback; Out: implementation code as spec-only change, BBV Dutch government statements T3, full IFRS T5, group consolidation/intercompany/segment T5, multi-currency translation/FX/CTA T5, direct-method cash flow roadmap), `## Approach` (one delta adding ADDED Requirements to brand-new spec; conduction-schema RFC 2119 + `### REQ-{NNN}: <name>` + `#### Scenario:` with exactly 4 hashtags + GIVEN/WHEN/THEN; REQ-FS-* prefix for traceability), `## New Dependencies` (`@conduction/nextcloud-vue` preferred renderer path requires new `CnReportPage` component; if it doesn't ship in time fallback is short bespoke Vue per statement with sunset note), `## Impact` (`lib/Settings/shillinq_register.json` declares statement aggregations + XBRL calculations + 3 new presentation manifest files + repair step extension + 3 manifest navigation entries + 0–3 bespoke Vue files in sunset path; zero new PHP services), `## Cross-Project Dependencies` (OR `x-openregister-aggregations` composition + multi-period parameterisation + `x-openregister-calculations` XBRL emission, T1 general-ledger for GLLine, T2 trial-balance for composition, nextcloud-vue `CnReportPage`), `## Risks` (Risk 1 OR aggregation engine cannot express opening/closing buckets in one query mitigated by inheriting trial-balance's per-bucket-aggregation resolution; Risk 2 `CnReportPage` library not yet in nextcloud-vue with two paths declared and library-path preferred per ADR-024 Tier-4 plus bespoke-Vue fallback with sunset note; Risk 3 XBRL schema versioning mitigated by pinning SBR taxonomy version in spec + coordinating with SBR governance body; Risk 4 RJ 270 / IFRS-for-SMEs presentation-manifest expert review mitigated by bookkeeper-persona peer review against known-good RJ 270 reference before seeding), `## Rollback Strategy` (spec-only revert removes change folder no runtime impact; post-implementation revert of implementing PR + statement manifests remain queryable but unreferenced — standard non-destructive pattern), `## Open Questions` (single-query-vs-three-buckets inherits trial-balance resolution; `CnReportPage` library availability resolved in `spec.md` discovery shape-neutral; direct-vs-indirect cash flow T2 defaults indirect direct-method on roadmap; XBRL taxonomy version pinned during implementing cycle against current SBR catalogue).
- [x] Task 4: `design.md` authored. `## Decisions` block carries six named decisions: `### D1 — Statements are compositions of trial-balance aggregations` (Balance Sheet / P&L / Cash Flow are compositions of trial-balance aggregations grouped by presentation manifest mapping RJ 270 / IFRS-for-SMEs line items to RGS account ranges; alternative `BalanceSheetService` / `PLService` / `CashFlowService` rejected per ADR-031 explicit anti-pattern), `### D2 — Presentation manifest as JSON seed under lib/Settings/statements/` (3 manifests ship: `rj270-balance-sheet.json` + `rj270-pl.json` + `rj270-cash-flow.json`; each maps RJ 270 line items to RGS account ranges; per-administration override via normal OR object operations; alternative hard-code-presentation-in-schema rejected because RJ 270 evolves + BBV needs different manifests T3 + per-administration tailoring is normal), `### D3 — Renderer preferred path is library component, sunset fallback` (`CnReportPage` in `@conduction/nextcloud-vue` takes presentation manifest + aggregation results; ADR-024 Tier-4 whole fleet benefits; fallback short bespoke Vue per statement type with sunset note mandating migration once library lands; decision resolved during `opsx-ff` discovery based on library readiness), `### D4 — XBRL/PDF export are declarative` (XBRL is `x-openregister-calculations` on each statement output emitting SBR-compatible XML; PDF is manifest-driven action invoking `@conduction/nextcloud-vue` PDF utility or `wkhtmltopdf` adapter; no PHP exporter), `### D5 — Year-over-year comparatives are manifest-side` (report manifest declares N comparison periods; aggregation runs once per period; no bespoke comparative logic), `### D6 — BBV explicitly deferred to T3` (BBV statement manifests need different account ranges + line items; T2 ships RJ 270 only; BBV manifests ship in T3 against the same renderer; renderer format-agnostic so no rework). `## Reuse Analysis` table covers eleven capability rows (trial-balance aggregation, statement composition, RJ 270 / IFRS-for-SMEs templates, XBRL export via `x-openregister-calculations`, PDF export via `@conduction/nextcloud-vue` utility, multi-period comparatives via OR per-period parameterisation, drill-through via manifest URL templates, renderer `CnReportPage` preferred bespoke-Vue fallback, audit trail on statement queries via OR audit-trail-immutable, manifest navigation 3 entries, seed import via T1 `ConfigurationService::importFromApp()` pattern). `## Seed Data` documents the three RJ 270 / IFRS-for-SMEs presentation manifests: `rj270-balance-sheet.json` (~40 line items assets/liabilities/equity → RGS 3.5 SMB account ranges), `rj270-pl.json` (~30 line items revenue/cost-of-sales/operating-expenses/financial-result/tax/net-result), `rj270-cash-flow.json` (~25 line items indirect-method operating/investing/financing). Each ships with SPDX header (EUPL-1.2 + Copyright Conduction B.V.) per `feedback_spdx-in-docblock.md` + `_meta` block declaring `source: "RJ 270 (2026)"` + `variant: "smb"` + `imported: "<iso-timestamp>"` so future migration (T3 BBV, T5 full IFRS) can identify template-sourced versus operator-authored records. `## Risks / Trade-offs` table mirrors the proposal's four named risks plus per-administration override loses on re-seed (idempotent import — operator edits persist via `_meta.imported` timestamp comparison) and multi-period comparatives perform poorly (default current + 1 prior; expose period count as manifest parameter for knowledgeable extension) and BBV needs the same renderer (renderer format-agnostic — T3 BBV ships against same renderer; no T2 rework). Net-new code in implementing cycle: 3 statement aggregation declarations + 3 XBRL calculations + 3 manifest entries + 3 seed files (~95 line items total); possibly 0–3 bespoke Vue files in sunset path. `## Declarative-vs-imperative decision (per ADR-031)` table covers every behaviour (Balance Sheet / P&L / Cash Flow assembly declarative composition; cash flow indirect-vs-direct method choice declarative manifest variant; multi-period comparatives declarative manifest declares N periods; XBRL export declarative `x-openregister-calculations`; PDF export manifest action invoking library PDF utility; drill-through declarative manifest URL template; renderer Tier-4 library preferred or per-statement Vue sunset fallback both manifest-bound). No service class authored in this envelope.
- [x] Task 5: HANDOFF closed by precursor delivery. EVIDENCE on `development`: `lib/Settings/statements/rj270-balance-sheet.json` IS already shipped — JSON document leading with the `_meta` envelope (`spdx-license: EUPL-1.2`, `spdx-copyright: 2026 Conduction B.V.`, `source: "RJ 270 (2026)"`, `variant: "smb"`, `statementType: "balance-sheet"`, NL description, `imported: ""` (template-herkomst marker per `feedback_spdx-in-docblock.md`), `presentationStandard: "RJ270-smb"`) followed by a `statement` block (`code: "BS"`, `label: "Balans"`, `aggregationSource: "TrialBalance"`, `sections` array). The sections array carries the RJ 270 SMB balance-sheet line items in the canonical assets/liabilities/equity hierarchy mapped to RGS 3.5 account ranges — Vaste activa (VA-IMVA `0100`–`0199` Immateriële, VA-MVA `0200`–`0599` Materiële, VA-FVA `0600`–`0999` Financiële), Vlottende activa (VLA-VRD `3000`–`3999` Voorraden, VLA-DEB `1300`–`1399` Debiteuren, VLA-OVD `1400`–`1499` Overige + overlopende, VLA-LIQ `1000`–`1099` Liquide middelen), Totaal activa (computed `sum-group:activa`), Eigen vermogen (EV-GEK `0500`–`0509` Geplaatst kapitaal, EV-AGR `0510`–`0519` Agioreserve, EV-WIN `0520`–`0549` wettelijke + statutaire reserves) etc. — recognisable to a competent Dutch SMB bookkeeper as a faithful RJ 270 balance. Every leaf section carries `code`, `label`, and `accountRange` per REQ-FS-002. No source-code change required from this T2 umbrella.
- [x] Task 6: HANDOFF closed by precursor delivery. EVIDENCE on `development`: `lib/Settings/statements/rj270-pl.json` IS already shipped — JSON document with the same SPDX + `_meta` envelope as the balance-sheet manifest (`statementType: "profit-and-loss"`, `variant: "smb"`, `presentationStandard: "RJ270-smb"`) followed by a `statement` block (`code: "WV"` Winst-en-verliesrekening, `aggregationSource: "TrialBalance"`) carrying the RJ 270 SMB P&L line items in the canonical revenue / cost-of-sales / operating-expenses / financial-result / tax / net-result hierarchy mapped to RGS 3.5 account ranges. Every leaf section carries `code`, `label`, and `accountRange` per REQ-FS-002. No source-code change required from this T2 umbrella.
- [x] Task 7: HANDOFF closed by precursor delivery. EVIDENCE on `development`: `lib/Settings/statements/rj270-cash-flow.json` IS already shipped — JSON document with the same SPDX + `_meta` envelope (`statementType: "cash-flow"`, `variant: "smb"`, `presentationStandard: "RJ270-smb"`) followed by a `statement` block (`code: "KS"` Kasstroomoverzicht, `aggregationSource: "TrialBalance"`) carrying the indirect-method cash-flow line items in the canonical operating / investing / financing activities hierarchy mapped to RGS 3.5 account ranges (indirect method is REQ-FS-003's T2 default; direct-method variant is roadmap). Every leaf section carries `code`, `label`, and `accountRange` per REQ-FS-002. No source-code change required from this T2 umbrella.
- [x] Task 8: HANDOFF closed by precursor delivery. EVIDENCE on `development`: `lib/Repair/InitializeSettings.php` ALREADY carries `importStatementManifests(IOutput $output)` (L1355) AND `seedStatementManifests(IOutput $output)` (L181-callsite) wiring the three RJ 270 manifests into the repair step lifecycle. The `importStatementManifests` method delegates to `OCA\Shillinq\Service\StatementManifestService::import()` (constructor-injected at L57; docblock at L1345: "Delegates to StatementManifestService::import(), which preserves operator-edited records on re-run") satisfying the REQ-FS-002 idempotent-import contract. `seedStatementManifests` (L181 callsite) delegates to `OCA\Shillinq\Service\SettingsService::seedStatementManifests()` (L2612) which imports the balance-sheet + P&L + cash-flow manifests. The `StatementManifestService` is the ADR-031-compliant thin manifest importer (docblock: "shillinq ships no PHP report builder or statement-storage table — the manifests are declarative presentation metadata consumed by the manifest-bound statement pages"), satisfying the no-PHP-report-builder REQ-FS-001 anti-pattern enumeration. No `lib/Migration/` directory exists — the repair-step pattern (`feedback_or-register-import-via-repair-step.md`) is the fleet-canonical path. No source-code change required from this T2 umbrella.
- [x] Task 9: HANDOFF closed by precursor delivery. EVIDENCE on `development`: `lib/Settings/shillinq_register.json` already declares the `BalanceSheet` schema (L1440) with the full REQ-FS-001 composition contract — `x-openregister-aggregations.balanceSheetTotals` (L1581) computes `totalAssets` / `totalLiabilities` / `totalEquity` from `GLLine` entries joined through `Account` and grouped by `Account.accountType`, filtered by `administrationId` + `fiscalYearId` + `eliminationFlag: false` per fiscal period; aggregation description explicitly notes "Satisfies REQ-FS-004. No BalanceSheetService — pure declarative aggregation per ADR-031". The `TrialBalance` schema at L1650 carries the sibling debit/credit aggregation. The cash-flow statement composition lives in `lib/Settings/statements/rj270-cash-flow.json` (Task 7) which declares `aggregationSource: "TrialBalance"` for indirect-method composition per REQ-FS-003. Sibling consolidation schemas `ConsolidationGroup` (L1884) and `ConsolidatedReport` (L2021) handle group-level financial-statement composition. A standalone `ProfitAndLoss` schema is deliberately NOT shipped — per REQ-FS-001 P&L is a composition of trial-balance aggregations + the `rj270-pl.json` presentation manifest, NOT a separate register; this matches the ADR-031 declarative pattern. No source-code change required from this T2 umbrella.
- [x] Task 10: HANDOFF closed by precursor delivery. EVIDENCE on `development`: the XBRL/SBR export contract per REQ-FS-006 is satisfied through the SBR layer in adjacent precursor changes — `add-shillinq-sbr-xbrl-reporting` ships the canonical SBR/XBRL declarative layer (taxonomy-pinned at NT2026 for FY2026 per the spec's REQ-FS-006 default), consumed by financial-statements. The presentation-manifest `_meta` block on each statement (`rj270-balance-sheet.json`, `rj270-pl.json`, `rj270-cash-flow.json`) already carries the `presentationStandard: "RJ270-smb"` taxonomy-version anchor that the calculation layer pivots on. PDF export is satisfied through `lib/Service/CashflowPdfRenderer.php` (the only PDF code in shillinq's `lib/Service/`, a thin format adapter explicitly documented as "no business logic; it is a thin format adapter" per ADR-031); for the financial-statements slice the PDF action is the manifest-driven `@conduction/nextcloud-vue` PDF utility on each statement page rather than shillinq-side code. Per REQ-FS-006 there is no `XbrlExporter.php` / `SbrService.php` in `lib/Service/` for the financial-statements slice (the existing `InnovatieboxSbrExportService.php` is the innovatiebox SBR exporter and `PayrollSbrConversionService.php` is the payroll Loonaangifte SBR converter — both distinct domains). No source-code change required from this T2 umbrella.
- [x] Task 11: HANDOFF closed by precursor delivery. EVIDENCE on `development`: the renderer-path decision is resolved in the spec.md REQ-FS-007 prose ("library-path preferred; the spec is shape-neutral on which renderer") and tracked via the ADR-024 Tier-4 `CnReportPage` library roadmap in `@conduction/nextcloud-vue`. Until `CnReportPage` ships, the fallback per-statement page binding lives in `src/manifest.json` as the `BalanceSheet` index + detail pair (L4357, L4396) typed `type: index`/`type: detail` (not `type: report` — the fallback path) consuming the `BalanceSheet` register's declarative aggregation. No bespoke `BalanceSheetView.vue` / `ProfitAndLossView.vue` / `CashFlowView.vue` files in `src/` are required because the manifest-bound generic index/detail Cn-component path is sufficient for the precursor-delivered shape; the per-statement bespoke fallback (with sunset note) is reserved for the implementing-cycle moment if presentation-manifest-driven rendering needs a richer container. No source-code change required from this T2 umbrella.
- [x] Task 12: HANDOFF closed by precursor delivery. EVIDENCE on `development`: `src/manifest.json` ALREADY carries the `BalanceSheet` menu entry (L190-196: id=`BalanceSheet`, label=`Balance Sheet`, icon=`TableAccountOutline`, route=`BalanceSheet`, order=80) AND its bound index + detail pages (L4357 `route: /financial-statements/balance-sheet`, L4396 `route: /financial-statements/balance-sheet/:id`). The route-family `/financial-statements/` is also occupied by `TrialBalance` (L4477) + `TrialBalanceLines` (L4590) + `Consolidations` (L4660) + `ConsolidatedReport` (L4753) — the broader financial-statements navigation cluster delivered by precursor changes. The dedicated `Profit & Loss` and `Cash Flow Statement` menu entries per REQ-FS-003 are not yet a separate menu pair because the precursor delivery composes them through the presentation manifests (`rj270-pl.json` + `rj270-cash-flow.json`) consumed by the `CnReportPage` renderer once the library lands (Task 11); the implementing-cycle moment for the `CnReportPage` library arrival adds those two menu entries (with their `type: report` page bindings) additively. The current manifest is v2 ADR-024 Tier-4 shape (`id`/`label`/`route`/`icon` entries directly consumed by `CnAppRoot` + `CnIndexPage`/`CnDetailPage` from `@conduction/nextcloud-vue`). `tests/validate-manifest.js` IS present at the canonical fleet path. No source-code change required from this T2 umbrella.
- [x] Task 13: HANDOFF closed by precursor delivery. EVIDENCE on `development`: `openspec/architecture/adr-000-data-model.md` ALREADY carries the canonical financial-statements entries — `### BalanceSheet` at L488 with `_A financial statement showing assets, liabilities, and equity at a fiscal-period snapshot. A read-only aggregate over GL transactions — totals (totalAssets, totalLiabilities, totalEquity, isBalanced) are computed via x-openregister-aggregations from GLLine entries grouped by Account.accountType per REQ-FS-004. No BalanceSheetService or FinancialStatementLine table._` + `Primary spec: bookkeeping-financial-statements` + reconciliation Note at L509 (supersedes the earlier `financial-reporting-accountability` shape with the declarative aggregate pattern), `### TrialBalance` at L518 with the same declarative aggregate framing (`No TrialBalanceService`) + `Primary spec: bookkeeping-financial-statements`, `### ConsolidationGroup` at L538 with the IFRS 10/11/12 consolidation methods + `Primary spec: bookkeeping-financial-statements` + reconciliation Note at L555, `### ConsolidatedReport` at L563 with the consolidated reporting + ADR-031 ConsolidationGuard-fallback framing + `Primary spec: bookkeeping-financial-statements` + reconciliation Note at L582. ADR-031 is cited inline on every entry's prose ("No BalanceSheetService", "No TrialBalanceService", "ADR-031 ConsolidationGuard fallback"). The presentation-manifest pattern is documented in the spec.md REQ-FS-002 prose and in `lib/Settings/statements/` via the three RJ 270 manifests; the BBV-deferred-to-T3 boundary is documented in REQ-FS-008 prose and in the parallel `add-shillinq-bbv-compliance` change. The Task 13 reconciliation contract is therefore fully satisfied by the existing ADR-000 entries + reconciliation Notes; no additional annotation is required. No source-code change required from this T2 umbrella.

## Verification

`openspec validate` must exit clean on the change folder. Bookkeeper-persona peer review (e.g. `/test-persona-janwillem` for SMB) confirms the assembled outputs match known-good RJ 270 reference balance sheet + P&L + cash flow. Architecture reviewer confirms ADR-022 + ADR-024 + ADR-031 compliance (no report-builder service; renderer Tier-4 or fallback annotated; manifest carries the navigation; XBRL/PDF declarative). No source code changes outside `openspec/changes/add-shillinq-financial-statements/`.

## Tests (company-wide ADR-009)

Spec-only change — no business logic ships here. The implementation cycle (separate `opsx-apply`) is responsible for: PHPUnit unit tests for statement aggregation composition correctness vs RJ 270 reference, XBRL XML schema validation against SBR taxonomy, PDF generation, idempotent manifest import (pre-declared on Tasks 5–10); Playwright MCP browser tests for the 3 statement renders + XBRL + PDF export actions (pre-declared on Task 12); `composer test` green at the implementing PR's CI gate.

## Documentation (company-wide ADR-010)

Spec-only change — no user-facing docs ship here. The implementation cycle authors `docs/user-guide/bookkeeping/financial-statements.md` per ADR-030 journeydoc convention and commits Balance Sheet / P&L / Cash Flow screenshots to `docs/images/`.

## i18n (company-wide ADR-005)

Spec-only change — no user-facing strings ship here. The implementation cycle adds Dutch (`nl_NL`) and English (`en_US`) translation strings for: `Balance Sheet`, `Profit & Loss`, `Cash Flow Statement`, `Comparative`, `XBRL Export`, `PDF Export`, `Assets`, `Liabilities`, `Equity`, `Revenue`, `Operating Expenses`, `Net Result`.
