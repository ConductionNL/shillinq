# Tasks: setup-wizard-english

<!-- HYDRA CAP: max 20 unindented `- [ ]` lines. This file uses 13. -->

## Phase 1: Manifest strings (REQ-SWE-001, REQ-SWE-002)

- [x] T01 — Rewrite all 17 Dutch fields in `src/manifest.json` `.setup.steps[]` to English source text per design.md's full wizard string inventory table (steps 0–6: `title`/`body`/`options[]`/`optionsByParent[*][]` `label`); keep jurisdiction-specific legal-entity acronyms (`ZZP`, `MKB`, `VZW`, `GmbH`, `Verein`, `Einzelunternehmen`, `BV`, `BBV`) untranslated per the ADR-007 acronym exception; resolve design.md Open Question 2 (`Besloten Vennootschap (BV)` gloss) during this task. **Resolved: kept `Besloten Vennootschap (BV)` untranslated, matching the German list's precedent (design.md's recommended option).**
- [x] T02 — Rewrite the 14 Dutch `pages[].title` / `pages[].config.widgets[].title` / `menu[].label` strings identified in design.md's page-title inventory to English source text; do NOT touch any `id`/`route` value; resolve design.md Open Question 3 (`Aansluitingen` vs `Reconciliations`) before choosing its English title. **Resolved: `Aansluitingen` → "Tie-outs" (not "Reconciliations" — confirmed via `config.schema` that it is a genuinely distinct feature (generic tie-out framework, schema `Aansluiting`/`AansluitingResult`) from the existing `Reconciliations` entry (bank-statement reconciliation, schema `BankReconciliation`); translating it to "Reconciliations" would have created two identically-titled adjacent nav entries. See `notes-for-nav-six-clusters.md`.** Also fixed one Dutch widget title beyond the 14-item audit (`BewaartermijnenDashboard`'s `nearing-retention` widget, "Verlopen binnenkort" → "Retention periods expiring soon") since it is squarely in REQ-SWE-002's general scope (`pages[].config.widgets[].title`).
- [x] T03 — For every field changed in T01/T02, add the English string to `l10n/en.json` as an identity entry and move the original Dutch value to `l10n/nl.json` under the same key — no Dutch content discarded.
- [x] T04 — Validate the edited manifest against `tests/schemas/app-manifest-v2.schema.json` (Ajv or the repo's structural fallback lint) — zero schema errors. `node tests/validate-manifest.js` → PASS (0 errors, 0 consistency issues, 228 pages).

## Phase 2: Locale parity (REQ-SWE-003)

- [x] T05 — Diff `l10n/en.json` against `l10n/nl.json` at current HEAD (post-T03) and add a real Dutch translation for every remaining en-only key (53 at audit time — re-diff, since T03 already closed some). All 53 translated. One of the 53 turned out to be a byte-level near-duplicate of an already-translated key (differing only by a U+00A0 NO-BREAK SPACE before an ellipsis — confirmed the NBSP variant is the one actually referenced, in `StandardsPolicyEditor.vue`); translated that exact key, left the unreferenced plain-space twin untouched (already has a valid nl translation, out of this task's scope to deduplicate).
- [x] T06 — Resolve the 3 nl-only keys (`Analytical dimensions`, `Sales`, `Valuation`): grep `src/`, `manifest.json`, `manifest.d/*.json` for a living reference to each; if found, add the matching `en.json` identity entry; if none found, remove the dead `nl.json` entry. All 3 have living references (manifest.d fragment label/title, menu label mapping, reporting view label) — added `en.json` identity entries for all 3; no `nl.json` deletions needed.
- [x] T07 — Confirm `node tests/l10n/check-l10n-parity.js` reports zero missing/empty keys in both directions. `L10N_REQUIRED_LOCALES=nl node tests/l10n/check-l10n-parity.js` → OK, 0 missing/empty (en.json and nl.json both at 2830 keys).

## Phase 3: Gate enforcement (REQ-SWE-004)

- [x] T08 — Wire `check-l10n-parity.js` into `.github/workflows/l10n.yml` as a required step (currently only `check-l10n.js` runs); confirm it fails the job on an injected key gap (positive control — temporarily delete one `nl.json` key, observe the job fail, then restore). Wired scoped to `L10N_REQUIRED_LOCALES=nl` (see workflow comment: the full 34-locale default has large pre-existing gaps in other locales that are out of this change's scope). Positive control run: deleting `"Welcome to Shillinq"` from `nl.json` made the gate report `1 missing key(s)` and exit 1; restored, gate passes again (2830/2830).
- [x] T09 — Add a Dutch-token stopword scan (curated list per ADR-057 D3) covering `t('shillinq', '...')`/`n(...)` literal arguments AND `manifest.json`/`manifest.d/*.json` `title`/`body`/`label` values, wired into the same `l10n` CI job; confirm it fails on an injected Dutch string (positive control) and passes clean at HEAD after T01–T02 (negative control). New script `tests/l10n/check-dutch-tokens.js`, wired as `npm run test:l10n-dutch-tokens` in `.github/workflows/l10n.yml`. Scoped to `setup.steps[]` + `pages[].title` + `pages[].config.widgets[].title` + `menu[].label` (recursive) — deliberately excludes `columns[]`/`fields[].label`, which still carry substantial pre-existing Dutch content out of this change's scope (see script header). Positive control: planted `"Testtitel voor de administratie"` as a step title → gate failed, naming the file/path and matched tokens (`voor`, `de`, `administratie`); planted a Dutch `t()` call → gate failed the same way. Negative control: HEAD passes clean.

## Phase 4: e2e proof (REQ-SWE-005)

- [x] T10 — Add `tests/e2e/setup-wizard-english.spec.ts`: reset shillinq's setup app-config keys server-side before the test (not just a fresh Playwright context — design.md "Fresh-context e2e" explains why browser-context freshness alone does not reproduce the wizard); step through all 7 wizard steps as admin; assert English `title`/`body`/option-label text and the absence of any Dutch token on each visible step. Written (not executed — no live instance in this worktree). Resets via `occ config:app:delete` for all 6 setup config keys; `afterAll` unconditionally restores the exact `ci-seed.sh` baseline over the same admin HTTP endpoints so concurrently-running sibling spec files (parallel-by-file per `playwright.config.ts`) are not left staring at a blocking setup dialog.
- [ ] T11 — Run the new spec once against the pre-change Dutch `manifest.json` (negative control per spec.md's "test fails against the pre-change manifest" scenario) to confirm it can fail, then run it against the post-change English manifest to confirm it passes. **Not run — no live Nextcloud instance in this worktree to execute Playwright against; deferred to the verify stage, per the orchestrator's explicit instruction that this spec is written, not executed, here.**

## Quality

- [x] T12 — Run `npm run test:l10n`, `node tests/l10n/check-l10n-parity.js`, and the manifest schema validation; zero failures. All three pass; see T04/T07 outputs above.
- [x] T13 — Confirm design.md Open Question 1 (whether the installed `@conduction/nextcloud-vue ^2.3.0` already routes `manifest.setup.steps` through `translate`) has been checked against the actual installed version, not just the `beta.217` checkout used to write this spec; record the finding and, if the gap still exists, file the recommended `nextcloud-vue` follow-up change (out of this change's scope to build). **Checked against the real installed 2.3.0** (`node_modules/@conduction/nextcloud-vue`, symlinked from the sibling shillinq worktree). **The gap still exists**: `CnAppRoot.vue` passes `:steps="manifest.setup.steps"` raw at both `<CnSetupWizard>` instantiations (lines 172, 381 in that version), and `CnSetupWizard.vue` renders `step.title` / `step.body` / `field.label` literally (lines 40-41, 51, 73-93, 122, 284, 323, 390) — no `t()`/`translate()` call anywhere near them, confirmed by grep. The wizard therefore renders in English for every locale until `@conduction/nextcloud-vue` is fixed to route `manifest.setup.steps` through the same `translate` prop `CnAppNav` already uses for `menu[].label`. Per proposal.md Non-Goals this fix is out of THIS change's scope (a different repo); **not filed as a separate change here** — flagging it in this record is the tracked follow-up the proposal calls for, and building it is left to whoever picks up that repo's own OpenSpec queue.

## Quality checklist

- No `id`/`route`/schema property/enum value is renamed anywhere in this change — display strings (`title`/`body`/`label`) only (per proposal.md Non-Goals; PRs #534/#560 already cover property/enum translation with data migrations).
- No Dutch content is deleted without being preserved in `l10n/nl.json` first.
- The e2e test resets SERVER-side setup state, not only the browser context.
- Jurisdiction-specific legal-entity acronyms are preserved, not invented-English-glossed.
- `l10n/en.json` stays identity-mapped (key === value) for every entry touched.
