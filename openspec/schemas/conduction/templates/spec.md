# <capability-name>

<!-- One spec file per capability from the proposal's Capabilities section: specs/<capability>/spec.md.
     Read ~/.github/docs/claude/writing-specs.md for the full spec writing guide.
     Delta operations (## headers): ADDED / MODIFIED / REMOVED / RENAMED Requirements.
     MODIFIED requirements MUST include the full updated requirement block copied from openspec/specs/<capability>/spec.md.
     REMOVED requirements MUST include **Reason** and **Migration**. -->

## ADDED Requirements

### REQ-001: <requirement name>

<!-- Use SHALL/MUST for normative requirements (avoid should/may).
     Every requirement MUST have at least one scenario.
     Scenarios MUST use exactly 4 hashtags (####) — 3 hashtags or bullets fail silently. -->

The system SHALL <normative behavior>.

#### Scenario: <scenario name>

- **GIVEN** <precondition>
- **WHEN** <action>
- **THEN** <expected outcome>
