# PHPStan baseline — only irreducible violations that cannot be fixed in code.
# Regenerate with: ./vendor/bin/phpstan analyse --generate-baseline
# Entries removed as the underlying code is refactored.

parameters:
	ignoreErrors:
